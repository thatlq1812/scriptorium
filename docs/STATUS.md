# Status — Scriptorium

| Last Updated | Status |
| 2026-08-15 | thatlq1812 asked what upgrading the `design` cluster with real web/UI/HTML skills would look like; recommended scoping narrowly (not a full "website builder", matching this project's own past decision to avoid "red-ocean complex image-gen or Figma clones" when the Light Design cluster was first scoped) and asked what output shape was wanted. thatlq1812 then asked to read a specific real repo, `nextlevelbuilder/ui-ux-pro-max-skill` — scouted for real (`gh api`, not a blog): **116,839 verified stars, MIT license, updated 2 days prior** — the single largest, highest-code-quality prior-art repo found in this entire session (bigger than the 44,335-star marketing-skills find). Real content confirmed by direct inspection: 7 sub-skills, a real BM25 search engine (not fake), 192 product-type → curated 16-token color palettes, 74 font pairings, 34 real landing-page patterns, a `data-provenance.json` tracking confidence/freshness per record — genuinely rigorous engineering, not a prompt dump. thatlq1812 directed harvesting directly ("làm luôn nhỉ"), reasoning that most non-technical users want a fast webpage draft from content they already have, not deep backend work — the explicit scope brief for what got built. `license-compliance-check` run for real (root LICENSE read directly: clean MIT, no nested override) before any harvest, then `scout-harvester`'s `clone_candidate.py` cloned it to the scratchpad for close reading, per this project's own pipeline order. Built 2 new skills, upgrading `design` and opening a new `webpage` domain — full detail in the "New this session" table below. Registry grew 82 → 84. |
| 2026-08-15 | thatlq1812 directed completing the 2 remaining candidate clusters flagged earlier this session (International Relations/Event Organization, Social Science/Psychology) rather than leaving them fully pending real client elicitation: "nếu không có nguồn, thì ta hoàn toàn có thể dịch ngược sản phẩm, đọc sách... để có được tri thức được chưng cất" (if there's no source, reverse-engineering real products and reading authoritative material is an acceptable way to distill the knowledge needed) -- an explicit thatlq1812 instruction under CLAUDE.md principle 4's 4th elicitation-source category. Quick-scouted both clusters first (WebSearch + `gh search`/`gh api`, real star counts): essentially no prior-art skill packs exist anywhere in this space (0 IR repos, a handful of 0-12-star event repos, a 1-3-star scattering of psychology-adjacent repos) -- unlike Marketing's 44k-star find, nothing to harvest from. Assessed value per sub-area: Events (buildable, real VN regulation exists) and Social-Science-research-methodology (buildable, real peer-reviewed literature exists) were judged general-capability tier; International-Relations-proper (too varied/abstract across real use cases to ground generically) and Psychology-clinical (real ethical/liability stakes, the exact 'AI therapist chatbot' trap this project's Legal cluster already declined) were flagged as needing real elicitation even under the reverse-engineering allowance -- proceeded on the 2 buildable halves plus a deliberately narrow, structural-only treatment of the 2 risk-flagged halves (diplomatic FORMAT only, never content; clinical-note STRUCTURE only, never diagnosis). 4 more deep-research briefs (`references/research_13..16/`, all `VALID`): `research_13` read Nghị định 144/2020/NĐ-CP's actual signed PDF page-by-page (primary source, same discipline as vn-ad-compliance-checker), catching and correcting a real 7-vs-10-working-day discrepancy; `research_14` reverse-engineered the Vienna Convention (1961) + a real US State Department protocol-office page + note-verbale format references; `research_15` grounded in peer-reviewed TRAPD/back-translation survey-methodology literature; `research_16` reverse-engineered SOAP format from a real deployed clinical-documentation product (SimplePractice) + the APA's own official Ethics Code, explicitly flagged as this project's highest-risk grounding (risk_tier N4, `stage4_required: true` under the same 'elevated risk, extended criterion' bucket as `socratic-concept-helper`, and the skill's own SKILL.md states production use requires real practitioner review first as a hard precondition). Built and shipped 4 more flagship skills across 3 brand-new domains: `event-compliance-checker` (`events`), `diplomatic-correspondence-checker` (`diplomacy`), `survey-adaptation-quality-checker` (`academic`), `clinical-note-structure-validator` (`psychology`) -- full detail in the "New this session" table below. Registry grew 78 → 82. `CLAUDE.md`'s domain-folder list updated. |
| 2026-08-15 | thatlq1812 directed completing both clusters rather than stopping at 1 flagship each ("làm đủ, rồi nhận feedback để nâng cấp" -- build the full set first, upgrade from real feedback after, since non-tech users can't articulate what they need but this project can reverse-engineer a skill from real public process/outcome data). Also directed searching Vietnamese-language sources, not just English, for every research pass. 2 more `deep-research` briefs (`references/research_11_business_admin_sop_budget/`, `references/research_12_marketing_funnel_campaign/`, both `VALID`), each explicitly including a Vietnamese-language search round: found Robert's-Rules-equivalent SOP structure is identical in Vietnamese practice (no separate rule set needed), but found a genuinely Vietnam-specific requirement English sources never mention -- a `Giấy đề nghị thanh toán` (payment-request form) must state the amount in words as well as numerals, and a `công tác phí` (business-travel) claim needs a prerequisite travel-authorization document plus a company-tax-code-bearing invoice. Built and shipped 4 more flagship skills (2 per cluster) on this grounding: `sop-structure-validator`/`budget-expense-tracker` (Business), `utm-campaign-governance-linter`/`campaign-funnel-brief-validator` (Marketing) -- full detail in the "New this session" row below. Registry grew 74 → 78. |
| 2026-08-15 | thatlq1812 directed 2 new specializer clusters, Business Administrator and Digital Marketing, with a research-first discipline: hunt real official standards/regulations/JD/course material before scoping any skill, not skill-pack prose. Renamed the root `outside_research/` living-input directory to `references/` first (the `.gitignore` `/references` rule already existed from 2026-08-13's `168272c`) — updated `CLAUDE.md`/`README.md`/`docs/README.md`/`docs/MASTER_CONTEXT.md`/`docs/ROADMAP.md`/`docs/specs/STRATEGY_SPEC.md`'s forward-facing convention text, left every dated historical citation of a specific `outside_research/research_XX_*.md` file untouched (registry `elicited_from`, changelog rows) per this file's own no-revisionist-history discipline. Ran 4 `deep-research` briefs (`references/research_07..10/`, all `VALID` against the skill's own citation checker): `research_07`/`08` (general task-taxonomy + prior-art scouting) found Business Admin's real gap is admin-*operations* tooling (Nghị định 30/2020 document *formatting* already covered by `latex-project-bootstrap`/`office-doc-creator`) and found a real, large MIT marketing-skill-pack (`coreyhaines31/marketingskills`, verified 44,335 stars via `gh api`) with zero Vietnam-specific content; `research_09`/`10` (official-standards follow-up) grounded a Business Admin flagship in Robert's Rules of Order + the RAID-log convention, and grounded a Digital Marketing flagship directly in Nghị định 342/2025/NĐ-CP's own signed PDF text (read page-by-page), catching a real 1.5s-vs-5s ad-close-wait-time discrepancy between an early news report and the final effective decree. Ran `scout-harvester`'s `clone_candidate.py` for real (caught and fixed a real `CLAUDE.md` doc bug in the process — it said to clone into `references/<topic>/`, but the script itself refuses any destination under the repo root; fixed to say "scratchpad, outside the repo") plus `license-compliance-check` (both scouted marketing repos SAFE/MIT). Built and shipped 2 flagship skills, `business`/`marketing` domains' first entries — full detail in the "New this session" row directly below. Full narrative: `docs/ROADMAP.md`'s Business Administrator/Digital Marketing cluster section. |
| 2026-08-15 | thatlq1812 brought `PROJECT.md` (repo-polish items: GitHub description/README refresh, release design, repo versioning, author-attribution correction) plus a separate `START_HERE_INSTRUCT.md` (workspace-onboarding redesign). All executed this round. **(1) GitHub repo**: description set via `gh repo edit` (was empty). `README.md` skill-count corrected (was stale "82 skills as of 2026-08-13", actual registry is 72 — the "82" figure predates the 2026-08-14 deletion round above and was never updated); also removed a stale mention of `svg-poster-builder`/`poster-generator`/`skills/archive/` as still `operational_status: superseded` — that same 2026-08-14 round actually deleted them outright, so the README claim of "not deleted" was already wrong. `CLAUDE.md`'s repo-structure diagram has the same `skills/archive/` staleness, left as a flagged, not-yet-fixed item (out of README's own explicit scope this round). **(2) Repo-level version**: new root `VERSION` file (`YY.MM.NN` scheme, matching sibling project `scriptorium_workspace`'s own convention) plus `scripts/bump_version.ps1` — deliberately a SEPARATE axis from each skill's own per-skill semver in `registry/skills.json` (bumped when a skill's own content changes; `VERSION` bumps when a release snapshot of skills/+registry/ is cut). Verified real: script run twice, `26.08.01`→`26.08.02`. **(3) Author attribution — "owner" → "thatlq1812"**: dispatched to a background agent (full session context, no re-derivation needed) with an explicit rule distinguishing founder-reference "owner" (elicited_from/decided_by/acknowledged_by/prose about who directs this project — always thatlq1812) from domain-content "owner" (PDF `owner_pass`, GitHub `owner/repo` slugs, RACI "Accountable owners", `personal-profile-manager`'s end-user "profile's owner", OOXML theme-code variables) — only the former renamed. 70 files, ~200+ occurrences changed; `docs/archive/` excluded (historical, kept verbatim per this file's own discipline); grammar cleanup applied where a common-noun-to-proper-noun swap needed it ("the owner's" → "thatlq1812's", "an owner-directed" → "a thatlq1812-directed"). Verified: repo-wide case-insensitive re-grep confirmed zero remaining founder-reference "owner"/"Owner" outside `docs/archive/`; spot-checked 5 files with legitimate domain-content "owner" mentions, confirmed untouched; `registry/skills.json` re-parsed as valid JSON after the edit (72 skills, unchanged count). **(4) `project-workspace-initializer` 0.4.0→0.5.0**, grounded in `START_HERE_INSTRUCT.md`'s real observed non-tech-user friction: (a) naming scheme revised again — 0.4.0's always-on `-v1` suffix on a project's FIRST scaffold reverted; a first-time `--name` now produces a bare `<YYYYMMDD>-<name>`, and `-v<N>` is appended only when that `<name>` already exists under `projects_root` (any date) — keeps 0.4.0's collision-safety, drops the always-present version noise thatlq1812 flagged as unwanted (explicit direction: "add -v{N} in last if this name already appear"). All 5 bundled templates' `workspace_md_prompts` naming-convention text updated to match (`legal-practitioner`/`educator`'s client/student-privacy guidance reworded, since `--name` is now literally the folder name). (b) Fixed a real naming-mismatch bug: `AGENTS.md`/`WORKSPACE.md` had documented a `personals/` directory for years that neither this skill nor `personal-profile-manager` ever actually created (`personal-profile-manager` has always used singular `personal/profile.json`, matching `.gitignore`'s pre-existing `/personal` rule) — corrected to `personal/` everywhere current (changelog history left as-is, per this file's own no-revisionist-history discipline). (c) `init_workspace.py` now unconditionally creates `data/`, `documents/`, `personal/` at the workspace root (new `FIXED_WORKSPACE_SUBDIRS`, always created in addition to whatever a template's own `workspace_subdirs` declares) — previously only whatever a template happened to declare existed, so `personal/` was never actually created despite being referenced. (d) `AGENTS.md` gained an explicit "this is a live filesystem workspace, not a chat session" framing line, an explicit rule against leaving a deliverable loose directly under `projects/` without first scaffolding a dated project directory, and a soft `projects/_archive/` convention note (manual, not automated) for finished projects — all grounded in real failure modes thatlq1812 reported from live use (an agent replying with a document's content inline instead of creating the file; an agent writing straight into `projects/`), not hypothetical hardening. Verified real: `scaffold_workspace.py` run 3x against the same `--name` (2x same date, 1x a later date) produced `<date>-<name>`, then `<date>-<name>-v2`, then `<later-date>-<name>-v3` — no collision, no premature suffix; all 5 bundled templates re-run end-to-end through all 3 scripts (`init_workspace.py`→`scaffold_workspace.py`→`init_project.py`), all exit 0; `validate_skill.py` re-run clean. **(5) `START_HERE.md` rewritten** (this file is `.gitignore`d — `/*.md` with only `README.md` excepted — so this is a local-only, not-tracked-in-git change, matching its role as a disposable companion to a raw zip snapshot): sequenced as install→delete-source→elicit-user-context→scaffold+customize-template→conditional-venv-bootstrap→2-key-rules-restated→cleanup, replacing the old flat 4-step list; also fixed a stale `image-generator-gemini` skill-name reference (that skill was merged into `gemini-generator` well before this round). **(6) New `scripts/build_release.py`**: packages `skills/`+`registry/`+`START_HERE.md`(renamed `README.md` inside the zip) into `releases/<VERSION>/scriptorium-<VERSION>.zip`, reading `VERSION` as the single source of truth — the "raw snapshot" release path `START_HERE.md` itself already documented as an alternative to `skill-exporter`'s curated/gated bundle. `/releases` added to `.gitignore` (binary zip artifacts, publish via `gh release create` instead of committing). Verified real: built a test zip (550 files, correct `README.md` rename confirmed via `zipfile.namelist()`), then removed it (was a verification-only build, not an intended release — next real release needs its own fresh `VERSION` bump). |
| 2026-08-14 | thatlq1812 directed 2 core changes from a workspace-side review (`PROJECT.md` change request, non-tech-user pilot workspace at `D:\AppData\my_workspace`). **(1) Deleted, not archived**: with the system still in a testing/pre-production phase and every one of the 10 `operational_status: superseded` skills already having a real, better replacement in active use, thatlq1812 directed actually deleting them rather than keeping the reversible `skills/archive/` holding pattern from the "(cont'd 7)" entry above. Removed `skills/archive/` entirely (`python-env-bootstrap`, `ffmpeg-bootstrap`, `pandoc-bootstrap`, `typst-bootstrap`, `xelatex-bootstrap`, `image-generator-gemini`, `video-generator-gemini`, `audio-generator-gemini`, `svg-poster-builder`, `poster-generator`) and their 10 `registry/skills.json` entries — re-confirmed via registry `dependencies` grep before deleting that zero remaining active skill depends on any of the 10 (all real dependents were already repointed to `toolchain-bootstrap`/`gemini-generator`/`html-poster-composer` in earlier sessions). Registry: 82→72 skills. `socratic-concept-helper` (`operational_status: paused`, not `superseded`) deliberately left untouched — a live open decision pending a real student/teacher survey, not a completed replacement, so it doesn't meet the "already have a better version" bar thatlq1812 gave for this deletion. Narrative mentions of the 10 deleted skill_ids elsewhere in this file, `docs/DECISIONS_PENDING.md`, `README.md`, etc. are left as-is (historical record of what happened and why, same discipline `docs/archive/` already applies) — only the live registry entries and skill folders were removed. **(2) `office-doc-creator` v0.5.0→0.6.0**: added Nghị định 30/2020/NĐ-CP default formatting (A4, Times New Roman, 13pt body text, NĐ30's permitted-range margins) to `create_docx.py`, since every prior document silently inherited python-docx's own US-Letter/Calibri/11pt template unless the caller happened to specify NĐ30-compliant values — a real, thatlq1812-flagged aesthetic gap ("rất quan trọng... ảnh hưởng rất lớn tới thẩm mỹ đầu ra"). New optional top-level `font_size` and `page` (`width_mm`/`height_mm`/`margins_mm`) content-spec fields let a caller override any/all of font/size/layout per document; `page.margins_mm` merges per-key against the NĐ30 defaults. Grounded in the same real NĐ30 government-PDF source `latex-project-bootstrap`'s vnnd30 mode already reads directly (`skills/office/latex-project-bootstrap/references/nd30-2020-formatting.md`) — not a new elicitation. Verified real: built one `.docx` with no font/font_size/page fields and confirmed via python-docx read-back (`sections[0]` page/margins, `styles['Normal'].font.size`, run-level `w:rFonts`) it is exactly A4/TNR/13pt/NĐ30 margins; built a second with `font: Arial`, `font_size: 11`, `page.margins_mm: {top: 25, left: 20}` and confirmed the override applied exactly, with the 2 non-overridden margins (bottom/right) still falling back to the NĐ30 default via the per-key merge. A workspace-conventions review (from the same pilot workspace) surfaced 4 more items, 2 fixed at core this round and 2 with a real structural limit: **(3a) project naming** — `scaffold_workspace.py` (0.3.1→0.4.0) now requires `--name <slug>` and produces `<YYYYMMDD>-v<N>-<name>/` instead of `project_YYYYMMDD_NN`, `<N>` auto-incrementing per project NAME across any date (a redo later gets v2/v3, not a collision or an indistinguishable re-run) — verified real (2 scaffolds of the same `--name` produced v1 then v2; an invalid name correctly refused, exit 2). **(3b) a stricter root `AGENTS.md`** — `init_workspace.py` now also writes `AGENTS.md` alongside `WORKSPACE.md` (same `--force`-gated refusal), with fixed rules covering the naming convention, a mandatory "create a real file inside a scaffolded project, don't just reply inline" rule (answers 3c), a "keep `personals/profile.json` current proactively" rule (answers 3d), plus the pre-existing skill-registry-lookup and shared-resources content — verified real (both files generated correctly, re-init without `--force` refused). **(3c/3d)**, the "agent replies instead of scaffolding" and "personals/ isn't kept current" behaviors, are answered by writing the rule into the new `AGENTS.md` (3b) rather than a script mechanism — a real structural limit, not an oversight: Scriptorium produces static skill artifacts, not a harness, and has no way to force a downstream consuming agent to actually read or obey a workspace's `AGENTS.md`. `project-workspace-initializer`'s registry entry and `SKILL.md` both updated (0.4.0); its frontmatter `description` also trimmed to stay under the 1024-char/no-angle-bracket schema cap after the edit (re-checked via `validate_skill.py`, clean). Both changed skills' installed copies (`office-doc-creator` 0.6.0, `project-workspace-initializer` 0.4.0) synced into the pilot workspace's own `.agents/skills/`; its actual `AGENTS.md`/`WORKSPACE.md` were deliberately NOT force-regenerated (would silently overwrite any manual edits thatlq1812 made) -- left as an explicit action item in the receipt. Full evaluation, verification detail, and 3 open questions for the workspace thatlq1812 (regenerate AGENTS.md now? rename the 6 existing hand-named project folders? build an `org_profile` for what looks like a THCS Lê Lợi deployment?) written to a companion `Scriptorium-core-receipt.md` at the pilot workspace's own root (`D:\AppData\my_workspace\Scriptorium-core-receipt.md`), not this repo -- that file is the thatlq1812-facing evaluation, this entry is the internal changelog record. Full registry re-checked: 82→72 after item 1's deletions, JSON valid; a repo-wide `validate_skill.py` sweep found the same 5 pre-existing invalids as before (4 `skills/office/{docx,pdf,pptx,xlsx}` missing `metadata`, `office-doc-creator`'s long-tracked `compatibility`-length overage) and zero new regressions. |
| 2026-08-13 (cont'd 12) | thatlq1812 directed chaining `office-doc-creator` (MIT-safe, Vietnamese-tailored document creator) into `docx` — office-doc-creator creates, docx picks up editing/comments/tracked-changes/XSD-validation/image-insertion it doesn't do itself. Real, not just documented: built a real `.docx` via `create_docx.py` with a Vietnamese `cover_page` (diacritics á/ề/ộ/ư/ơ/đ) and an ordered list, fed the output straight into `docx`'s `scripts/office/validate.py` and `scripts/comment.py` — both ran directly on the file, no conversion step needed. This first real run of the chain caught **2 genuine bugs**, neither previously known because neither skill had ever been exercised against the other's real output before: (1) `docx`'s `validators/base.py` opened each XML part with `open(xml_file, "r")` — no `encoding=`, which on Windows decodes as the OS codepage (cp1252) rather than UTF-8, so any Vietnamese content raised `'charmap' codec can't decode byte...` before the real XSD check ran at all — i.e. it broke specifically on the content class this whole chain exists to serve. Fixed: open in binary mode so `lxml.etree.parse` reads the encoding from the XML declaration itself (standard-correct pattern). (2) `office-doc-creator`'s `docx_numbering.py` wrote `w:lvlRestart` before `w:numFmt` inside `w:lvl` — `CT_Lvl`'s real OOXML schema sequence is `start, numFmt, lvlRestart, ..., lvlText, ..., lvlJc, pPr, rPr`; the swap was invisible in Word's lenient parser but caught immediately once real XSD validation ran against this skill's own output for the first time. Fixed by reordering the two element-creation calls. Re-ran validation after both fixes: the numbering error and the encoding error are both gone; one pre-existing, unrelated finding remains and was deliberately NOT patched — `word/settings.xml`'s `<w:zoom w:val="bestFit"/>` missing a `w:percent` attribute, confirmed (by grepping office-doc-creator's own scripts) to come from `python-docx`'s own bundled default template, never touched by any script in this skill. `docx` (1.0.0→1.0.1) and `office-doc-creator` (0.3.1→0.4.0) both version-bumped, re-audited (`registry/skills.json`), and cross-documented ("Chains into `docx`" / "Chains from `office-doc-creator`" sections in each `SKILL.md`); `office-doc-creator`'s registry `dependencies` now lists `docx`; `docx`'s registry `harness_compatibility` gained `claude-code` (directly verified running its scripts in this session, per `CLAUDE.md` principle 3 — only direct verification counts). See `docs/DECISIONS_PENDING.md` item 13's addendum for the risk-posture note: this moves the accepted-risk content from "dormant, left in place" to "a real dependency of a skill meant for actual Vietnamese-context use," which thatlq1812 explicitly confirmed. Full registry re-swept via `validate_skill.py` (repo-wide, all `SKILL.md` files, not just the 2 touched): 7 pre-existing invalids, none new — 2 are `skill-creator`'s own template files (expected, not real skills), 4 are `docx`/`pdf`/`pptx`/`xlsx` all missing a `metadata` frontmatter block (consistent with all 4 having bypassed this project's own scaffold pipeline, already noted in the "(cont'd 11)" entry), 1 is `office-doc-creator`'s long-tracked `compatibility`-field-length violation (pre-dates this session by weeks, deliberately deferred every prior round, unrelated to this round's edits). `soffice`/LibreOffice not on PATH in this environment, so the chain's visual-verification step (`soffice.py` render → `pdftoppm` → look at it) was not re-exercised this round — the script itself is unchanged from its prior verification. |
| 2026-08-13 (cont'd 11) | Documentation/status-check session (no code work) — closed a real gap between logged status and actual commit history, and a business-model correction. **Gap found**: 3 commits (`91902a8`/`5a650eb` "Add validators for PPTX and Word redlining, and implement Excel formula recalculation script", `c7c18d6` "Update compatibility and verification details across multiple skills", `2ad4978` "Remove obsolete validators...") landed same-day (2026-08-13) with no corresponding STATUS.md entry, and `c7c18d6`'s message references verification "with Antigravity CLI instead of Claude Code" — i.e. a different harness/session did this round, outside this project's own Claude-Code-tracked history. Registry grew 78→82 (`docx`, `pptx`, `xlsx`, `pdf` added directly, `skill_id`s not present in the prior 78-count) without going through this project's own 9-step pipeline (no scout-harvester run, no `license-compliance-check` run — confirmed via each entry's `elicited_from`, which credits "thatlq1812 (2026-07-26)" generically and never mentions a source repo).
| 2026-08-13 (cont'd 10) | Closed the one item deferred from the previous entry: GenVid finding 1d (watermark + intro/outro cards), the last of the 6 surveyed findings, flagged there as larger-scope. Investigated before building: intro/outro cards needed NO new code -- `video-assembly-composer`'s `video_track` already supports `type: "image"` items with `ken_burns: false` (a still frame held for its declared `duration`), so this was purely a documentation gap, closed with a new "Intro/outro cards" SKILL.md section, not a schema change. The one genuinely new capability was a persistent watermark overlay spanning part of the timeline (not the intro/outro cards), ported from the same GenVid project's `build_watermark_overlay.py`/`build_final_video.py` (a full-frame transparent PNG, its own content positioned via HTML/CSS, ffmpeg-composited over the body only). This needed a real image-rendering capability `html-poster-composer` didn't have: **`html-poster-composer`** (0.3.0→0.4.0) gained a `VIDEO_HD` canvas preset (1920x1080, added to `CANVAS_PRESETS_MM` as an exact-round-trip mm-equivalent at the module's existing 96 CSS-px/inch conversion) and a `transparent_background: true` content.json field (real alpha-channel PNG output via `omit_background=True`, requiring `html,body{background:transparent}` in addition to the `.canvas` fill -- verified this distinction matters, not just `.canvas`'s own background). **`video-assembly-composer`** (0.2.0→0.3.0) gained a top-level `watermark_image` path + a per-`video_track`-item `watermark: true` boolean: every flagged item contributes its own `[start, start+duration)` span to an ffmpeg `overlay` filter's `enable` expression (adjacent spans merge into one `between()`, non-adjacent spans OR together), always composited at `0:0` since the source image supplies its own internal layout -- matching the elicitation source's real technique rather than inventing a corner/margin positioning scheme not present in it. Reused the existing `include_own_audio` cross-item validation pattern: flagging any item `watermark: true` requires every item to have a numeric `duration` (needed to compute the spans). **Real bug found and fixed during testing** (not theoretical -- reproduced and confirmed): the watermark image input was first built as `ffmpeg.input(path, loop=1, framerate=fps)` with no `t=` bound, matching `_build_video_stream`'s image-handling pattern but missing that pattern's OWN `t=duration` bound. `loop=1` alone makes an image input infinite (it never hits EOF); the `overlay` filter's default framesync behavior does not stop at the finite main video's length once the second input is infinite -- a real 10-second test timeline ran for 3+ hours (280,000+ frames encoded, confirmed via the stalled process's own progress output) before being killed manually, instead of finishing in seconds. Fixed by always bounding the watermark input with `t=total_duration` (guaranteed computable per the validation rule above). Verified real end-to-end, not mocked: rendered a 3-segment timeline (solid-blue 2s intro card, a 6s `testsrc` body with `watermark: true`, solid-green 2s outro card) against a real transparent watermark PNG rendered through `html-poster-composer`'s new preset/flag -- confirmed via `ffmpeg -i` (exactly 10.00s output) and 3 extracted frames (t=1s/5s/9s): intro/outro frames show pure solid color with NO watermark, the body frame shows both watermark text lines correctly composited in the bottom corners over the test pattern, confirming the per-item time-gating works correctly and not just that the overlay filter runs at all. Also independently verified the transparent PNG itself via PIL before using it in the render: `mode == 'RGBA'`, exactly 1920x1080px, corner pixel `alpha == 0`, thousands of non-zero-alpha pixels in the actual rendered text region. Both skills' registry entries updated (version, security_audit re-audit note, elicited_from) and both re-pass `validate_skill.py`. Full 78-skill registry re-validated clean (JSON valid, same 1 pre-existing unrelated `office-doc-creator` issue). All 6 GenVid survey findings now closed. Not yet pushed. |
| 2026-08-13 (cont'd 9) | thatlq1812 directed importing real capabilities from D:/elix/projects/20260805_GenVid (a separate, real, completed project -- 3 AI-generated history-education videos actually delivered/submitted), following a research-agent survey earlier this session that ranked 6 real findings by priority. Implemented the top 4 (items 1a/1c/1e/1b from that survey; item 1d, watermark/intro-outro cards, deliberately deferred -- larger scope, see the next entry). All grounded directly in real GenVid source, read in full, not skimmed: `scripts/pipeline/build_v5_fullmerge.py` (duration-stretch), `build_ambient_dub_mix.py` (per-clip audio mixing), `batch_gen_tts_multiprocess.py` (real-duration measurement), `docs/AUDIO_STEERING_AND_FOLEY_GUIDE.md` (Veo audio-steering). **`video-assembly-composer`** (0.1.0→0.2.0): 3 new optional `video_track` item fields -- `stretch_from` (slow-motion `setpts` stretch to an exact target `duration` instead of trimming, for a clip whose native length is fixed by whatever generated it but needs to match something measured afterward), `include_own_audio` (mixes a clip's own embedded audio into the final mix, atempo-matched to any stretch, edge-faded ~0.1s to prevent a click/pop at scene-cut concat boundaries -- a real audio artifact the plain concatenation didn't guard against before), `own_audio_volume_db`. A clip requesting `include_own_audio` with no real audio stream falls back to silence (detected via a real `ffmpeg -i` subprocess call parsing stderr for "Audio:" -- deliberately NOT ffprobe, since the portable `imageio-ffmpeg` binary doesn't bundle it and this project never depends on a host-system binary). `validate_timeline.py` extended to match (type/range checks on the 3 new fields, plus a hard "every item needs a numeric duration if any item sets include_own_audio" cross-item rule, since the own-audio track's time-alignment depends on it). Incidentally found and fixed WHILE testing this addition, unrelated to it: the `subtitles` filter's own argument parser breaks on an absolute Windows drive-letter path, and manually escaping it doesn't help (`ffmpeg-python`'s own filter-argument serializer escapes `\` again on top of any manual escaping, compounding into garbage -- confirmed empirically against real ffmpeg across several candidate escapings before finding the real fix); fixed by referencing the SRT by bare relative filename with the ffmpeg subprocess's own `cwd` set to match (`ffmpeg.compile()` + `subprocess.run(cwd=...)`, replacing `ffmpeg.run()`, which has no `cwd` param) -- every other path in the render resolved to absolute up front specifically so this cwd change can't silently break them. Deliberately NOT ported this round: GenVid's multi-track music crossfade (`acrossfade`) -- the simpler flat-attenuation two-track mix it also supports was already possible with the existing schema (no `duck_under_voice` + an explicit `music.volume` already gives that), so only the crossfade specifically was new, and it's noted as a known limitation rather than built, to keep scope controlled. Verified real, not mocked: built real ffmpeg `lavfi` test fixtures (a 4s clip with a real embedded tone, a 4s clip with NO audio stream at all, an 11s voice track) and rendered a real 2-segment timeline exercising stretch + real-audio-mixing + the silent fallback all together -- output confirmed via `ffmpeg -i` (correct ~11s duration, correct video+audio streams both present); re-verified with captions enabled after the subtitles-path fix, a frame extracted mid-caption visually confirms correct burned-in text at the correct position. **`media-pipeline-orchestrator`** (0.1.1→0.2.0): the `audio` and `videos` pipeline stages swapped order (audio now runs first) specifically so each scene's REAL measured narration length (`measure_wav_duration_seconds()`, stdlib `wave` module, no ffprobe) can drive both the Veo call's requested duration (`map_to_veo_duration()`, identical threshold rule to GenVid's own) and the final assembly's `stretch_from` target -- closing a gap the SKILL.md itself had flagged since v0.1.0 ("does not correct a mismatch between a scene's spoken narration length and its declared duration_seconds"). Both real values persist back into `scenes.json` per scene (`actual_audio_duration`, `veo_duration`) so a resumed run doesn't re-measure anything; a resume-from-an-older-project.json path also backfills them without re-marking the audio stage done. `_build_timeline()` now wires every scene's video item to `stretch_from`/`include_own_audio: true`/`own_audio_volume_db: -12`, and `generate_video()` is now called with `generate_audio=True` explicitly (previously implicit/unset) so there's real ambient audio for `include_own_audio` to actually mix in. Verified real: `map_to_veo_duration()` re-checked against GenVid's own exact threshold boundaries; `measure_wav_duration_seconds()` against a real 11.0s test wav (exact match); `_build_timeline()`'s output independently re-validated clean by `video-assembly-composer`'s own `validate_timeline.py`. NOT yet verified: one live Gemini-API-backed run chaining all of this end-to-end (no `GEMINI_API_KEY` in this environment) -- each piece is independently real-verified, not yet chained live. **`gemini-generator`** (0.1.0→0.1.1, doc-only): added a real 4-layer Veo prompt-steering schema to the video section (visual action -> hard vocal-mute constraint -> positive ambient-SFX redirect -> cast-lock) -- real finding, reproduced twice in GenVid: asking Veo to "silently lip-sync" a line still made it invent an unrelated talking host character; the schema is what actually prevented that. No script/schema change, pure documentation for whoever constructs a `generate_video.py` prompt when a separate TTS track carries the real voice. Full 78-skill registry re-validated clean via `validate_skill.py` (same 1 pre-existing unrelated `office-doc-creator` issue). Not yet pushed. |
| 2026-08-13 (cont'd 8) | thatlq1812 directed a further domain split, still same day as the archive move: `general` → `general`/`office`/`design`, `education` → `education`/`academic`. Classification grounded in existing data rather than invented: `office` (`office-doc-creator`, `document-ai-structurer`, `slide-deck-composer`, `latex-project-bootstrap`, `mermaid-diagram-designer` -- document/diagram production) and `design` (`brand-identity-linter`, `light-logo-arranger`, `slogan-copy-linter`, `personal-style-library` -- visual-brand tools) split `general`'s 26 skills by real functional shape; `academic` (`ta-grading-rubric-linter`, `lab-practicum-guide`, `dissertation-proposal-scaffold`, `academic-grant-proposal`, `manuscript-journal-formatter`, `academic-source-finder`, `essay-structure-scaffold`) split out of `education`'s 25 along `docs/specs/STRATEGY_SPEC.md` §5.1's own already-documented audience-tier ladder (TA/Graduate + Lecturer/Researcher tiers vs. the remaining K12/Teacher/Parent/Lifelong-Learner tiers) -- both splits proposed by Claude, confirmed by thatlq1812 before executing. 16 skills moved via `git mv` + a registry `tags.domain` update (5 office, 4 design, 7 academic). Confirmed a second time that the domain-agnostic glob-fallback mechanism built during the original reorg generalizes cleanly: zero script changes needed, only a mechanical path-reference rewrite pass caught 6 files with a literal old-domain-qualified path string (the earlier reorg's own mechanical script only handled "not yet domain-qualified" text; a second small script handled "domain-qualified but with a now-stale domain name" specifically). Registry now has 8 domain values (`general`/`office`/`design`/`education`/`academic`/`meta`/`media`/`legal`) plus the non-domain `archive/`. Verified real: authoritative registry-vs-disk path check (all 78 skills resolve to their expected `skills/<domain-or-archive>/<skill_id>/` path, zero missing), full `validate_skill.py` sweep (clean, same 1 pre-existing unrelated `office-doc-creator` issue), a real cross-domain export (`slide-deck-composer` now `office` depending on `toolchain-bootstrap` in `general`) and a real cross-domain scaffold duplicate-refusal (`brand-identity-linter`, now `design`) both re-confirmed working end to end. Structure diagrams updated in `CLAUDE.md` and `docs/MASTER_CONTEXT.md`. Not yet pushed. |
| 2026-08-13 (cont'd 7) | thatlq1812 directed cleaning up unused skills, suggesting either deletion or a dedicated archive folder. Chose the archive route (matches this registry's own established reversibility discipline -- `operational_status: superseded` skills are "left in place, not deleted," a principle already applied 3 times this session). Moved all 10 skills currently marked `operational_status: superseded` -- the 5 merged bootstrap skills (`python-env-bootstrap`, `ffmpeg-bootstrap`, `pandoc-bootstrap`, `typst-bootstrap`, `xelatex-bootstrap`), the 3 merged Gemini generators (`image-generator-gemini`, `video-generator-gemini`, `audio-generator-gemini`), and 2 from an earlier session (`svg-poster-builder`, `poster-generator`) -- via `git mv` out of their active domain folders into a new flat `skills/archive/<skill_id>/` (no domain subfolder, matching `docs/archive/`'s own flat convention). Confirmed via registry `dependencies` grep before moving: zero active skills still depend on any of the 10 (all real dependents were already repointed to `toolchain-bootstrap`/`gemini-generator`/`html-poster-composer` earlier this session). Real finding: **this move required zero code changes anywhere.** Every domain-aware script built during this session's earlier `skills/<domain>/<skill_id>/` reorg already resolves a sibling skill via a one-level glob fallback (`next(SKILLS_DIR.glob(f"*/{skill_id}"), None)`) whenever the registry-declared domain doesn't match a real on-disk path -- `archive/` just reads as one more sibling folder at the same depth as the 5 real domains to every one of those lookups. Verified for real, not assumed: `validate_skill.py` re-run against all 10 at their new `archive/` path (all pass); `scaffold_skill.py`'s cross-location duplicate check correctly refused re-scaffolding `xelatex-bootstrap`, finding it at its new `skills/archive/xelatex-bootstrap` path; `export_bundle.py` still correctly refuses to export an archived skill (same `operational_status: superseded` refusal message as before the move); `media-pipeline-orchestrator/run_pipeline.py --help` re-confirmed the real, unaffected `gemini-generator`/`toolchain-bootstrap` chain still resolves (neither of those active skills was touched by this move). `registry/SCHEMA.md`'s `operational_status` field documentation updated to describe the new physical location. Structure diagrams updated in `CLAUDE.md` and `docs/MASTER_CONTEXT.md`. Full 78-skill registry re-checked clean (JSON valid, same 1 pre-existing unrelated `office-doc-creator` issue). Not yet pushed -- pending the same separate push confirmation this session has consistently asked for before every push. |
| 2026-08-13 (cont'd 6) | Scaffolded the 3 remaining planned generator skills (`gpt-generator`, `claude-generator`, `normal-generator`; 75→78) per thatlq1812 direction from the same round as `gemini-generator`'s real merge -- explicitly skeleton-only this round, real build deferred. Added a new `operational_status` state, `"planned"` (`registry/SCHEMA.md` updated), for a scaffolded-but-unimplemented skill -- distinct from `paused`/`superseded`; its `security_audit.status: "pending"` already independently hard-excludes it from `skill-exporter` bundles via the existing gate, `planned` is an explicit human-readable signal on top of that, not a new export-gating mechanism. Each of the 3 SKILL.md files states real, honest scope + why it isn't built yet, deliberately NOT asserting any provider-specific API detail (model names, endpoints) as fact without direct verification, per this project's own grounding discipline applied to a skill's own documentation. `claude-generator`'s scope was directly thatlq1812-confirmed as code/SVG/document generation (not pixel image/video, which Claude's API doesn't offer) rather than assumed. `normal-generator`'s design is explicitly left unresolved, grounded in a real finding: dispatched a research agent to read thatlq1812's own Elixverse codebase (`D:/elix/platform/server/src/core/ai/`) for its real multi-provider routing pattern, since thatlq1812 pointed at it as a reference -- found `BaseAIAdapter` (adapters/base.py) with one hand-written concrete adapter class per provider (gemini/openai/anthropic), each using that provider's own SDK directly, `AIRouter` doing a 3-step waterfall (explicit model pin -> DB default -> hardcoded fallback chains); critically, Elixverse itself has **no** generic "any provider from docs alone" mechanism -- every real adapter there is still hand-written. This is a real, load-bearing finding recorded in `normal-generator`'s own `elicited_from`: its 'adapt to any provider' goal has no existing real precedent to copy, even in thatlq1812's own most relevant production system, so its actual mechanism (generic HTTP-call helper vs. caller-declared adapter template vs. something else) is documented as an explicitly open question, not assumed solved. All 3 skeletons validate clean via `validate_skill.py`; full 78-skill registry re-checked (JSON valid, same 1 pre-existing unrelated `office-doc-creator` issue, untouched). Not yet pushed to the public GitHub remote -- pending a separate explicit push decision, per this session's established pattern of confirming before every push. |
| 2026-08-13 (cont'd 5) | thatlq1812 directed restructuring the media domain's 3 BYOK-Gemini generator skills (`image-generator-gemini`, `video-generator-gemini`, `audio-generator-gemini`) into one `gemini-generator` skill (74→75), part of a broader plan to organize media generation by provider (`gemini-generator` now; `gpt-generator`/`claude-generator`/`normal-generator` scaffolded only this round, real build deferred -- thatlq1812's own words: "gemini trước, 2 cái kia dựng khung sẵn... rồi ta thu thập và dựng hoàn chỉnh gemini-generator nhé"). Same merge shape as `toolchain-bootstrap` earlier this session: content copied not moved, 3 old skill_ids marked `operational_status: superseded` pointing at `gemini-generator`, nothing deleted. New skill covers all 3 modalities via one router SKILL.md (image: single/batch/identity+style-anchoring/vision-analysis/PDF-cover-extraction; video: text-to-video/image-to-video/keyframe-interpolation/motion-intensity/segment-chaining; audio: single-voice/2-speaker-dialogue/consent-gated voice cloning) -- all 8 scripts copied verbatim, only 2 internal cross-reference comments updated (stale mentions of the old separate skill names). Real dependents fixed: registry `dependencies` for `media-pipeline-orchestrator` consolidated from 3 entries to 1; `run_pipeline.py`'s 3 separate `sys.path` insertions collapsed to 1 (all 3 modalities now live in the same folder); `generate_script.py`'s hardcoded glob target updated from `video-generator-gemini` to `gemini-generator`; prose cross-references fixed across `media-anchor-profile` (SKILL.md + 2 scripts), `video-assembly-composer`, `html-poster-composer`, `toolchain-bootstrap`, `slide-deck-composer` (SKILL.md + 2 scripts), `personal-style-library` (SKILL.md + script), `project-workspace-initializer` (SKILL.md + 2 bundled templates), and `skill-creator`'s own templates/README (left untouched, per the same no-revisionist-history discipline as the bootstrap merge: dated `changelog_X` fields, "Verified" log entries, and `elicited_from` grounding narrative across all touched files, e.g. `media-anchor-profile`'s and `media-pipeline-orchestrator`'s own dated entries). Verified real: all 6 scripts (`generate_image.py`, `analyze_style.py`, `extract_pdf_page.py`, `generate_video.py`, `extract_last_frame.py`, `generate_speech.py`) re-run via `--help` from the new location, full argument surface intact; `media-pipeline-orchestrator/run_pipeline.py --help` and `generate_script.py --help` both re-run to confirm the full real cross-skill import chain (now touching 2 domains: `media` and `general`) still resolves correctly end-to-end. No real Gemini API calls re-run (no `GEMINI_API_KEY` in this environment) -- this round changed file locations and cross-references only, not generation logic, so the pre-merge real-API verification recorded in each original skill's own history still describes the underlying behavior. Full 75-skill registry re-validated clean via `validate_skill.py` (same 1 pre-existing, unrelated `office-doc-creator` issue, untouched). `gpt-generator`/`claude-generator`/`normal-generator`: scaffolding + research pending in a following round of this same session -- see the next entry once written. |
| 2026-08-13 (cont'd 4) | thatlq1812 directed removing `scriptorium-updater` (75→74) -- a real deletion, not a supersede (no successor skill, unlike the bootstrap merge earlier this session). Removed the skill folder (`skills/meta/scriptorium-updater/`), its registry entry, and the dangling mention in `README.md`'s "What's in here" summary. Not touched, per this project's standing no-revisionist-history discipline: `docs/MASTER_CONTEXT.md`'s dated 1.7.0/1.8.0 changelog rows, which correctly describe real work done on/with this skill at the time. No other skill's `dependencies` referenced `scriptorium-updater` (confirmed via repo-wide grep before deleting), so no downstream fix needed. Full registry re-validated (74 skills, JSON valid, `validate_skill.py` clean except the 1 pre-existing unrelated `office-doc-creator` issue). |
| 2026-08-13 (cont'd 3) | Second half of the STRATEGY_01.md-derived work (see "(cont'd 2)" entry below): directory-per-domain reorganization of `skills/`, thatlq1812-confirmed to proceed in full after a scope-check flagged the real size of the change (initially looked like "move folders," turned out to require fixing every cross-skill path assumption in the repo). Domain choice: reused the 5 domain values already live in `registry/skills.json` (`general` 26, `education` 25, `meta` 9, `media` 9, `legal` 6, sum 75) rather than inventing STRATEGY_01's 9-category scheme -- zero new classification judgment calls, and the folder structure becomes mechanically checkable against the registry's own `tags.domain` field. All 75 skills moved via `git mv` (73) or a plain filesystem move for the 2 not yet `git add`ed this session (`toolchain-bootstrap`, `lesson-differentiation-builder` -- git mv refused these with "source directory is empty" since git's index had no tracked content there yet); verified after the move that every registry skill_id resolves to a real directory at `skills/<tags.domain[0]>/<skill_id>/`, no exceptions. Fixed every real path assumption the move broke, each independently re-verified by actually running the affected script (not just reading the diff): (1) every `Path(__file__).parents[N]` computation that reaches repo-root/skills-root, +1 depth across ~10 scripts (`skill-exporter`'s 3 scripts, `dedup-novelty-check`, `latex-project-bootstrap`, both `toolchain-bootstrap` and the superseded `typst-bootstrap`'s `check_typst.py`/`install_typst.py`, both `toolchain-bootstrap` and the superseded `python-env-bootstrap`'s `bootstrap.ps1`/`.sh` shell path-splitting chains) -- re-run for real post-fix: `check_typst.py`/`check_pandoc.py`/`check_xelatex.py` all still correctly detect the real installed toolchains, `bootstrap.ps1` still correctly resolves the real repo root and installs into the real shared venv. (2) 11 hardcoded cross-skill `sys.path.insert(..., SKILLS_DIR / "other-skill-id" / "scripts")` call sites across 9 files (`poster-generator`'s 2 scripts → `svg-poster-builder`, `upskilling-roadmap-builder` → `study-plan-builder`, `media-pipeline-orchestrator`'s 3 scripts → 5 different sibling skills, `video-generator-gemini`/`video-assembly-composer` → `toolchain-bootstrap`, `image-generator-gemini`/`personal-style-library`'s 2 scripts → `media-anchor-profile`/`brand-identity-linter`) -- these broke in a way a flat +1-depth fix couldn't solve, since a sibling skill can now live in a genuinely different domain folder than the caller (e.g. `personal-style-library` is `general`, `media-anchor-profile` is `media`); replaced with a domain-agnostic one-level glob (`next(_SKILLS_DIR.glob(f"*/{skill_id}"), None)`) at every site, refusing with a clear message if the sibling isn't found under any domain. Found one real additional cross-skill import in `personal-style-library/validate_library.py` that the initial grep missed (a second import reusing the same `_SKILLS_ROOT` variable, to `media-anchor-profile`, sharing one `sys.path` setup block with the `brand-identity-linter` import already found) -- caught by reading the full file after the first fix, not assumed complete from the grep alone. All 9 files' real cross-skill import chains re-verified by actually invoking each script's `--help` (triggers full module-level import execution) or a direct function call for lazily-imported cases (`upskilling-roadmap-builder`'s `_import_scheduling_core()`): all succeeded, including `media-pipeline-orchestrator/run_pipeline.py`'s 5-way fan-out across 2 domains (`media` + `general`). (3) `skill-exporter` made domain-aware: `export_bundle.py` gained `skill_dir_for()` (registry-domain lookup, glob fallback) replacing every `SKILLS_DIR / skill_id` construction; critically, the exported bundle's own internal zip structure stays FLAT (`skills/<skill_id>/...`, explicit `arcname` construction, not derived from `path.relative_to(SKILLS_DIR)` which would have leaked this repo's internal domain nesting into every recipient's harness -- Claude Code/goose/Cursor have no concept of Scriptorium's own domain taxonomy). `verify_lock.py` updated to try flat-then-nested when resolving a locked skill_id, so it works against both an extracted bundle (flat) and this repo's own internal `skills/` (nested) with no flag needed. `list_candidates.py` needed no change (registry-only, never touches a skill's on-disk path). Verified real end-to-end: exported `legal-citation-checker` (real 3-skill dependency chain `legal-citation-checker` → `document-ai-structurer` → `toolchain-bootstrap`, crossing `legal`/`general` domains), confirmed the zip's top-level `skills/` entries are flat skill_ids with no domain segment, then ran `verify_lock.py` against both the extracted bundle and this repo's live `skills/` -- both reported all 3 skills matching. (4) `scriptorium-updater` made dual-layout-aware for the transition period (local repo now nested, the public GitHub remote still flat until a push decision is made separately): `sync_skills.py` gained `_enumerate_source_skills()`, auto-detecting per top-level child whether it's a skill directory itself (flat, legacy) or a domain directory containing skill directories (nested, current) -- the destination side (a tester's own `.claude/skills/` etc.) stays flat regardless, matching `export_bundle.py`'s same reasoning. `check_updates.py`'s `changed_skill_dirs` extraction fixed from a fixed path-index assumption (broke by definition once local and remote can have different depths) to matching against the known skill_id set from both sides' registry snapshots, at whatever depth each diffed path happens to have. Verified via direct function calls against synthetic flat and nested fixtures (both layouts correctly detected) and a synthetic mixed-depth diff list (both correctly attributed to the right skill_id) -- full real end-to-end verification against the actual GitHub remote is blocked on the (separate, not yet made) decision to push, since the remote's current flat layout means a real `check_updates.py`/`sync_skills.py` run right now would compare across the exact transition case the fixes target, not exercise the post-push steady state. (5) `skill-creator/scaffold_skill.py` gained a required `--domain` argument (places a new skill directly at `skills/<domain>/<skill_id>/`, no fixed choices list since domain is this registry's own open/growing taxonomy) and a cross-domain uniqueness check (refuses a skill_id that already exists under ANY domain, not just the requested one) -- verified real: scaffolded a real test skill into `skills/general/`, confirmed the duplicate-across-domain refusal by requesting the same skill_id under `--domain education`, cleaned up the test artifact. (6) Docs: `CLAUDE.md` and `docs/MASTER_CONTEXT.md`'s repo-structure diagrams and every path example updated; a repo-wide grep afterward for any remaining flat `skills/<skill_id>/...` reference (excluding the superseded skills' own untouched historical content, and dated historical log/changelog entries across `STATUS.md`, `STRATEGY_SPEC.md`'s dated pipeline-status table, and `docs/archive/` -- none of those rewritten, matching this project's standing no-revisionist-history discipline) found and fixed 2 real stragglers the earlier mechanical sweep missed because it was scoped to `skills/` only: `START_HERE.md` (repo-root, user-facing, 3 real flat references) and one more `CLAUDE.md` line. Full 75-skill registry re-validated clean via `validate_skill.py` (same 1 pre-existing `office-doc-creator` compatibility-length issue, untouched, out of scope) and spot-verified by actually running scripts from 4 more diverse skills (`legal-citation-checker`, `office-doc-creator`, `lesson-plan-builder`, `lesson-differentiation-builder`) at their new nested paths. Not yet done, deliberately deferred to a separate decision: pushing the reorganized local repo to the public GitHub remote. |
| 2026-08-08 | thatlq1812 asked for an independent cold-agent verification of `scriptorium-updater` (a fresh subagent, no context from the building session, its own new scratch directory) -- standard practice for a newly-built skill in this registry (matches `slide-deck-composer`'s own v0.5.0 cold-agent round). The subagent reproduced every claim in the skill's own SKILL.md for real against the actual public repo and found ONE new real bug: `sync_skills.py`'s `git pull` hung indefinitely when the calling shell invocation was piped (`... | tail -N`) -- `subprocess.run()` with no explicit `stdin` inherits the parent's stdin, and git's credential helper (Windows GCM in the reproduction) tried to prompt interactively on that inherited stdin and blocked forever with zero error message, exactly the "silent hang" failure class this project refuses everywhere else. Fixed: extracted the 3 scripts' near-identical `_run_git` helpers (a real, already-duplicated risk -- the same duplication pattern caused a real bug earlier this same session, see `html-poster-composer`'s `ZONE_BASE_CSS` note) into one shared `git_utils.run_git()` with `stdin=subprocess.DEVNULL` set unconditionally, so any credential prompt now fails fast and loud instead of hanging silently. Re-verified real: the exact previously-hanging piped invocation now completes in under 1 second with correct output; full 3-script regression (`init_source_clone.py`, `check_updates.py`, `sync_skills.py --dry-run`) confirmed unaffected. `scriptorium-updater` 0.1.0→0.1.1. Full 73-skill registry re-checked clean. |
| 2026-08-07 (cont'd 5) | thatlq1812 asked about a "skills-updater"/"skills-management" idea: auto-check/update skills from the GitHub repo each session. Investigated before building: `gh repo view thatlq1812/scriptorium` confirmed the repo is genuinely PUBLIC (created 2026-07-26, actively pushed, matches local HEAD before this session's own unpushed work) -- flagged directly as a real conflict with `CLAUDE.md`'s stated "proprietary... sold retail, never given away" principle, since a public repo lets anyone clone the whole registry free. thatlq1812 clarified: deliberate for now, pre-commercial dev/test phase, goal is letting TESTERS get updates without re-downloading/re-exporting a bundle each time -- not a business-model violation, a real known future decision point (flagged in the new skill's own Known Limitations, not silently resolved). Design follows this registry's own 2-step bootstrap discipline (detect safely by default, apply as an explicit separate step) rather than a fully-automatic silent updater -- thatlq1812 explicitly ruled out pure mechanical automation ("không phải chạy cơ học không đâu, mà do agent làm theo ghi chú"), confirming the report-then-agent-decides shape already used elsewhere in this registry (`document-ai-structurer`'s propose/review/apply). Add/update-only (never delete) chosen explicitly over full mirror-sync, since a destination-only skill could be the tester's own local addition. Built `scriptorium-updater` v0.1.0 (registry 72→73): `init_source_clone.py` (one-time shallow clone), `check_updates.py` (read-only `git fetch`+diff, reports exactly which skill_ids changed version between local HEAD and origin/master, never touches the working tree), `sync_skills.py` (pulls the source, refuses on uncommitted local changes, copies `skills/` into a caller's own harness-specific path -- `.claude/skills/`/`.agents/skills/`/etc., not guessed -- add/update only, `destination_only` skill_ids reported not deleted). Verified real end-to-end against the actual public repo: cloned for real, `check_updates.py` correctly reported up-to-date then correctly reported 10 real skill_id version changes after simulating a stale clone (reset 5 real commits back); `sync_skills.py --dry-run` correctly pulls the source (verified this distinction matters -- dry-run only gates the destination write) without writing; the real sync wrote 71 real skill folders while leaving a pre-seeded unrelated test skill and an unrelated extra file inside an updated skill folder both completely untouched (add/update-only confirmed at both the skill-folder and individual-file level). One real bug found and fixed during verification: `git rev-parse` on a bad branch ref exits non-zero but still echoes the raw argument to stdout, silently defeating an `if not remote_head` emptiness check -- fixed by checking `returncode` explicitly. 4 refusal paths verified. Full 73-skill registry re-checked clean. |
| 2026-08-07 (cont'd 4) | thatlq1812 raised a real generic use case (turning any long-form structured content into multiple visually-consistent information pages, exported PDF/PNG/JPG -- explicitly generalized away from the legal example, "design and media" not domain-specific) and asked whether the system already supported it. Assessed honestly: `document-ai-structurer` already parses structured content, `slide-deck-composer` already renders many consistent slides (proven on legal-register content) -- but `html-poster-composer` itself only rendered one canvas per call, no multi-page concept. thatlq1812 approved building it. Built `compose_batch.py` v0.3.0: N pages sharing ONE `layout.json` + a `pages.json` (shared `fonts`, one `zones` entry per page), reusing `compose.py`'s own `_build_zone_html`/`_autofit_elements` rather than a second implementation; numbered per-page PNG/JPG plus an optional single combined multi-page vector PDF (`prefer_css_page_size=True` + CSS `@page`, not merged files). Found and fixed a real, serious bug during adversarial testing (not batch-specific -- shared by the single-page path too): `.text-content`'s fit-check only ever measured HEIGHT, trusting `width:100%`+word-wrap to guarantee no width overflow -- true for normal text, false for a single unbroken long token (no whitespace to wrap at), which silently overflowed the canvas edge sideways with zero warning, invisible to the height-only auto-fit check. Root cause was compounded by real code duplication (the same zone CSS block hand-copied into both `compose.py` and `compose_batch.py`) -- fixed by extracting a shared `ZONE_BASE_CSS` constant (adding `overflow-wrap:break-word`) that both files import, closing the duplication that let the bug exist in two places. Verified real: a 3-page generic (non-legal) step-by-step guide rendered with consistent shared styling across pages, combined PDF confirmed real multi-page vector output via `pypdfium2` (3 pages, each independently text-extractable); the overflow-wrap fix verified on both a genuinely-unfittable adversarial case (6,000 unbroken characters, correctly refuses) and a merely-large-but-fittable case (800 unbroken characters in a tall zone, correctly renders -- confirmed the fix doesn't over-refuse); 4 new refusal paths verified. Full 72-skill registry re-checked clean. |
| 2026-08-07 (cont'd 3) | thatlq1812 asked whether a logo-design capability was worth building, pointed directly at a real prior project as elicitation source: `D:/elix/projects/temp_project_20260728` (a real signboard/logo/banner client job, "Mê Xôi" xôi-mặn stall). Investigated directly (PROJECT.md phase-3 notes, `design/{logo,signboard,banner}/{layout.md,components.json,render_preview.py}`, `export_pdf_logo_tem.py`, `fonts_embed.css`, `logo-seal.svg`) rather than guessing. Real finding: the actual workflow was never "AI draws vector art from imagination" -- Gemini generated individual RASTER assets, a human/agent composited them into hand-authored SVG at precise coordinates, real vector TEXT was kept for print sharpness, Playwright rendered previews and exported final print PDFs at real physical cm sizes -- i.e. the same shape `html-poster-composer` already implements, just more mature in 3 concrete ways. thatlq1812 approved building "whatever is reasonable and feasible" from that finding; built all 3, each independently verified: (1) **Font embedding** (v0.1.1) -- content.json gained an optional top-level `fonts` object, base64-embedded into `@font-face`, one complete file per family, matching the real project's own proven convention exactly (rejected the more complex latin/vietnamese-subset-split approach after confirming the real project didn't use it either). A real, worthwhile false start: attempted an automated Vietnamese-glyph-coverage check via `fontTools` cmap inspection, found it FALSE-REFUSED a font this same real project already ships successfully in production (Vietnamese font subsets commonly use OpenType GSUB mark-positioning, not one glyph per precomposed character -- a naive cmap-membership check gets this wrong), correctly abandoned in favor of matching the real elicitation source's own verification method (render + human look) instead of shipping an unreliable check. Verified real: 2 real fonts (Baloo 2, Quicksand) from the elicitation project rendering full Vietnamese diacritic text correctly, zero network requests. (2) **Anchor-relative zone positioning** (v0.1.2) -- a zone may declare `relative_to`+`anchor` (reusing `light-logo-arranger`'s own 7-anchor vocabulary) +`gap_pct` instead of `x_pct`/`y_pct`, resolved in a single deterministic left-to-right pass, elicited from the real project's icon-scatter-around-a-dish-name layout AND its explicit instruction to allow deliberate overlap ("cho nó tràn ra một chút thì tốt" -- negative `gap_pct` supported for exactly this). Verified real: a 4-icon scatter (2 outside, 2 deliberately overlapping) rendered exactly as specified; 5 refusal paths verified including a forward-reference correctly cascading failures to every dependent zone. (3) **`compose_seal.py`**, a new circular seal/badge composer (v0.2.0) -- elicited from a real 25-`<path>` circular seal logo in the same project. Deliberately generates real SVG (not HTML/CSS zones) since curved text needs `<textPath>` along a real arc, which CSS can't do -- the one place this session's own earlier SVG-to-HTML migration doesn't apply, confirmed consistent with the separate real answer given the same session to "should logos stay SVG" (yes). Wraps the SVG in HTML and renders via the same Playwright pipeline, matching the real elicitation source's own `export_pdf_logo_tem.py` technique exactly. Real bug found and fixed during build: text on the bottom half of the circle rendered upside-down (a well-known SVG `textPath` quirk), fixed by auto-detecting the arc midpoint and reversing the underlying path's direction when it falls in the bottom half -- verified real with a top+bottom 2-arc seal, both correct after the fix. Verified end-to-end with real assets from the elicitation project (real fonts, the real center icon, real PDF export confirmed true vector via `pypdfium2`); 5 refusal paths verified. All 3 features individually version-bumped and re-verified against `validate_skill.py`; full 72-skill registry re-checked clean throughout. |
| 2026-08-07 (cont'd 2) | Render-backend migration for the Light Design/media poster cluster: SVG-string+Pillow → HTML/CSS+headless-browser, executed same session as the decision (thatlq1812: "Có thể triển khai và kiểm thử"). New skill `html-poster-composer` v0.1.0 (registry 71→72) supersedes `svg-poster-builder` + `poster-generator` (new `skill_id`, not an in-place rename -- `registry/SCHEMA.md` keeps `skill_id` immutable, same precedent as `slide-deck-builder`→`slide-deck-composer`). Same `layout.json`/`content.json` contract as before (zone taxonomy, canvas presets, 40%-text-coverage rule, image/text/fill content types) -- `layout_schema.py`/`validate_content.py` carry the exact validation logic forward (copied, not cross-imported, since a superseded skill shouldn't be a live dependency). New mechanism: `compose.py`'s auto-fit loop renders each `typography_frame` zone for real, measures the actual rendered text-block height via Playwright's `bounding_box()` (the browser's own layout engine), shrinks font size and re-measures in a real loop until it fits, refuses loudly (exit 1, naming every zone and by how much) if nothing fits -- strictly stricter than the old Pillow renderer, which silently overflowed with zero warning. Directly re-verified against the exact real bug that motivated the migration (see the entry above): the same coffee-poster content (`font_size_pct: 70`, the value that overflowed in Pillow) rendered correctly with zero manual tuning; separately confirmed the loop does NOT fix an invisible-title bug (declared text color == background color) -- proving layout-mechanics and design-judgment fixes are genuinely different problems, as argued in the decision. 2 real bugs found and fixed while building: (1) the fit-check's width comparison was comparing `.text-content`'s CSS-declared `width:100%` against itself, always trivially true regardless of content -- removed, height is the only real unknown since word-wrap already guarantees the width axis; (2) Playwright's `page.pdf()` defaults to US Letter and drops CSS backgrounds unless told otherwise -- fixed with explicit `width`/`height` in mm + `print_background: true`, re-verified via `pypdfium2` that the exported PDF is real vector output (210.2x297.0mm exact A4, extractable/selectable text via `get_textpage().get_text_range()`), not a rasterized image. `svg-poster-builder`/`poster-generator` marked `operational_status.state: "superseded"` (new state added to `registry/SCHEMA.md` alongside the existing `paused`; `skill-exporter`'s `list_candidates.py`/`export_bundle.py` hard-exclusion logic extended to enforce it, verified real -- `export_bundle.py svg-poster-builder` now correctly refuses with the superseded reason) -- both left in place, not deleted, same reversibility discipline as a paused skill. Updated 6 dependent skills' cross-references from `poster-generator`/`svg-poster-builder` to `html-poster-composer` (`brand-identity-linter` 0.2.1→0.2.2, `light-logo-arranger` 0.2.1→0.2.2, `media-anchor-profile` 0.1.1→0.1.2, `personal-style-library` 0.1.0→0.1.1, `project-workspace-initializer` 0.3.0→0.3.1's 2 bundled templates) -- all doc-only version bumps, no script changes, all 8 touched skills (including the 2 new/superseded ones) re-verified passing `validate_skill.py`, full 72-skill registry re-checked clean. Deliberately NOT ported this round (real, flagged follow-up, not silently dropped): `poster-generator`'s optional Gemini-powered `suggest_layout.py` (auto-layout suggestion). |
| 2026-08-07 (cont'd) | thatlq1812 asked to fix the 6 skills flagged (not by the day's own 4 items) when this session ran `validate_skill.py` across the full registry for the first time since the GenVid batch was added: `audio-generator-gemini`, `ffmpeg-bootstrap`, `media-pipeline-orchestrator`, `skill-exporter`, `video-assembly-composer`, `video-generator-gemini` all had a `description` past the 1024-char cap (same recurring bug class as the 2026-07-27 compatibility-field cleanup -- narrative accumulating in a spec-capped field over several editing rounds); `media-pipeline-orchestrator` additionally had literal `->` pipeline-arrow ASCII text tripping the validator's angle-bracket check and a `compatibility` field past its own 500-char cap. Fixed by trimming wording only (redundant phrasing, not facts) -- no scope/behavior/contract change in any of the 6, so no version bump, matching the 2026-07-27 precedent for this same bug class. `media-pipeline-orchestrator`'s `->` arrows replaced with the Unicode `→` (already used elsewhere in this registry's own pipeline diagrams) instead of being reworded away. All 71 skills in the registry now pass `validate_skill.py` (previously 65/71 -- the 6 above were pre-existing violations, not introduced this session). |
| 2026-08-07 | thatlq1812 brought `PROJECT.md` (4 directives) after a system-state review session (no open `DECISIONS_PENDING.md` items, 70 skills, all security-audit passed). All 4 executed, registry 70→71. (1) `svg-poster-builder` v0.2.0→v0.3.0: fixed a real label-legibility bug thatlq1812 reported directly ("toàn box đè lên nhau, không rõ ràng") -- confirmed by rendering `layout_template.json` and reading the raw SVG (a small zone's inline label rendered at 1.3mm, near-invisible, no floor on the font-size formula); fixed with a [3.5mm, 11.0mm] clamp, shorter inline labels (id only, except `typography_frame`'s real placeholder text), and a new legend band below the canvas listing every zone's id/type/fill-swatch/`text_label`. No schema/validation change; confirmed `poster-generator` never parses this skill's rendered SVG (computes zone positions from `layout.json` directly), so zero downstream impact. (2) Wired the Light Design cluster (`brand-identity-linter`, `light-logo-arranger`) and the media cluster (`media-anchor-profile`, `poster-generator`) together for real, closing a gap thatlq1812 asked to look into -- `poster-generator` existed but no `SKILL.md` on either side said these composed. Verified end-to-end with zero AI calls: `brand-identity-linter` resolved a 2-color brand's `accent` via fallback-to-secondary, those exact hex values became `poster-generator` `content.json` fills; `light-logo-arranger`'s `compute_anchor.py` computed a collision-checked logo position (confirmed the refusal path is real by forcing a collision separately), converted to zone percentages, rendered into a real poster PNG at exactly that position. `media-anchor-profile`'s stale "poster-generator doesn't exist yet" Known Limitation (it now does) corrected. 4 `SKILL.md`s gained "Chains into" sections + doc-only version bumps (`poster-generator` 0.1.0→0.1.1, `media-anchor-profile` 0.1.0→0.1.1, `brand-identity-linter` 0.2.0→0.2.1, `light-logo-arranger` 0.2.0→0.2.1); no scripts touched in any of the 4. (3) `project-workspace-initializer` v0.2.1→v0.3.0: added `init_project.py` (standalone single-project scaffold -- no `WORKSPACE.md`/date-indexing, for when one project is the whole unit of work, reuses the same `subdirs`/`project_md_prompts` template fields `scaffold_workspace.py` already reads) and 4 new templates (`media-content-creator`, `educator`, `brand-design-project`, `researcher-writer`), each `project_md_prompts` step naming real skills from that cluster in their own documented chain order. thatlq1812 explicitly waived the per-template real-practitioner-elicitation bar for these 4 ("template hoàn toàn có thể điều chỉnh được... cá nhân bạn cũng đủ năng lực để biết cluster nào thì sẽ khai báo project thế nào") -- recorded as `docs/DECISIONS_PENDING.md` item 11; `legal-practitioner` remains the one directly-piloted-user-grounded template. All 5 templates re-verified end-to-end through all 3 scripts (15 runs, all exit 0). (4) New skill `personal-style-library` v0.1.0 (70→71): thatlq1812's own idea (`PROJECT.md` item 4, a personal store for color palettes/styles, reusable later as "generate a poster with this content, this style, that color"), category taxonomy beyond thatlq1812's original 2 delegated to this session ("nhiều cái khác, bạn sẽ nghiên cứu và bổ sung") -- scouted first (W3C Design Tokens Community Group JSON format, color-quantization prior art), then designed as 4 categories (`palettes`, `styles`, `motifs`, `fonts`) each reusing an already-elicited schema in this registry rather than inventing new ones: `palettes/` reuses `brand-identity-linter`'s color-role schema verbatim (validated via direct cross-skill import of its `resolve_colors()`), `styles/` reuses `media-anchor-profile`'s style-anchor-block schema verbatim (validated via its `_validate_anchor_block()`), `motifs/` generalizes `brand-identity-linter`'s per-brand `motifs` field into a reusable catalog, `fonts/` is a lighter non-safety-checked companion to `light-logo-arranger`'s font-fallback table. 4 scripts: `init_library.py` (scaffold), `validate_library.py` (per-category check, reusing the 2 sibling skills' own validators), `extract_palette.py` (real deterministic dominant-color extraction from an image via Pillow's adaptive quantization -- ranks by real pixel share only, never assigns a semantic role), `export_anchor_profile.py` (composes a `styles/` entry into a real `media-anchor-profile`-shaped profile, re-validated against that skill's own validator before writing, not assumed valid). All 4 scripts run for real end-to-end this session: real extraction against a real PNG (5 dominant colors, e.g. `#F9D0C4` at 25.72% pixel share); a real style entry exported and independently re-validated clean by `media-anchor-profile`'s own CLI; every category's bundled valid example plus one deliberately-broken variant each (missing `primary`, invalid `strength`, empty `motifs`, missing `body_font`, a dangling `reference_images` path) correctly accepted/refused. `security-audit` passed (stdlib-only except Pillow, an already-audited dependency in this registry; 0 hits on the standard subprocess/eval/network grep). `stage4_required: false` (foundation/general-capability, deterministic catalog + validator reuse, no uncontrolled external input). |
| 2026-08-05 (cont'd) | GenVid media cluster transfer completed: the 8 skill folders (`ffmpeg-bootstrap`, `media-anchor-profile`, `image-generator-gemini`, `video-generator-gemini`, `audio-generator-gemini`, `video-assembly-composer`, `poster-generator`, `media-pipeline-orchestrator`) are now physically copied into `skills/` (the prior entry below only added registry rows pointing at them -- the actual `SKILL.md`+`scripts/` folders hadn't been copied in yet, fixed same session). `domain` tag changed `general`→`media` for all 8 (thatlq1812 decision: "domain là media"), both in `registry/skills.json` and each skill's own `SKILL.md` frontmatter -- `ffmpeg-bootstrap` included (was `meta`) since its only real consumer today is this cluster. `SKILLS_MAP.md` deleted per thatlq1812 instruction ("có thể xóa luôn") rather than regenerated for the new count; removed as a read-order/repo-structure entry from `CLAUDE.md`, `README.md`, `docs/MASTER_CONTEXT.md` (historical mentions in this file's own older log rows left untouched, per the archive convention). Stage 4 (quality-eval) run for real on the 5 `stage4_required: true` skills from this batch, thatlq1812-authorized same session ("stage 4 có thể chạy luôn cho 5 skills mới") -- Pass A (contract conformance under adversarial input) executed for real against each skill's own scripts via the bootstrapped shared venv; Pass B (with-skill vs baseline delta) could NOT run for any of the 5 -- each has only 1 verified harness (`claude-code`), below quality-eval's own stated >=2-harness precondition, correctly not relaxed. Pass A found and fixed 4 real contract violations, all the same shape (a CLI argument's file was read via `.read_bytes()` before checking the path existed or before a stated argument-combination rule was enforced, crashing with a raw Python traceback instead of the clean one-line refusal every sibling check in the same script already produced): `image-generator-gemini`'s `extract_pdf_page.py` (corrupt PDF + out-of-range `--page`, 2 violations, →v0.4.2), `video-generator-gemini`'s `--last-frame`/`--image` handling (→v0.1.1), `audio-generator-gemini`'s `--clone-voice-sample`/`--clone-consent` handling (→v0.1.1). `poster-generator` and `media-pipeline-orchestrator` had 0 violations (the latter's videos-stage uncaught-traceback-on-exhausted-retries behavior matches its own SKILL.md verbatim, not a defect). All 5 fixes re-verified for real post-fix (clean exit codes, correct messages, valid-input paths unaffected); `registry/skills.json`'s `quality_score.stage4_formal`/`security_audit` updated per skill with the full Pass A record (`verdict: "pass-a-only"`, since Pass B is blocked, not because Pass A itself found nothing). |
| 2026-08-05 | GenVid media cluster transferred into the registry (63→70 skills), built in a standalone project (`D:/elix/projects/20260805_GenVid`) for a real mentee project (Plan47) with the 9-step bootstrap pipeline explicitly waived by thatlq1812 for this batch ("quy trình 9 bước chỉ áp dụng cho các skill được tạo ra từ con số 0, còn những cái này đều là nội dung phổ thông, và có tham khảo chi tiết rồi") -- elicited from a real 22-repo GitHub architecture survey (`data/references/*/NOTES.md` in that project) + real API testing with thatlq1812's own Gemini key, not from scratch. 7 new skills registered: `ffmpeg-bootstrap` 0.2.0, `media-anchor-profile` 0.1.0, `video-generator-gemini` 0.1.0, `audio-generator-gemini` 0.1.0, `video-assembly-composer` 0.1.0, `poster-generator` 0.1.0, `media-pipeline-orchestrator` 0.1.1; `image-generator-gemini` updated 0.3.0→0.4.1 (identity/style-anchor channel split + `--anchor-profile`). Formal license-compliance-check run for the first time on this batch (all 22 surveyed repos' LICENSE files read directly, not guessed -- one self-declared classification cross-checked and confirmed correct, no violation found, no `license_debt` needed); formal dedup-novelty-check run (found the tool's own tag-collision weakness: any two `domain: general` skills sharing one `task_type` hit its 0.8 threshold on tags alone regardless of real content -- every flag manually reviewed and dismissed except the one real, deliberate, already-documented overlap `poster-generator`/`svg-poster-builder`, which extends rather than duplicates); formal security-audit run (all 8 passed, no serious finding -- see each entry's `security_audit.note`). One real fix applied during transfer: `media-pipeline-orchestrator`'s `generate_script.py` no longer redefined Veo's `duration_seconds=(4,6,8)` constraint as its own separate constant (a duplicate-source-of-truth risk its own v0.1.0 Known Limitations had flagged) -- now imports it directly from `video-generator-gemini`. 5 skills gained `stage4_required: true` in this batch (Gemini-API-calling / external-content-ingesting: `image-generator-gemini`, `video-generator-gemini`, `audio-generator-gemini`, `poster-generator`, `media-pipeline-orchestrator`); `ffmpeg-bootstrap`/`media-anchor-profile`/`video-assembly-composer` classified exempt (no AI/network ingestion). Full review trail: `data/references/LICENSE_COMPLIANCE_REVIEW.md`, `DEDUP_NOVELTY_CHECK_REVIEW.md`, `SECURITY_AUDIT_REVIEW.md` in that project. Not yet done, flagged for a later session: formal stage 4 (per standing policy, not scheduled without asking first); a final decision on whether these 8 skills stay tagged `domain: general` or get a new dedicated `domain: media`/`creative` (SkillsMP occupation-group axis, thatlq1812 call); cleanup of that project's `data/test_output/`/`data/projects/` dev fixtures once no longer needed as evidence. |
| 2026-08-03 (cont'd 2) | `slide-deck-composer` v0.8.1 → v0.9.0: thatlq1812 directed a v3 dogfooding round on a genuinely new content genre (legal/procedural, not marketing) — real content from `test/Aug032026_v4/` (a synthetic legal-document test fixture this project's own Legal Skill Network generated for a different testing purpose, generic placeholder names, not real people), against a fitting formal template (`Federal Law Enforcement Training Center by Slidesgo.pptx`). Content deliberately scoped to procedural/legal-analysis register (legal basis, skill-chain validation results, checklist status) rather than narrative case details. Surfaced a significant, previously-hidden bug: `text_utils.clean_markdown_formatting` ran BEFORE `rich_text.parse_inline_formatting` (the real run-based formatter) on every text-injection call site, silently stripping every Markdown bold/italic/code/link marker to plain text before the real formatter ever saw them -- meaning that formatting had never actually rendered as styled output in this skill's entire history (survived 8 prior hardening rounds only because no prior test content happened to use inline emphasis syntax). Surfaced concretely by real content containing literal Python filenames with underscores (`validate_legal_brief.py`) -- the underscore-stripping regex matched ANY pair of underscores anywhere in a string, corrupting it into `validatelegalbrief.py` in real rendered output. Fixed: removed every regex `clean_markdown_formatting` shared responsibility for with `parse_inline_formatting`, keeping only leading-bullet-marker stripping and HTML-comment removal. Re-verified directly: bold/italic/code/link now correctly produce styled runs through the full real pipeline, and literal underscores survive intact -- confirmed both by direct testing and by rendering the recompiled deck. Also fixed the weight/style-suffix font-table duplication flagged as a known limitation in v0.8.1, since it recurred a third time (`Poppins ExtraBold`/`Poppins Black`) -- `check_font_vietnamese_safety` gained a fixed, unambiguous weight/style-suffix-stripping fallback (never a fuzzy/prefix match); added 2 more real, cited font entries (Poppins, Zen Kaku Gothic New, each confirmed via direct fetch of METADATA.pb -- neither has a vietnamese subset). Regression-confirmed the v0.8.0/v0.8.1 fixes generalize to a third, unrelated real template (same card-cluster pattern correctly required `custom`; same watermark-false-positive class correctly fixed on a different fake contact block). Full regression re-run of 3 prior real test decks after the formatting-pipeline change, all still compile/validate clean. `registry/skills.json` and `SKILL.md` updated (version, `security_audit`, Verified, Known limitations). Artifacts in `test/Aug032026_v3/` (gitignored). |
| 2026-08-03 (cont'd) | `slide-deck-composer` v0.8.0 → v0.8.1: thatlq1812 asked to re-run the exact same 20-slide dogfooding process (v2, `test/Aug032026_v2/`), same brief and same template (`AI Automation by Slidesgo.pptx`) as `test/Aug032026/`, entirely in this session, to verify the v0.8.0 cardinality fix end-to-end. Confirmed it works exactly as designed: the same "THREE IDEAS" 3-card slide (only reachable via `custom` now) rendered correctly filled every one of its 5 real reuses in the new deck. Two more real bugs found and fixed along the way: (1) `--vietnamese` correctly refused 3 real fonts this template uses that had never been checked (Silkscreen, Schibsted Grotesk, TASA Orbiter) -- the refuse-loudly design working as intended, not a bug; each verified via a real source (direct fetch of the font's own `METADATA.pb` on `github.com/google/fonts`) before being added to `font_vietnamese._FONT_TABLE`, not guessed. (2) A real bug: `shape_roles.is_watermark_text`'s bare brand-word substring match protected an obviously-fake placeholder contact block ("YOUREMAIL@FREEPIK.COM") from ever being cleared, because "freepik" happened to appear inside the placeholder email domain -- confirmed via direct shape inspection and by rendering the compiled output (the fake contact info was visibly present, unused, never blanked), and confirmed as the root cause of a real `validate_deck.py` failure too (`no_leaked_vendor_text`). Fixed: a brand-word match immediately adjacent to `@` or `.` (the shape of an email/domain token) no longer counts as a real attribution match; verified this doesn't weaken protection for genuine attribution lines (both real historical regression cases still match). Re-verified: recompiled the full 20-slide v2 deck with `--vietnamese`, `validate_deck.py --original` reports all 8 checks clean, every slide type rendered to JPG and visually inspected including the specific shapes these bugs affected -- the previously-fake contact block now correctly shows real injected closing content, with the genuine required Slidesgo/Freepik credit line (a slide-layout-level element, confirmed outside this skill's shape tree entirely) still intact. `registry/skills.json` and `SKILL.md` updated (version, `security_audit`, Verified, Known limitations). Artifacts in `test/Aug032026_v2/` (gitignored): phase1-2 docs, `content.json`, compiled deck + PDF + per-slide JPGs, compile/validate reports. |
| 2026-08-03 | `slide-deck-composer` v0.7.1 → v0.8.0: card-cluster cardinality mismatch, surfaced by a real 20-slide deck thatlq1812 pointed at (`test/Aug032026/`, built in a separate session — a real deck introducing the whole Scriptorium project against `AI Automation by Slidesgo.pptx`, with its own honest self-review `phase4_evaluation_review.md` flagging two suspected engine gaps). Verified the claims directly before touching code: the reported "engine can't clear graphic badge shapes" was wrong in its own framing (the "1"/"2"/"3" badges are correctly-classified decorative digit text, correctly left alone) but pointed at a real, worse defect underneath -- a "THREE IDEAS" template slide's 6 real body shapes are 3 header+description card pairs, and `_hard_structural_fit` (the check an explicit `source_slide_idx` must clear) only verified `body_count >= min_body` (2 for `two_column`), which 6 trivially clears; `find_title_and_body`'s ranking then picked its 2 best-scoring shapes with no notion they belong to different cards. Rendered the real compiled output to confirm: content landed in 2 shapes from *different* cards, the other 2 cards left completely blank, zero error anywhere in the compile report -- worse than a stale-text leak, since real content silently vanished with no signal. Reproduced directly with a minimal repro (`test/slide-deck-composer-v2/repro_cardinality.json`). Fixed: new `_MAX_BODY_SLACK` per-layout cap (`two_column`: 3, `standard`/`section`: 8, `image`: 4, `quote`/`title`: 2) wired into both the explicit-`source_slide_idx` path and the automatic-selection path (previously looser there too, same flat 8-shape cap for every non-quote/title layout). A slide offering drastically more body shapes than the layout needs is now refused loudly, naming `custom` as the fix. Re-verified: the repro now fails with a concrete message; the pre-existing 10-slide regression deck still compiles unchanged; this skill's own real 7-slide showcase deck (v0.7.1) hit the SAME tightened cap on 2 of its own slides (a `two_column` and a `section`, 7 and 11 real body shapes respectively) -- converted both to `custom` with an exact slot per shape, recompiled, revalidated (8/8 checks clean), re-rendered, visually confirmed clean. Also surfaced by the same real deck, documented not code-fixed: reusing the same source slide across unrelated positions (new `repeated_source_slides` report field added so it's visible without manual cross-referencing) and a section-divider's baked-in decorative ordinal ("01") not updating when reused for a later, differently-numbered part -- both added to "Known limitations" (the ordinal case can't be safely auto-fixed without reopening the false-positive class the decorative-digit rule exists to prevent). `registry/skills.json` and `SKILL.md` updated (version, `security_audit`, Verified, Known limitations, Usage). Repro/regression artifacts in `test/slide-deck-composer-v2/` (gitignored). |
| 2026-08-02 (cont'd 3) | `slide-deck-composer` v0.7.0 → v0.7.1: thatlq1812 asked for a real, best-effort dogfooding round ("chạy thử một slide mà bạn nghĩ bạn có thể làm tốt nhất"). Built a real 7-slide showcase deck about this skill's own build history against a new real template not tested before (`Data Infographics by Slidesgo.pptx`, a dense stat-dashboard style — every slide built from many small separate chart/table/callout widgets, no plain slides at all), exercising thumbnail-grid selection, `custom` layout with precisely pre-counted slots, and the v0.7.0 schema validator together in one real compile for the first time. Compiled, validated (`all_checks_passed: true`), rendered to JPG via LibreOffice, and actually looked at every slide — found 3 more real bugs no self-test had hit: (11) the leaked-vendor-text watermark-paragraph threshold (40 chars) false-flagged the template's own legitimate "This template has been created by Slidesgo" footer (42 chars, confirmed by direct measurement) — raised to 60, comfortably clear of the real footer while still catching both real historical instructional-paragraph bugs (#4, #10); also fixed an independent duplicated hardcoded `40` in `validate_deck.py` that had drifted out of sync with the same constant. (12) a real vendor "Infographics" editing-instructions slide (template slide 21: "Choose your favourite infographic...", "Keep source formatting", split across ~7 short step-boxes) matched none of the existing instruction markers and got auto-picked for 4 different output slides in the same compile, each showing the same irrelevant flowchart graphic — fixed by adding 2 specific, idiomatic markers no real content would contain; verified `is_instruction_slide` now catches it while still catching bugs #4/#10 at the new threshold. (13) a native chart's vendor editing caption ("Follow the link in the graph to modify its data...") leaked into real output on two slides — it sits in the same bottom strip as the legitimate footer, so the position heuristic built to protect that footer also protected this vendor filler, and since it names no brand word `is_watermark_text` never caught it either; fixed with a new, distinct `shape_roles.is_vendor_instructional_caption` category (a watermark is legitimate content to protect, a chart caption is vendor filler that must always clear, regardless of position) wired into both the compile-time clearing sweep (force-clears even when decorative/watermark would otherwise protect it) and `validate_deck.py`'s leaked-text check (independent, position-agnostic). Also surfaced and documented, not code-fixed: this specific template has no slide structurally resembling a plain pull-quote or 2-region comparison, so `two_column`/`section`/`quote` against it left some sibling regions blank or backing charts visible — a genuine template/layout mismatch, not a script defect, correctly worked around in the same round by using `custom` (2 of 7 slides, zero visual defects). All 3 fixes independently re-verified: recompiled, re-ran `validate_deck.py --original` (all 8 checks clean), re-rendered to JPG, visually confirmed zero leaked text across the final 7-slide deck. `registry/skills.json` and `SKILL.md` updated (version, `security_audit`, Verified, Known limitations). Test artifacts in `tests (ignore)/slide-deck-composer/` (gitignored). |
| 2026-08-02 (cont'd 2) | `slide-deck-composer` v0.6.0 → v0.7.0: real ECMA-376 schema validation, closing a gap a reviewer agent named directly when comparing this skill against Anthropic's own `pptx` skill — that skill's `validate.py` checks a real XSD schema, `validate_deck.py` (through v0.6.0) only checked well-formedness plus 7 self-authored heuristic checks, categorically weaker since well-formed XML can still violate the OOXML schema. Feasibility confirmed empirically before adoption (the real risk: real-world OOXML producers like Slidesgo/Google-Slides exports sometimes deviate from the pure standard while still opening fine in PowerPoint, which could make strict validation noisy/untrustworthy) — validated 15 real slide XML parts across 3 different real Slidesgo templates plus 9 slides from this skill's own real compiled output, all 24 passed clean, before committing to build anything. Schema source chosen for a verifiable license, not the first hit: rejected an unlicensed GitHub mirror (`t-yuki/ooxml-xsd`), used instead `scanny/python-pptx`'s own repo, which bundles the complete real ECMA-376 schema set (24 files) under the same MIT license `python-pptx` already carries as this skill's core dependency — copied verbatim into `assets/xsd/` with a `NOTICE.md` recording provenance. No new Python dependency (`lxml.etree.XMLSchema`, already used throughout this skill). `validate_deck.py` gained an 8th check, `ooxml_schema_valid` (always-run, not `--original`-gated). Verified for real: a deliberately corrupted slide XML (invalid attribute injected onto `<p:sld>`) correctly rejected with the exact element/attribute/reason named, exit 3, while remaining well-formed (proving the new check catches something the existing well-formedness check structurally cannot); full compile-then-validate regression against a real template still passes all 8 checks clean. `registry/skills.json` updated (version, `security_audit`). |
| 2026-08-02 (cont'd) | `slide-deck-composer` v0.5.0 → v0.6.0: closes the font-safety blind spot the cold-agent test flagged as an honest limitation rather than fixing on the spot. Root-caused for real: direct XML inspection of `Vintage Recipe Book by Slidesgo.pptx` (the tester's own chosen template) confirmed its real title font ("Libre Baskerville") is declared on the slide LAYOUT's `<a:lstStyle>/<a:lvl1pPr>/<a:defRPr>/<a:latin>`, not `theme1.xml` (which just says "Arial") -- the old `--vietnamese` check only ever read the theme, so it silently missed the template's real typography entirely. New `font_manager.resolve_effective_font(shape, slide)`: checks a shape's own explicit run-level font, then its slide-layout placeholder's `lstStyle`, then the slide master's `titleStyle`/`bodyStyle`/`otherStyle`, falling back to the theme only if none of the three has an explicit font. `compile_deck.py` restructured from one deck-wide font decision to a per-shape `_font_for` closure (an explicit `--heading-font`/`--body-font` override still applies globally, unchanged). Real, cited finding added to `font_vietnamese._FONT_TABLE`: web-searched Libre Baskerville's actual language coverage (Google Fonts specimen page + a googlefonts-discuss thread) -- confirmed no Vietnamese subset ships for this real Google Font, added as `("serif", False)` with the citation in a code comment, not guessed. Full pipeline re-verified against the cold-agent tester's real 9-slide deck with `--vietnamese`: `font_resolution_log` now correctly shows the title `Libre Baskerville -> substitute -> Times New Roman` while the subtitle shows `Roboto -> safe -> Roboto` (unchanged, not force-overwritten) -- confirmed via direct run-level `python-pptx` read-back on the real compiled output. Regression confirmed both ways: no-`--vietnamese`/no-override compile unchanged; explicit `--heading-font`/`--body-font` override still applies globally as before. `registry/skills.json` updated (version, `security_audit`). |
| 2026-08-02 | `slide-deck-composer` v0.4.0 → v0.5.0: first independent cold-agent usability test, thatlq1812-directed ("cho một subagent vào... bạn là khách hàng, yêu cầu nó tạo ra sản phẩm..."). Closes a real gap flagged twice earlier this session — every prior round's testing was done by the same session that wrote the code, with full accumulated context; this dispatched a genuinely fresh agent (no session history, briefed only by reading `SKILL.md` cold) to act as a deck-design vendor fulfilling a real, novel client brief (a Vietnamese artisanal coffee-roasting startup's investor pitch deck — deliberately different subject/tone from every deck this session tested itself) using its own judgment throughout, then report friction honestly. The tester picked a real illustrated "Vintage Recipe Book" template (not a corporate one — correctly matched the brief's tone), used `thumbnail_grid.py` for every slide, and used `custom` for 5 of 9 slides (direct evidence the escape hatch built two rounds ago is genuinely load-bearing, not just theoretically useful), rendered every slide via LibreOffice before delivering per the skill's own documented discipline. Found 2 real, independently-confirmed bugs: (1) `LAYOUT_2COL`'s `### Left`/`### Right` sub-headings were parsed by `text_utils` but never read by `compile_deck.py` -- silently discarded, no error; the tester found this by rendering and worked around it, this session confirmed the bug by reading the code, then fixed it (`_inject_bullets` gained an optional bold-heading param) and re-verified with the tester's exact real content (both headers now land, confirmed bold via run-property read-back). (2) `custom` layout's positionally-ranked slot matching gave the tester no way to know a slot's real target position before compiling -- a team-slide test swapped a founder's bio under the wrong-gender photo on the first compile, caught only by rendering; fixed by adding a `slot_mapping` report field (each slot's real target `left_pct`/`top_pct`/`area_pct`) so a caller can cross-check against an already-rendered thumbnail without a second compile cycle, re-verified against the tester's real content.json showing the exact real left/right positions. One honest limitation surfaced and documented, not code-fixed: `--vietnamese`'s font-safety check only reads `theme1.xml`, blind to slide-layout/master-level typography some real templates use instead. Regression confirmed: the pre-existing 10-slide test deck recompiled unchanged. `registry/skills.json` updated (version, `security_audit`). Cold-agent deliverable + report artifacts: `tests (ignore)/slide-deck-composer/cold_agent_delivery/` (gitignored). |
| 2026-08-01 (cont'd 8) | `slide-deck-composer` v0.3.0 → v0.4.0: grouped-shape support, a thatlq1812-directed priority call — offered the choice between building more layout coverage next or fixing this foundational gap first, thatlq1812 chose the foundation ("làm ổn trước vẫn hơn là làm cái nhà cao hơn trong khi cái móng còn yếu"). Real 3-template survey done BEFORE writing fix code: `Business Innovation Plan` has a 60-child-shape vector-illustration group with empty text frames (correctly already excluded by the pre-existing `is_decorative_text` filter, zero regression risk confirmed empirically, not assumed); multiple templates have real grouped `Picture` shapes previously invisible to `image_injector` ("no picture to inject into" on every one); `Candy Pastels Galaxy` slide 54 has real grouped TEXT — a title and 5 real fact captions, all previously invisible to `find_title_and_body`. Implemented real OOXML group-transform math (`shape_roles.iter_shapes_with_absolute_bbox`/`_group_transform`, per the real `a:xfrm`/`chOff`/`chExt` spec, handling both identity and non-identity scale/offset transforms, not just the common identity case) so every shape's bounding box is correctly computed regardless of nesting depth; `score_title_shape`/`score_body_shape`'s blanket exclusion of grouped shapes was removed (now scored on real geometry through the same decorative/watermark filters top-level shapes already use); `image_injector.py`'s picture-finding updated to the same recursive walker. Verified for real: a grouped fact-caption on `Candy Pastels Galaxy` slide 54 successfully replaced via the `custom` layout, rendered to JPG, confirmed the new text landed in the exact correct position with no distortion (direct proof the transform math is correct); a grouped picture on `Business Plan_ Minimalist Aesthetics` slide 7 successfully replaced with a placeholder image, frame dimensions in the report matched the real absolute size exactly, rendered and visually confirmed correctly positioned/letterboxed. Regression confirmed: the pre-existing 10-slide real test deck recompiled with identical output. `registry/skills.json` updated (version, `security_audit`). |
| 2026-08-01 (cont'd 7) | `slide-deck-composer` v0.2.2 → v0.3.0: `custom` layout, an ad-hoc slot-mapping escape hatch, thatlq1812-directed: "khai báo thêm layout tạm nếu trong template có, và nhu cầu sử dụng của bạn cần nó." Rather than hardcoding the ~14 not-yet-built layout shapes one at a time, `content.json` can now declare `{"layout": "custom", "source_slide_idx": N, "slots": [{"role": "title"|"body", "text": "..."|["...","..."]}]}` for any real template slide shape the 6 named layouts don't cover. No automatic selection (mandatory `source_slide_idx` — there's no generic pool to search for an ad-hoc shape, the caller already judged the slide visually via `thumbnail_grid.py`); positional matching (declared title slot -> the slide's one scorable title shape, body slots in written order -> `shape_roles`' own ranked body-shape order); refuses loudly on any slot/shape-count mismatch. `constants.py` gained `LAYOUT_CUSTOM`; `compile_deck.py` gained validation + a new injection branch reusing the exact same `_inject_single_line`/`_inject_bullets`/font-size-balancing/Vietnamese-font-safety primitives every named layout already uses. Verified for real: compiled a 2-slide deck against `Business Innovation Plan by Slidesgo.pptx`'s real slide 10 — a `$4,498,300,000` stat-callout shape confirmed (same session, prior round) as unusable by any of the 6 named layouts even with `source_slide_idx` pointing right at it — rendered to JPG, visually confirmed the title-role slot ("63 skills") landed correctly in the large stat box and the body-role slot's caption landed correctly below it, template design untouched. 2 refuse-loudly paths confirmed: missing `source_slide_idx` (exit 2); 3 slots declared against a slide with only 1 real body shape (exit 2, names the exact shortfall). Backward compatibility confirmed: the pre-existing 10-slide real test deck recompiled unchanged. `registry/skills.json` updated (version, `security_audit`). |
| 2026-08-01 (cont'd 6) | `slide-deck-composer` v0.2.1 → v0.2.2, doc-only. thatlq1812 raised two points: (1) layout coverage should grow toward common real-world needs, and content should be outlined and classified into a layout *before* being written, not the reverse — agreed as sound, and validated by the thumbnail-grid work itself: real templates surveyed this session visibly contain slide types (a big stat-number callout, a budget pie chart, an org chart) the current 6 abstract layouts can't use even with `source_slide_idx` pointing right at them, confirming layout-coverage expansion is a real, not cosmetic, gap (tracked, not built this round). (2) The "cold agent" context gap flagged earlier this session (all verification so far done within one continuous session with accumulated context) — thatlq1812 asked to re-read `SKILL.md` and add guidance closing it. Re-read the full file, rewrote "Usage" into an explicit 6-step workflow: outline content first (no layout yet) -> classify each section's shape against a real content-shape-to-layout mapping table (with an explicit escape hatch: say so and don't force-fit when nothing matches, rather than picking the nearest wrong layout) -> render the thumbnail grid now knowing what to look for -> write markdown shaped for the chosen layout's actual fields -> compile -> validate+render-check flagged slides only (not every slide, defeats the point of the triage tools). No script changes. `registry/skills.json` version bumped. |
| 2026-08-01 (cont'd 5) | `slide-deck-composer` v0.2.0 → v0.2.1: content-density triage, a thatlq1812-proposed idea ("phân tích vùng nội dung dựa trên màu nền, chỉ ra những điểm còn trống — máy học hay không cũng được, thử research trước") researched and validated by this orchestrating session directly (no sub-agent dispatch — small enough scope to do inline) before writing any shipped code. Researched real prior art first (web search): found `enimda` (PyPI, entropy-based whitespace/border detection), a content-density-threshold `crop_empty.py` pattern, and Canny-edge-based empty-shelf-space detection — confirmed no existing tool solves this for pptx-template slide content specifically. Ran a real 2-round experiment on an actual rendered slide (`v7_slide_00.jpg`) before deciding on a technique: round 1 (naive per-cell pixel-variance, 16x16 grid) wrongly flagged 177/256 cells "empty," including most of the slide's flat-color illustration — visually confirmed wrong via a red-tint overlay. Round 2 (edge density via PIL `FIND_EDGES` instead of raw variance) correctly excluded the illustration and correctly flagged only the genuine blank background — visually confirmed correct via the same overlay technique. This directly answered thatlq1812's own framing (deterministic pixel analysis, not a trained ML model, since the calling agent already has multimodal vision — an ML model would just duplicate judgment the agent already has cheaper access to) — recommended and agreed before building. Built `scripts/content_density.py` (edge-density grid scan + largest-empty-rectangle via the standard histogram/monotonic-stack algorithm), explicitly scoped as a triage/pre-filter tool, never a judge — flags `flagged_for_review`, never auto-adds/removes content, since a large empty region can be intentional whitespace (this skill's own "Zero-noise delivery" example slide). Reuses `thumbnail_grid.py`'s `render_template_to_pdf`/`pdf_to_thumbnails` rather than duplicating the LibreOffice subprocess call. Verified for real against `scriptorium_intro_v7.pptx` (the same 10-slide real deck from earlier rounds): flagged 5/10 slides; overlay images for 2 of them visually inspected and confirmed correct — slide 9 ("What is next," a sparse closing slide) got the single largest flagged region (70.7% empty, 49.2%-area bounding rectangle covering the actual blank lower half), slide 6 ("What is in the registry today") correctly excluded its real bullet text from the empty mask while still flagging the genuine blank area below it. Refuse-loudly path confirmed (nonexistent deck path, exit 2). `registry/skills.json` updated (version, `dependencies` gained `numpy==2.5.1`, `security_audit`). |
| 2026-08-01 (cont'd 3) | `slide-deck-composer` v0.1.0 → v0.1.1, a real hands-on visual-review round thatlq1812 explicitly asked for: "loop-fix tới mức nó ổn." Every bug the original build found (see the row below) was caught via structural/XML inspection alone, never an actual rendered image — this round rendered for real (`libreoffice --headless --convert-to pdf` + `pypdfium2` → JPG) a real 10-slide test deck introducing this project itself (`tests (ignore)/slide-deck-composer/content.json`, gitignored, not a shipped asset) against a real Slidesgo template, and the very first render immediately surfaced defects invisible to XML-level checks. Found and fixed 5 more real bugs across 7 render→inspect→fix→re-render iterations: (6) `shape_roles.score_body_shape`'s flat `+1000` placeholder-type bonus let a tiny 6-item icon-grid's caption box outscore genuinely spacious body placeholders — fixed with new `scripts/shape_capacity.py` (ported from `platform_archive`'s `template_import/capacity.py`, dimension-only EMU heuristic) giving every shape a real character-capacity estimate, now the dominant scoring term; (7) short content still cleared a tiny caption's capacity via relative slack alone, but the box remained visually the wrong shape — fixed with an absolute 120-char prose-capacity floor; (8) `quote`/`title` layouts never touch a body shape, so an icon-grid slide's normal-sized title let it slip through capacity checks entirely, leaving 6 unrelated icons in frame — fixed with a body-shape-count cap for those layouts; (9) a different infographic pattern (a decorative world-map graphic with 19 small per-region caption boxes) passed every geometric check and still got picked for a plain bullet slide — fixed by generalizing the shape-count cap to all layout types (`_MAX_BODY_SHAPES_FOR_PROSE = 8`); (10) fixing #9 shifted round-robin onto a previously-unseen slide whose title was literally "Fonts & colors used" (a Slidesgo template meta/credits slide) — real, normal-capacity text, so none of the geometry fixes could catch it; `slide_classifier._INSTRUCTION_MARKERS` already had `"fonts used"` from the original build's bug #4 but missed this near-miss substring (`"fonts & colors used"`), fixed by adding the exact phrase plus 2 close variants. All 10 fixes independently re-verified by this session re-rendering the same real deck after each change; final render shows all 10 slides clean. `registry/skills.json` (version, `security_audit`) updated. |
| 2026-08-01 (cont'd 2) | Built `slide-deck-composer` v0.1.0 (registry: 62→63 skills), the `slide-deck-builder` candidate parked earlier the same day. thatlq1812 reversed the initially-recommended "build-from-scratch" (`ooxml/layout_builder.py`) architecture in favor of "clone-and-inject" (`pptx/compiler.py`/`pptx/slide_builder.py`) after comparing against `github.com/thatlq1812/frontend-slides` (scouted for pattern-learning only, not copied — its progressive-disclosure template-index pattern and independently-confirmed content-density axis are cited in the new skill's SKILL.md, its HTML-deck output target and agent-improvises-aesthetics model were explicitly not adopted): thatlq1812's reasoning was that Elixverse's own fully-deterministic scoring/validation approach was itself a design mistake given current agent capability — "cung cấp hạ tầng và bối cảnh là đủ, thêm nữa là thừa và lấy đi mất khả năng sáng tạo của agent." Real Slidesgo `.pptx` templates (`D:/Document/slidesgo_templates`, 149 files/1.9GB) copied to repo-root `data/` (new `/data` `.gitignore` entry) as **local dev/test fixtures only** — Slidesgo's license does not permit redistributing raw template files, so the skill is built BYOT (Bring Your Own Template): the caller supplies any `.pptx` template path they have rights to use, this skill never bundles or hardcodes one. Built via one background agent (isolated worktree), interrupted once by a session-limit error mid-build and resumed from its own transcript (same agent, full context retained) to finish. Ported 13 scripts (~2100 lines) from `D:/elix/archive/platform_archive/modules/presentation/` (thatlq1812's own prior production code, no license issue), stripped of every non-portable dependency confirmed by this session's earlier research pass: `font_service.py`/`font_embedder.py` (live Google Fonts API), `image_providers.py` (AI image-gen + stock-photo API via an AIRouter — exactly the AI-backend-in-the-loop pattern `CLAUDE.md` principle 8 forbids), `template_manager.load_from_database` (GCS), `compiler.convert_pptx_to_pdf` (libreoffice subprocess), `placeholder_images.py`'s stock-photo-inventory lookup (depends on Elixverse's own separately-licensed photo corpus, same redistribution problem as the templates). Ported 6 of the source's ~20 layout types for v1 (title, standard, two_column, image, quote, section) — the rest explicitly listed as not-yet-built in the skill's own `SKILL.md`, not silently dropped. 2 new features per thatlq1812's explicit request: `font_vietnamese.py` (Vietnamese-diacritic font-safety checker, cross-checked against `layout_registry/font_config.py`+`template_factory/font_pairing.py` from the same archive plus this repo's own `office-doc-creator` `ALLOWED_FONTS` allowlist, refuse-rather-than-guess pattern matched from `light-logo-arranger/scripts/resolve_font_fallback.py`) and `font_size_balance.py` (content-length-ratio-driven font-size step-down, prioritizing standalone titles per thatlq1812's own framing — a new, self-designed heuristic, not a port). Ran thatlq1812's specified self-test methodology for real: 3 structurally different real Slidesgo templates × 3 content sets (short pitch, content-heavy, Vietnamese-diacritics) — found and fixed 5 real bugs (stale-text leaks from naive round-robin reuse; a shape-identity bug wiping just-injected content; `section` layout silently dropping un-headed subtitle text; English-only vendor-instruction-slide detection missing a real Portuguese template page; closing-slide reuse picking a title-less picture-only slide and silently dropping quote content), each confirmed fixed via re-run + XML read-back. Independently re-verified by this orchestrating session (not just trusted from the agent's report): ran `compile_deck.py` for real against `Business Plan_ Minimalist Aesthetics by Slidesgo.pptx` with 5 Vietnamese-diacritic slides and `--vietnamese`, confirmed via direct `python-pptx` reopen + `zipfile.testzip()` that all 4 bullets/title/quote/section text landed correctly, font resolved to a real Vietnamese-safe font (Arial), and font-size balancing actually applied (28pt→18-20pt titles, 16pt→10pt body, matching the tool's own reported ratios). Also independently caught and fixed 2 real spec violations the building agent's own validation apparently missed before finishing: `metadata.source: "self-authored, engine ported/adapted..."` (only `self-authored`/`harvested` are valid per schema) and `metadata.version: 1.0.0` (should start at `0.1.0` like every other skill in this registry, not claim a 1.0 maturity level for a v1 build with 14/20 layout types still unbuilt) — both fixed, `validate_skill.py` now passes clean. `security_audit` self-check re-run by this session: clean, and confirmed no Slidesgo filename/path is bundled or hardcoded anywhere in the skill folder (the only real "slidesgo" string literals are vendor-watermark/instruction-text DETECTION keywords used at runtime against a caller-supplied template — a real, clever piece of bug-fix #4/#5's design, not a leak). `stage4_required: true` (ingests arbitrary caller-supplied `.pptx` files — uncontrolled external input, same bucket as `document-ai-structurer`/`browser-web-renderer`) — not yet scheduled, per standing policy. Composability documented in the skill's own `SKILL.md`: `image-generator-gemini` (fills picture placeholders, replacing the AI-image-gen path deliberately stripped from `image_providers.py`), `deep-research`/`literature-review`/`citation-management` (content source), `personal-profile-manager`'s `org_profile` (title-slide org identity), `light-logo-arranger`'s `resolve_font_fallback.py` (same refuse-rather-than-guess pattern this skill's own font checker follows). Not yet done, flagged as open follow-up: the ~14 remaining layout types, `layout_registry/taxonomy.py`'s auto-sequencing engine (deliberately deferred, matches thatlq1812's "don't over-automate what the agent can judge" direction), and font embedding (network-dependent, stripped entirely for v1). |
| 2026-08-01 (cont'd) | Document-standard + Light Design upgrade round, thatlq1812-directed after reviewing `SKILLS_MAP.md`. thatlq1812 pointed to `D:/elix/archive/platform_archive` (an archived prior version of thatlq1812's own Elixverse platform) as a real elicitation source for both clusters — surveyed first via an Explore agent (research-only, no modification), then externally scouted GitHub (docxtpl, python-docx, deterministic-SVG-tooling space — confirmed genuinely thin, `platform_archive` is the stronger source for both clusters). 6 existing skills version-bumped via 5 parallel background sub-agents (`isolation: worktree`, one skill-group per agent, no file-path overlap so no merge conflicts), each independently re-verified by this orchestrating session (not just trusted from the agent report) before registering: `office-doc-creator` 0.1.1→0.2.0 (Vietnamese-diacritics `w:rFonts` fix + real OOXML `w:abstractNum`/`w:num` list numbering, both ported from `modules/document/v6/renderers/docx/{styles,numbering}.py`; docxtpl evaluated and explicitly NOT adopted, reasoning recorded in the skill's own SKILL.md); `latex-project-bootstrap` 0.4.0→0.5.0 (vnnd30 mode gained a `--density {tiet_kiem_giay,tieu_chuan,trang_trong}` preset from `modules/document/v6/config/law_constants.py`, plus 2 new doc_kieu — `cong_dien`/`giay_moi` — grounded directly in the real NĐ 30/2020 government PDF, `modules/document/v6/config/doc_type_taxonomy.py` was checked but lacked field-level layout so wasn't the actual grounding source; 2 real bugs found and fixed during the building agent's own verification, see the skill's own row); `personal-profile-manager` 0.1.1→0.2.0 (new optional `org_profile` section — departments, signatory roles with mandatory signing-authority prefix, cross-field reference integrity — ported from `modules/document/v6/co_quan_profile/schemas.py`, not yet wired into `light-logo-arranger`/`latex-project-bootstrap`, composability intent documented only); `brand-identity-linter` 0.1.0→0.2.0 (color-role fallback-chain resolution replacing flat presence-check, max-3-accent-colors, background-never-pure-white — from `modules/presentation/template_intel/palette_binding.py` and the statistically-sourced `modules/presentation/scoring/design_rules.py`, real analysis of 149 Slidesgo templates/7,854 slides + 10 Canva templates); `svg-poster-builder` 0.1.0→0.2.0 (max-40%-text-coverage rule from the same `design_rules.py`; the companion min-30%-whitespace rule was deliberately NOT ported — the building agent found it didn't fit this skill's real zone schema, full-bleed background zones would always fail or overcount under intentional zone overlap, documented as a forced-fit refusal rather than silently contorting the skill); `light-logo-arranger` 0.1.0→0.2.0 (2 new scripts: `check_asset_completeness.py`, a brand-asset-deliverable gap checklist from `scripts/gen/gen_brand_identity.py` — checklist only, the AI-image-generation mechanism was explicitly NOT ported; `resolve_font_fallback.py`, a font-substitution resolver using the real ~50-entry mapping table ported verbatim as data from `modules/presentation/pptx/font_fallback_map.py`, refuses loudly on any unmapped font rather than guessing). All 6 re-validated via `skill-creator`'s `validate_skill.py` after merge — 3 SKILL.md `description` fields initially exceeded the 1024-char spec cap after the agents' edits (a real, caught spec violation, same class of bug as the 2026-07-27 `compatibility`-field cleanup), trimmed by this orchestrating session, all 62 skills re-verified passing. Orchestrator independently re-ran (not just re-read) representative real tests beyond each agent's own report: a real Vietnamese-diacritics docx generated and its `word/styles.xml`/`document.xml` unzipped and inspected for all-4-rFonts-attributes; a 3-list docx unzipped and its `word/numbering.xml` inspected for distinct abstractNum/num entries; a real `cong_dien` document compiled end-to-end via `typst compile` with `--density trang_trong`; `personal-profile-manager`'s `--with-org-profile` scaffold-then-validate round-tripped clean; `light-logo-arranger`'s both new scripts run against both a valid and a refusal case each. Combined security self-check (grep `subprocess`/`os.system`/`eval(`/`exec(`/`urllib`/`requests`/`socket`/`shell=True` across all 6 skills' scripts) found zero new hits beyond the pre-existing, already-audited `typst`/`pandoc` subprocess calls in `latex-project-bootstrap`. `registry/skills.json` updated centrally by this session (versions, `security_audit` dates/notes, `elicited_from` appended with the real `platform_archive` source paths) — not done by the parallel agents themselves, to avoid registry merge conflicts. The `slide-deck-builder` candidate (a much larger cluster found in the same `platform_archive` survey — a statistically-grounded deterministic slide-layout/color-role/font-pairing engine, `modules/presentation/`) was deliberately NOT started this round — thatlq1812 explicitly scoped it out as deserving its own dedicated session, tracked as the next candidate in `docs/ROADMAP.md`. |
| 2026-08-01 | Root cleanup + skills map. `UPGRADE_PLAN_20260729.md` (all 7 items done) and `request.md` archived to `docs/archive/upgrade-plan-2026-07-29/` — no longer active, `CLAUDE.md`/`README.md`/`docs/MASTER_CONTEXT.md` updated to stop pointing at them as repo-root files. The archived plan's still-relevant "scout 2-4 representative repos before running `skill-creator` on a new skill type" working rule was extracted into `CLAUDE.md`'s pipeline section as a standing rule before archiving, so it survives past this one execution round. Built repo-root `SKILLS_MAP.md` — a Mermaid flowchart of all 62 skills by cluster (pipeline/meta, foundation, light design, legal, and the 6 audience tiers) plus real cross-skill dependency edges, generated from `registry/skills.json` + documented `SKILL.md` chains (not invented), linted clean with `mermaid-diagram-designer`'s `lint_mermaid.py`. Also corrected a real documentation error found during this cleanup: the row below (2026-07-27 cont'd 2) claims a "Dev Agent Response" section was appended to `request.md` — direct inspection found no such section in the file; flagged in `docs/archive/README.md`, not silently left. |
| 2026-07-29 | Full-session round, committed as `71d5ad1` and pushed to `origin/master`. (1) thatlq1812 rescoped stage 4 (quality-eval): no longer a blanket policy, exempt for foundation/general-capability skills, required only for expert-elicited niche specializers or skills ingesting uncontrolled external input — implemented as `quality_score.stage4_required`/`stage4_reason` on every registry entry. (2) Executed all 7 items of `UPGRADE_PLAN_20260729.md` (rewritten from a strategy narrative into a checklist first): registry grew 39→62 skills — Personal Profile/Workspace tooling, 17 Role Capability Layer skills across 6 sub-clusters (built via parallel background agents, independently re-verified by this session), a 4-skill Light Design cluster grounded in 2 real reference projects, `skill-creator` v0.4.0's Document Distillation Mode, `skill-exporter` v0.2.x's Knowledge Deployment Engine upgrade (`--exclude`, `skills.lock`, `verify_lock.py`), and a Non-Tech User Guide. (3) thatlq1812 review of the finished registry surfaced real issues, each fixed for real: `socratic-concept-helper` PAUSED (a same-day `deep-research` round confirmed public research doesn't substitute for the real student/teacher survey `STRATEGY_SPEC.md`'s own brief recommended, plus an operator-mismatch objection — a K12 student doesn't self-operate an agent harness); Student(K12)/University Student tiers merged; a domain-tag inconsistency across 7 skills fixed; `knowledge-gap-analyzer`/`upskilling-roadmap-builder`'s overlap resolved by composition (`from_gap_analysis.py`), not deletion; `personal-profile-manager` wired for real into `legal-form-filler`; and a bundle-composability guidance gap (a consuming agent under-using chained skills) fixed in `skill-exporter`'s generated `MANIFEST.md` plus a new standing convention in `CLAUDE.md`. All 62 skills pass `validate_skill.py`. Full narrative detail lives inline throughout this file's per-skill/per-cluster entries below and in `docs/DECISIONS_PENDING.md`'s resolved items 4-10. |
| 2026-07-27 (cont'd 6) | thatlq1812 asked directly whether vnnd30 mode's core engine should be Typst instead of XeLaTeX, given the praxis_csc precedent from the prior round. Recommended a split (vnnd30 mode → Typst for portability; book/report scaffold mode stays XeLaTeX, grounded in a real production LaTeX project that already assumes a full TeX distribution) rather than switching everything — thatlq1812 agreed and directed implementing it, plus documenting the split explicitly ("ghi rõ cho các option của skill đó luôn"). Built `typst-bootstrap` (new dependency skill, detect + download-a-static-binary into a shared `.tools/typst/` location, gitignored like `.venv/`) and installed Typst 0.15.1 for real. Ported `vnnd30.sty` to `vnnd30.typ` (Typst) and rewrote `render_nd30_document.py` to emit/compile Typst instead of XeLaTeX — content JSON schema unchanged, `export_nd30_docx.py` unchanged (already Pandoc/Markdown-based). All 5 `doc_kieu` re-verified real (compiled to PDF via `typst compile`, visually inspected). 2 real bugs found and fixed during the port: a `REPO_ROOT` path off-by-one in `typst-bootstrap`, and Typst parsing a literal `"- "` line-start as a bullet-list marker instead of a literal dash. Added a new "Engine per mode" section to `latex-project-bootstrap/SKILL.md` making the XeLaTeX-vs-Typst split explicit per thatlq1812's request. |
| 2026-07-27 (cont'd 5) | thatlq1812 pointed to a sibling project, `D:/elix/praxis_csc`, as a portability reference for document tooling, describing it as having "tích hợp thành công xelatex." Direct code inspection (`backend/praxis_csc/grader/render.py`, `docs/CHANGELOG.md`) found the opposite of the described claim: praxis_csc deliberately REPLACED xelatex/pdflatex/MiKTeX/TeX Live with a bundled Typst binary (~52MB static, vs ~5GB for an offline LaTeX distro) specifically to stay portable/self-contained across any target machine — flagged this correction to thatlq1812 rather than silently building on the wrong premise. Separately, found genuinely reusable value: praxis_csc's real, working fix for Pandoc's DOCX export defaulting to the "Aptos" font + stock blue/teal heading colors (`resources/templates/reference-vn.docx`, patched via `theme1.xml` font + `styles.xml` heading `w:color`) — this exact recipe (not the file) was adapted into `latex-project-bootstrap` v0.3.0's new `reference-vn-nd30.docx`. thatlq1812 also directed finishing the previously-flagged `latex-project-bootstrap` gaps in the same round: fixed a real modeling error (v0.2.0 wrongly gave "văn bản hành chính cá biệt" types a with-year số/ký hiệu convention that only applies to normative documents like Nghị định), added `nghi_quyet`/`bien_ban` doc_kieu, the "TM. <tập thể>" collective-signature pattern, configurable margins, and a full DOCX export path — see that skill's own STATUS row for full verification detail. |
| 2026-07-27 (cont'd 4) | thatlq1812 directed acting on the 3 fronts opened in the prior round. (1) Recorded the confirmed `dangkykinhdoanh.gov.vn` access failure (TLS cert error + headless-browser timeout) as a running log entry in `docs/ROADMAP.md`, alongside the existing `dichvucong.gov.vn` entry, so a later session doesn't re-propose `vn-business-registry-check` without re-testing access first. (2) Built (not just documented) `latex-project-bootstrap` v0.2.0's real NĐ 30/2020 administrative-document rendering capability — see its own STATUS row below for full detail; real government PDF read, real `.sty`/render script written, real `xelatex` compiles verified, 2 real bugs found and fixed. (3) `contract-batch-tabular-review`: thatlq1812 confirmed no real diligence schema exists to elicit from, and explicitly authorized proceeding with a generic/common schema (the "verbatim quote + location + 3-state not-found" pattern already found in the legal-repo comparison round) rather than blocking on it — built as `contract-risk-log` v0.2.0's `batch_tabular_review.py` (see that skill's own STATUS row). |
| 2026-07-27 (cont'd 2) | thatlq1812 brought in `request.md`, a roadmap proposal from another agent (cross-referencing `references/` — Harvey AI/CoCounsel/Ironclad-inspired playbooks). Reviewed critically rather than executed wholesale: flagged `dossier-completeness-checker` as a redundant rename of `legal-form-filler`'s existing `check_dossier.py`; flagged `legal-dossier-template-selector` as repeating the "form suggestion" gap `legal-form-filler` already declared out of scope for lack of real government checklist data; flagged the item-7 mapping (NĐ 30/2020 + vision-agent document generation) as answering a different question than what the survey actually asked (checking an existing contract's format, already partly covered by `contract-consistency-linter`, not generating new administrative documents). Flagged `legal-web-search` as originally proposed (scrape thuvienphapluat.vn/luatvietnam.vn to assert hiệu lực status) as the "legal-lookup chatbot" `STRATEGY_SPEC.md` §2 explicitly rules out — thatlq1812 agreed and reframed it as a rules-based search protocol instead (built, see below). Accepted and built: `xelatex-bootstrap`, `pandoc-bootstrap` (real infra gaps, not previously covered), `legal-web-search` (reframed). `legal-glossary-translator` idea noted as already an existing backlog item (`translator-en-vi` extension), not new. A "Dev Agent Response" section was appended to `request.md` itself, addressed to the proposing agent. |
| 2026-07-27 (cont'd) | Same-day follow-up: thatlq1812 directed 2 more items — fold the "git handler" idea into `scout-harvester` (v0.3.0, `clone_candidate.py`) instead of a separate skill, and wire `legal-form-filler` into `document-ai-structurer`'s catalog (`legal-form-filler` v0.2.0's `checklist_from_catalog.py`, `document-ai-structurer` v0.3.1 propagating Khoản lists into `catalog.json`). Verified end-to-end: catalog → citation-existence check → checklist extraction → dossier check, all 3 downstream skills chained successfully against a synthetic corpus. Explicitly NOT running stage 4 (`quality-eval`) per thatlq1812's standing instruction. |
| 2026-07-27 | Full session: `document-ai-structurer` bumped to v0.3.0 (v0.2.0 added legal-article structuring mode for Điều/Khoản/Điểm, agent-reviewed boundaries not a hardcoded rule; v0.3.0 same day added `build_catalog.py`, a multi-document catalog wired into `legal-citation-checker` v0.2.0's new `--catalog` Điều-existence check — not a hiệu lực check). 2 new foundation-tier skills (`domain: general`, not legal-specific despite being motivated by the Legal cluster): `pii-masking` (v0.1.0 — generalizes `grading-and-feedback`/`peer-review`'s local-mapping precedent to arbitrary sensitive terms) and `deep-research` (v0.1.0 — a protocol + grounding validator the calling agent executes with its own backend, explicitly not Scriptorium holding an AI key; reuses `legal-research-brief`'s validator shape). thatlq1812 also discussed and explicitly deprioritized/reshaped 3 other candidate ideas this session: web-search skipped (can't compete with harness-native search), provider-key-handler skipped entirely, git-handler folded into `scout-harvester` conceptually (not yet built). See `important.md`'s discussion history and `docs/ROADMAP.md` v1.8.0-1.10.0 for full design rationale on all of it. |
| 2026-07-26 | 31 skills written. All have passed `security-audit` (all `passed`), none have a `quality_score` yet (stage 4 hasn't run on any skill) — so no skill is officially "ready to use" yet, even though several have already seen real use this session. Harvesting the "general tier" from K-Dense-AI/scientific-agent-skills (MIT) is complete: `citation-management`, `literature-review`, `exploratory-data-analysis`, `hypothesis-generation`, `peer-review` all done, then all 5 **hardened to v0.2.0** after an adversarial re-test found 17 real defects in their scripts. Registry schema gained 2 new tag axes (`grounding` mandatory, `object_type` optional — `registry/SCHEMA.md`), all prior entries retrofitted. Audience-tier progress: `study-plan-builder` (Student/K12) done. **Teacher tier substantially staffed**: `lesson-plan-builder` (built directly this session) plus 6 more built via parallel sub-agents and reviewed/re-tested by the orchestrating session — `assessment-builder`, `grading-and-feedback`, `competency-rubric-builder`, `classroom-materials-builder`, `grade-book-builder`, `parent-communication` — consolidating ~40 fragmented prior system folders down to 7 practical skills, deliberately excluding prior system's bureaucratic-paperwork cluster per thatlq1812 direction. Only `teacher_self_eval` (TT20/2018) remains unbuilt on the Teacher-tier shortlist, lowest priority. thatlq1812 then directed jumping straight to the **Legal specializer network** (skipping University Student/Lecturer tiers for now, per explicit sequencing this session) — surveyed the legal candidate cluster (`outside_research/`), consolidated 9 raw survey items into 5 skills + 1 extension of `translator-en-vi`, flagged 2 real data gaps (no free Vietnamese legal-document API, no real government checklist/template data) rather than building around them with invented data. **All 5 legal-cluster skills now done**, built sequentially (thatlq1812 requested one-at-a-time this round, not parallel dispatch): `contract-consistency-linter`, `legal-research-brief`, `legal-citation-checker` (deliberately scoped to format-only), `contract-risk-log`, `legal-form-filler` (deliberately scoped to a generic engine, no hardcoded procedure data). `translator-en-vi` + legal glossary extension remains as the last item in the legal-cluster shortlist. See `docs/ROADMAP.md` for the running expansion backlog. Everything below is verified directly against `registry/skills.json` + `skills/`. |

## Existing skills

> [!NOTE]
> **Two separate verification levels — do not conflate them.**
> - **Script-level verification** (implementation tested): every skill below has been run against real inputs AND deliberately adversarial inputs, with real bugs found and fixed during testing. Security audit has passed for all 62 skills (`security_audit: passed`). Content is grounded in authoritative public sources (official curriculum standards, peer-reviewed literature, harvested MIT-licensed skills, real deployed systems) — except where a known open gap is explicitly flagged in the skill's own row.
> - **Formal stage 4 Quality Evaluation** (multi-harness scoring): applies only to skills where `registry/skills.json`'s `quality_score.stage4_required` is `true` (thatlq1812 decision, 2026-07-29) — niche-specializer skills elicited from a real expert source, or skills that ingest uncontrolled external input. **20 of 70 skills** carry `stage4_required: true` (see table below); the rest are exempt by design, not deferred. Running stage 4 remains deliberately not yet scheduled for any of them — not because any skill failed, but because the instrument itself was still being hardened when the deferral started (2026-07-26). Do not read `quality_score.stage4_formal: null` as "this skill doesn't work" — it means the formal QA scoring run hasn't been scheduled yet. Ask before running it. Per-skill rows below still say "stage 4 on hold" from before this scoping decision — read that phrase against the classification table immediately below, not literally; the registry is authoritative on this axis.

### Stage 4 applicability (thatlq1812 decision, 2026-07-29)

| Bucket | skill_id | Why |
| --- | --- | --- |
| Expert-elicited (Legal specializer) | `contract-consistency-linter`, `legal-research-brief`, `legal-citation-checker`, `contract-risk-log`, `legal-form-filler`, `legal-web-search` | Elicited from a real junior-lawyer survey / practitioner source, not public-source grounding alone. |
| Uncontrolled external input | `document-ai-structurer`, `browser-web-renderer`, `scout-harvester`, `license-compliance-check`, `citation-management`, `literature-review`, `image-generator-gemini`, `deep-research`, `video-generator-gemini`, `audio-generator-gemini`, `poster-generator`, `media-pipeline-orchestrator` | Core function ingests arbitrary external documents, rendered web pages, third-party repos, or external API responses (incl. Gemini image/video/audio/layout responses to arbitrary caller prompts) — the injection/hallucination-risk surface stage 4's adversarial-input Pass A targets. |
| Elevated risk (extended criterion) | `socratic-concept-helper`, `clinical-note-structure-validator` | Not expert-elicited or externally-fed in the strict sense, but flagged real-stakes: `socratic-concept-helper` per `docs/specs/STRATEGY_SPEC.md`'s own research naming it the highest-risk Student-tier item, built ahead of the recommended real survey (Item 3 section below, `docs/DECISIONS_PENDING.md`); `clinical-note-structure-validator` (2026-08-15) per its own `risk_tier: N4` — clinical/mental-health content, grounded via reverse-engineered public sources rather than real practitioner elicitation, flagged for real practitioner review before production use as a hard precondition in its own SKILL.md. |
| **Exempt** (`stage4_required: false`) | all other 50 skills (pipeline/infra, `office-doc-creator`, `mermaid-diagram-designer`, `translator-en-vi`, `latex-project-bootstrap`, `pii-masking`, `personal-profile-manager`, `project-workspace-initializer`, `svg-poster-builder`, `brand-identity-linter`, `slogan-copy-linter`, `light-logo-arranger`, `exploratory-data-analysis`, `hypothesis-generation`, `peer-review`, all Teacher/Student-tier skills, `python-env-bootstrap`, `xelatex-bootstrap`, `pandoc-bootstrap`, `typst-bootstrap`, `quality-eval`, `security-audit`, `dedup-novelty-check`, `skill-creator`, `skill-exporter`, `ffmpeg-bootstrap`, `media-anchor-profile`, `video-assembly-composer`) | Foundation/infrastructure or general-capability skills grounded in public sources, structured caller-supplied input only — no expert-elicitation requirement, no uncontrolled external ingestion. `video-assembly-composer` in particular makes zero AI/LLM calls at all (pure deterministic ffmpeg assembly), a stronger exemption case than the others in this bucket. |

This is Claude's application of thatlq1812's rule to the existing registry, not a separately thatlq1812-confirmed skill-by-skill list — flag any row that looks miscategorized. Classification lives in the registry itself (`quality_score.stage4_required` / `stage4_reason` per entry), this table is a summary view.

| skill_id | version | risk_tier | quality_score | security_audit | Operational status |
| --- | --- | --- | --- | --- | --- |
| `skill-creator` | 0.4.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable** — added 2 gold templates (standalone/dependency) + `scripts/scaffold_skill.py`, adapted from prior system's `_templates/` pattern, smoke-tested (both templates scaffold correctly, invalid names rejected). v0.3.1 (2026-07-27) added `scripts/validate_skill.py`, a mechanical 6-field-spec + metadata validator adapted from `anthropics/skills`' `quick_validate.py` pattern (no code copied) — found and fixed 19 real pre-existing `compatibility`-field spec violations across the full registry when run retroactively. **v0.4.0 (2026-07-29, `UPGRADE_PLAN_20260729.md` Item 5)** added Document Distillation Mode: `scripts/scaffold_distillation.py` (deterministic `chapters/`+`glossary.md`+`patterns.md`+`cheatsheet.md`+master-`SKILL.md` scaffolding) + `scripts/validate_distillation.py` (chapter-naming, leftover-placeholder, dangling-`Ch NN`-cross-reference, and approximate token-budget checks — `word_count*1.3` heuristic, explicitly not exact). Directory shape adapted from the already-cloned `outside_research/references/book-to-skill` (MIT, license-compliance-checked 2026-07-27) — scaffolding/validation automated, chapter-content authoring stays agent-executed (requires real reading comprehension, this skill never invents domain content). Verified real: scaffolded a 2-chapter distillation, filled it with real content distilled from `registry/SCHEMA.md` (not placeholder text), validated clean (`~42 tokens` approx SKILL.md body, correctly small since detail lives in chapters); separately verified an unfilled-placeholder refusal, a dangling `Ch 09` cross-reference correctly demoted to a non-blocking warning, an oversized SKILL.md body (~13k tokens) correctly refused, a missing `glossary.md` correctly refused, an empty `chapters/` dir correctly refused (exit 2), and the scaffold's own overwrite-protection. Full registry re-verified 62/62 passing `validate_skill.py`. Doesn't apply stage 4 by design. |
| `skill-exporter` | 0.3.1 | N1 | `null` | `passed` | **Script-verified; stage 4 not applicable** — the distribution half of the pipeline (2026-07-27, thatlq1812-directed): everything else in Scriptorium creates/audits/catalogs skills, nothing packaged them for someone outside the repo until now. `list_candidates.py` filters `registry/skills.json` by domain/task_type/object_type; `export_bundle.py` packages requested skills into a `.zip`. Both hard-exclude any skill with `license_debt` set, `security_audit.status != "passed"`, or `operational_status.state == "paused"` — no override flag, giving `registry/SCHEMA.md`'s license-debt/pause distribution rules their first real enforcement point. thatlq1812 explicitly declined a `quality_score` caveat in the output ("tôi kiểm soát được nó") — the field is shown factually, not moralized about. **v0.3.0 (2026-08-05)**: added `--all` (export every skill passing the gate, non-exportable ones silently skipped rather than refusing the whole export) and `START_HERE.md` (an ordered first-run checklist: install skills, bootstrap a shared venv if needed, run `project-workspace-initializer`'s workspace init if bundled, then clean up), written into every bundle. Built to replace a real habit thatlq1812 reported — manually zipping the repo's `skills/`+`registry/` folders wholesale, bypassing every gate this skill enforces. Verified real: `--all` against the live 63-skill registry produced a correct 62-skill bundle (skipped the real paused `socratic-concept-helper`, reason printed); `--all --exclude domain:legal` correctly narrowed to 56; `--all` combined with an explicit skill_id, and neither given, both correctly refused (exit 2); `START_HERE.md` correctly rendered all 4 steps (with correct renumbering) for a bundle needing both venv-bootstrap and workspace-init, and only the 2 unconditional steps for a dependency-free single-skill export; `verify_lock.py` (unchanged) correctly verified a real `--all`-exported lock file. **v0.3.1 (2026-08-05)**: wording pass on `START_HERE.md` (matching a root-level static companion file for thatlq1812's manual zip habit) — venv step now reads "(required)" when shown, cleanup step now names `START_HERE.md` and the `.zip` archive itself explicitly instead of "this file". Verified real: `office-doc-creator` re-export shows both changes; `mermaid-diagram-designer` regression confirms the venv step still correctly doesn't render without a dependency. **v0.2.4 (2026-08-05)**: added a "Python environment" section to generated `MANIFEST.md` (when the bundle includes `python-env-bootstrap` or lists non-skill package deps) telling the recipient to bootstrap ONE shared venv at their workspace root and always invoke through it — real friction reported from a sibling project re-linking skills/venv per small project instead of once per workspace. Matching "workspace root, not per-project" language added to the existing "Where to install" section. Verified real: exporting `office-doc-creator` (pulls in `python-env-bootstrap` + lists `openpyxl`/`python-docx`/`python-pptx`/`uv`) produced the new section verbatim; a regression export of a dependency-free skill correctly omitted it. **v0.2.3 (2026-08-05)**: added a "Where to install" section to generated `MANIFEST.md` listing known per-harness skill directory conventions (Claude Code `.claude/skills/`, goose `.agents/skills/`, Cursor `.cursor/skills/`) — verified real against an exported manifest. **v0.2.2 (2026-07-29)**: thatlq1812 reported real friend-machine testing where the consuming agent under-used a bundle's skills, applying a generic "minimize tool calls" instinct against a real intended pipeline. Added a "Note for the agent using this bundle" section to every generated `MANIFEST.md`, explicitly telling the consuming agent that chained/dependent skills are meant to be used together — verified real in an actual exported manifest, regression-checked against a single-skill export. Deliberately scoped to what Scriptorium controls (the manifest it hands the consuming agent) — cannot and does not edit a downstream user's own `AGENTS.md`/system prompt. See `docs/DECISIONS_PENDING.md` resolved item 10. **v0.2.1 (2026-07-29)**: added the `operational_status` hard-exclusion check to both scripts, verified real against `socratic-concept-helper`'s real paused entry (not synthetic data) — both `list_candidates.py` and `export_bundle.py` correctly excluded/refused it with the full pause reason. **v0.2.0 (2026-07-29, `UPGRADE_PLAN_20260729.md` Item 6)** reframed per a thatlq1812-provided discussion transcript from a "zip utility" to a Knowledge Deployment Engine: added `--exclude` (repeatable, exact `skill_id` or `axis:value` tag pattern e.g. `domain:education`) to `export_bundle.py` — refuses the whole export rather than silently shipping a bundle with a broken dependency chain if an exclusion removes a still-needed dependency; added `dependency-tree.md` (why each skill is in the bundle) and `skills.lock` (`skill_id` -> `{version, content_sha256}`) to every export; added `scripts/verify_lock.py` (recomputes hashes against a target `skills/` dir, reports drift). Verified real: `list_candidates.py` against the real 62-skill registry; `export_bundle.py`'s pre-existing dependency-chain/overwrite/unknown-id behavior regression-checked unchanged; `--exclude python-env-bootstrap` on a 3-skill chain correctly refused (named the broken-dependency conflict), excluding the next level up (`document-ai-structurer`) too still correctly refused (conflict now against `legal-citation-checker`) — confirms multi-level refusal chaining, not just one hop; `--exclude domain:legal` on a 2-skill export correctly dropped only the tag-matching one; `verify_lock.py` correctly matched a fresh lock against the live registry, correctly caught a deliberately tampered `SKILL.md` (named the exact hash mismatch), correctly reported all-missing against an empty target dir, and correctly refused malformed lock JSON (exit 2). |
| `document-ai-structurer` | 0.3.1 | N1 | `null` | `passed` (re-audited at 0.3.1) | **Script-verified; stage 4 on hold** — v0.2.0 added a legal-article structuring mode for Vietnamese Điều/Khoản/Điểm documents: `suggest_legal_boundaries.py` proposes candidate boundaries by regex (explicitly labeled unverified), `apply_legal_structure.py` mechanically builds sections/index.json from a `structure.json` the reviewing agent has already checked against the source. v0.3.0 added `build_catalog.py`: aggregates multiple legal-article-mode outputs into one `catalog.json`, parsing an integer Điều number per section, matching documents by an optional caller-declared `doc_meta.json` (`van_ban_so_hieu`/`van_ban_ten`, never invented — an undeclared folder is still included, flagged unmatched). Wired into `legal-citation-checker` v0.2.0's `--catalog` flag (Điều-existence, not hiệu lực). **v0.3.1 (same session)** made `build_catalog.py` also propagate each Điều's Khoản list into `catalog.json`, now consumed by `legal-form-filler` v0.2.0's new `checklist_from_catalog.py` — closes the "`legal-form-filler` remains unwired" gap from the prior row: a checklist can now be mechanically extracted from a real Điều instead of hand-typed with no source (still doesn't decide which Điều/document is the right one for a procedure — that domain judgment stays out of scope). Verified real against a synthetic 2-document corpus end-to-end (catalog → citation existence check → checklist extraction → dossier check, all 3 downstream skills chained successfully). 2 real bugs found and fixed earlier in the session: `slugify()` stripped Vietnamese diacritics instead of transliterating; Windows console `UnicodeEncodeError` printing Vietnamese to stdout/stderr — both fixed in all affected scripts. **Not yet verified against real Docling output from an actual scanned Vietnamese legal PDF** — open gap, flagged in the skill's own limitations. Original default mode (non-legal documents) unchanged, still only real-tested on 1 English PDF. Hasn't passed stage 4. |
| `python-env-bootstrap` | 0.2.0 | N1 | `null` | `passed` (accepted risk, noted) | **Script-verified; stage 4 on hold** — verified working correctly on Windows/PowerShell. Security-audit flagged a blind-trust pattern (`curl\|sh`/`irm\|iex` from astral.sh) as acceptable risk (reputable, declared source), hasn't passed stage 4. |
| `browser-web-renderer` | 0.1.0 | N2 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — proposed by another agent via `request.md` (2026-07-27) to close `legal-web-search`'s `dichvucong.gov.vn` JS-rendering gap; scouted first (`scout-harvester` confirmed `playwright`/Apache-2.0, 14.8k stars, actively maintained), thatlq1812 approved a 2-step plan (static code, then explicit approval for the real ~300MB install + real test), both executed same session. Real install verified: `playwright` 1.61.0 + Chromium 149.0.7827.55 installed into the shared venv via `uv`, `check_browser.py` correctly detects both. `render_and_extract.py` verified real against 2 sites: `vanban.chinhphu.vn` rendered cleanly (2865 chars, richer than a plain `WebFetch` got earlier the same session — found a real attached PDF link `30.signed.pdf` that WebFetch's summary missed); `dichvucong.gov.vn` tested on 2 URLs (a procedure page + the bare homepage), **both WAF-blocked** ("Request Rejected", a bot-detection response, not a rendering failure). This is a materially different and harder finding than the JS-rendering hypothesis the skill was built to fix — deliberately does NOT attempt WAF evasion (see SKILL.md's "What this skill does NOT do"), so `dichvucong.gov.vn`/Item 4 remains genuinely unsolved by this skill, not just "not yet built out." Hasn't passed stage 4. |
| `xelatex-bootstrap` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — accepted from another agent's `request.md` proposal (2026-07-27), reviewed and confirmed as a real gap: `latex-project-bootstrap` assumes XeLaTeX/biber already installed, doesn't install them itself (same relationship `python-env-bootstrap` has to Python). `check_toolchain.py` (detect-only, safe) verified real on this machine: correctly detected an already-installed MiKTeX 25.12 (xelatex, biber, fontspec.sty, real version strings), and correctly reports a missing binary (verified directly against a nonexistent binary name). `install_toolchain.ps1`/`.sh` (winget/brew/apt) written but **not executed for real this session** — no toolchain-less machine available to test against, flagged as an open verification gap. Hasn't passed stage 4. |
| `pandoc-bootstrap` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — accepted from the same `request.md` proposal. `check_pandoc.py` verified real: detected an already-installed pandoc 3.8.1 (real version string), correctly reports missing when simulated. Distinct from `office-doc-creator` (python-docx/pptx/openpyxl, no pandoc involved). `install_pandoc.ps1`/`.sh` written but **not executed for real this session** (pandoc already present on the only available test machine). Hasn't passed stage 4. |
| `license-compliance-check` | 0.2.0 | N2 | `null` | `passed` | **Script-verified; stage 4 on hold** — verified running for real against anthropics/skills, security-audit clean, hasn't passed stage 4. |
| `quality-eval` | 0.2.0 | N2 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — v0.2.0 added Pass A (contract conformance under adversarial input) ahead of the existing baseline-delta comparison, plus 3 supporting files (adversarial-input catalog, fixture generator, report template). Still **not applied to any real skill — running stage 4 stays deliberately deferred by thatlq1812 decision (2026-07-26)**; this round only builds the instrument. The fixture generator was verified for real: 21 fixtures generated, every one run through the hardened `exploratory-data-analysis` with zero tracebacks, the known-quartile fixture confirming q1=3.25/q3=7.75/1 outlier, and no raw-value leakage from the embedded-instruction fixture. It also found a residual defect in this session's own EDA fix (a `1e400` literal overflows to infinity on parse and crashed the same statistics call the token-based `inf` guard had just fixed) — fixed and re-verified. |
| `security-audit` | 0.1.0 | N2 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — already applied for real to the 5 skills above (self-audit), hasn't passed stage 4. |
| `scout-harvester` | 0.3.0 | N1 | `null` | `passed` (re-audited at 0.3.0) | **Script-verified; stage 4 on hold** — distilled from 3 real runs (Docling, uv, anthropics/skills). `scripts/github_scout.py` + `scripts/pypi_license_check.py`, tested for real (reproduced prior manual findings for mermaid-js/mermaid + 3 PyPI packages). v0.3.0 (2026-07-27, thatlq1812-directed) folds a candidate "git handler" skill idea into this one instead of a separate skill — added `clone_candidate.py`, a shallow `git clone --depth 1` wrapper (subprocess, fixed argument list, no shell=True, same safety shape as the existing `gh` wrapper) that refuses to clone into a destination inside the Scriptorium repo tree itself (would nest a second `.git`). Verified real: an actual clone of `octocat/Hello-World` succeeded, plus 3 refusal paths (invalid slug, destination inside this repo, destination already non-empty). Hasn't passed stage 4. |
| `office-doc-creator` | 0.5.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — real smoke test across all 3 formats (docx/xlsx/pptx), verified by reading content back including correct Vietnamese diacritics. v0.5.0 (2026-08-14): fixed a real incident (raw Markdown asterisks leaking into .docx/.pdf output) by adding Markdown-emphasis parsing to paragraph/table-cell/list-item text — verified by reading run.bold/run.italic back from a generated .docx. Uses python-docx/python-pptx/openpyxl (MIT, verified directly). Hasn't passed stage 4. |
| `dedup-novelty-check` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — real overlap-scoring script (stdlib, no dependency), tested both a flagged case and a safe case against the real registry. Hasn't passed stage 4. |
| `mermaid-diagram-designer` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — 2 reference files + lint script (stdlib), tested both a valid and an invalid diagram. Hasn't passed stage 4. |
| `translator-en-vi` | 0.2.0 | N2 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — elicited from thatlq1812 (no fixed glossary, flexible register, for general translation), 2 reference files. v0.2.0 (2026-07-27, thatlq1812-directed) completed the last item on the Legal-cluster shortlist: `references/legal-glossary.json` (10 starting EN-VI contract-term pairs, e.g. Indemnification → Bồi hoàn, Force Majeure → Bất khả kháng — explicitly not an official standard, extensible) + `scripts/check_terminology_consistency.py`, which validates a caller-declared usage log against it (same consistency-check shape as `legal-citation-checker`'s title check / `contract-consistency-linter`'s party-label check, applied to terminology). Considered and declined renaming the skill to a bare `translator` in the same session — `registry/SCHEMA.md` declares `skill_id` immutable, and the skill's real capability (EN-VI contrastive linguistics) doesn't generalize to other language pairs without real work, so a bare name would misrepresent scope. Verified real: bundled template passes clean; an unregistered translation, the same term translated 2 different ways across entries, an unknown `term_id` (typo), empty entries, and malformed JSON all correctly caught. Hasn't passed stage 4. |
| `latex-project-bootstrap` | 0.4.0 | N1 | `null` | `passed` (re-audited at 0.4.0) | **Script-verified; stage 4 on hold** — grounded in thatlq1812's real LaTeX project. Real 4-pass build smoke test (xelatex→biber→xelatex→xelatex), 5-page PDF with correctly-rendered Vietnamese diacritics (book/report scaffold mode, unchanged, still XeLaTeX). **v0.2.0-0.3.0** built vnnd30 mode (real NĐ 30/2020 admin-document rendering, 5 `doc_kieu`, DOCX export via a patched Pandoc reference-doc) — see prior entries archived in git history for full detail. **v0.4.0 (2026-07-27)** — thatlq1812-directed architecture change after comparing `D:/elix/praxis_csc`'s own `docs/CHANGELOG.md`, which records that project independently switching its document-export PDF engine from XeLaTeX/pdflatex/MiKTeX to a bundled Typst binary for portability (~50MB static binary vs ~5GB TeX distribution): the SAME switch was applied here, but ONLY to vnnd30 mode — the book/report scaffold mode deliberately stays on XeLaTeX (grounded in a real production LaTeX project that assumes a full TeX distribution already; switching would gain nothing and break consistency with that real system). New dependency skill `typst-bootstrap` (v0.1.0, see its own row) installed and verified for real (Typst 0.15.1). `assets/vnnd30.sty` (LaTeX) replaced by `assets/vnnd30.typ` (Typst) — same font/size/style values, ported using Typst's own primitives (`grid()` for 2-column layout instead of LaTeX minipages). `render_nd30_document.py` rewritten to emit `.typ` + compile via a single `typst compile` pass (vs XeLaTeX's 2-pass loop) — content JSON schema (`validate()`) UNCHANGED. `export_nd30_docx.py` UNCHANGED (already Markdown-mediated via Pandoc, never depended on the LaTeX engine). Verified real: all 5 example content JSONs re-rendered + compiled via Typst, PDFs visually inspected page-by-page, functionally identical to the retired LaTeX version (plus correct page-1-suppressed/page-2-shown numbering, resolved in one pass — no LaTeX-style paragraph-glue bugs to work around either, since Typst's block layout doesn't need explicit `\par`). 1 real bug found and fixed during the port: a literal `"- "` at the start of a markup line is parsed by Typst as a bullet-list marker (renders `•`) — fixed by building those lines via code-mode string concatenation instead of markup. `--margin-*` and all 9 deliberately-broken-content-JSON refusal paths re-verified unaffected by the engine swap. A real Typst-special-character stress test (`&%#_*` + literal quotes/backslash) compiled cleanly, zero markup injection. `SKILL.md` gained a new "Engine per mode" section documenting explicitly which mode uses which engine and why, per thatlq1812's explicit request to make this clear. Not yet built: Công điện/Giấy mời/Giấy giới thiệu/Giấy nghỉ phép (Mẫu 1.6-1.10, lower priority). Hasn't passed stage 4. |
| `typst-bootstrap` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — new dependency skill (2026-07-27), same relationship to `latex-project-bootstrap`'s vnnd30 mode that `xelatex-bootstrap` has to its book/report scaffold mode, except "install" here means downloading a single static binary (no system package manager) — the entire reason Typst was chosen. `check_typst.py` (detect-only) checks the shared `.tools/typst/` location first (mirrors `python-env-bootstrap`'s shared `.venv/` pattern), then PATH. `install_typst.py` downloads the official release from `github.com/typst/typst` for the current platform/arch. Verified real end-to-end: `check_typst.py` correctly reported MISSING before install; `install_typst.py` downloaded `typst-x86_64-pc-windows-msvc.zip`, extracted `typst.exe` into `.tools/typst/`, confirmed via `typst --version` → `typst 0.15.1 (9dfd3a08)`; re-running the install script correctly short-circuited ("already present"). A real path-computation bug (`parents[2]` instead of `parents[3]`, one directory too shallow) was found and fixed before the install was attempted. macOS/Linux asset paths written by analogy to the same release's naming convention, not independently tested. Hasn't passed stage 4. |
| `image-generator-gemini` | 0.3.0 | N2 | `null` | `passed` | **Script-verified; stage 4 on hold** — expanded into a full designer toolkit (thatlq1812 request): added batch auto-anchor, vision-analysis (image→text style), PDF-page-extraction (no AI needed). All 4 capabilities verified for REAL via actual API calls/renders (thatlq1812-authorized test key, lightweight model). Grounded in a full survey of `D:/elix/platform/scripts/gen/` (9 scripts) + `D:/UNI/S9_SP26/MLN131/project`. Hasn't passed stage 4. |
| `citation-management` | 0.2.0 | N1 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — first harvested skill (from K-Dense-AI/scientific-agent-skills, MIT). DOI/PMID/arXiv → LaTeX-safe BibTeX via free CrossRef/PubMed/arXiv APIs, no AI backend. v0.2.0 fixed the skill's worst defect: an unresolvable PMID produced a placeholder entry and exit 0. Verified real at 0.2.0: AlphaFold DOI (URL form), a PMID, an old-style arXiv ID (`cond-mat/0102536`, published DOI auto-picked up), 3 non-resolving identifiers each failing loudly with nothing written, plus offline escaping/collision tests. Hasn't passed stage 4. |
| `literature-review` | 0.2.0 | N1 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — second harvested skill (from K-Dense-AI/scientific-agent-skills, MIT). Keyword search across arXiv/PubMed/CrossRef with cross-source merging, PRISMA-lite screening counts, year filtering, optional abstracts; dropped the paid parallel-web CLI, mandatory AI-figure-generation, and biology-specific databases from the original. Depends on `citation-management` for verification. Verified real at 0.2.0: 3-source search for "CRISPR sickle cell" (5/source → 15 raw, 15 distinct, abstracts fetched from all 3), plus offline fixtures proving an arXiv record and its CrossRef record now merge into one row. Hasn't passed stage 4. |
| `exploratory-data-analysis` | 0.2.0 | N1 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — third harvested skill (from K-Dense-AI/scientific-agent-skills, MIT). Bounded local CSV/TSV/JSON profiler (schema/missingness/IQR-outlier stats), stdlib-only, zero network calls, path-confined to `--root`. Scoped down to the original's "automated core" tier only (dropped optional NumPy/h5py/Biopython/Pillow/tifffile inspectors + 6 biology/chemistry reference docs as specialized-tier). v0.2.0 corrected the quartile/IQR math driving the outlier flag and turned 4 crash paths into clean refusals. Verified real at 0.2.0: quartiles checked against `statistics.quantiles`, real outlier caught, JSON missingness now counted correctly, and 8 refusal/robustness cases (outside-root, nonexistent, unsupported extension, empty file, existing output, unknown encoding, non-UTF-8 CSV, 1200-deep JSON). Symlink rejection still not empirically exercisable in this sandbox (no admin rights). Hasn't passed stage 4. |
| `hypothesis-generation` | 0.2.0 | N2 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — fourth harvested skill (from K-Dense-AI/scientific-agent-skills, MIT). Turns an observation into rival candidate hypotheses + discriminating predictions + a preregistration scaffold. 3 scripts (schema validator, causal-claim linter, preregistration generator), stdlib-only, zero network. Scoped down from the original's 7-script suite. v0.2.0 closed the central hole: the preregistration generator ran on any record, so "status can never be confirmed" was only enforced by an optional validator — it is now a hard gate with no bypass. Verified real at 0.2.0: broken record refused by both validator and generator, self-contrast and uncovered-candidate rules each caught, linter severity/hedge/quote handling verified. N2 reflects subject-matter risk, not a tooling finding. Hasn't passed stage 4. |
| `peer-review` | 0.2.0 | N2 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — fifth and final general-tier harvested skill this round (from K-Dense-AI/scientific-agent-skills, MIT). Mandatory intake gate (authorization/confidentiality/accountability) blocks all manuscript inspection until `READY_FOR_LOCAL_REVIEW`; claim-evidence matrix validator; private review scaffold with strict Comments-to-Authors/Confidential-Comments-to-Editor channel separation; tone/placeholder/channel-leak/decision-overreach linter. 4 scripts, stdlib-only, zero network. Scoped down from the original's 7-script suite. v0.2.0 closed a gate weakness where the bundled template with only its 3 booleans flipped reached `READY_FOR_LOCAL_REVIEW` with every text field still a placeholder. Verified real at 0.2.0: that exact intake now BLOCKED (8 reasons) and refused by the generator, a filled intake passes, linter catches all 15 scaffold placeholders, no false positives on "garbage collection"/"lazy evaluation"/lowercase headings, real personal attack and channel leak caught, clean draft passes. N2 reflects subject-matter risk, not a tooling finding. Hasn't passed stage 4. |
| `study-plan-builder` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — first skill for the Student (K12) audience tier (`docs/specs/STRATEGY_SPEC.md` §5.1). Deterministic day-by-day study/review schedule from a topic list: round-robin interleaved new-topic sessions (no back-to-back repeats) + periodic review cycling through everything introduced so far; refuses loudly (never silently drops a topic) when capacity is insufficient. Self-authored, stdlib-only, zero network — no real student survey exists yet, so scope was deliberately kept to pure scheduling (never touches subject content or answers questions) per thatlq1812 direction. Verified real: 14-day/4-topic schedule correct, exact-fit case produced exactly the right new/review split, insufficient-capacity/invalid-review-every/duplicate-name/bad-weight all correctly refused (exit 2) — a real crash bug (extra capacity beyond topic needs) was found and fixed during this testing. Hasn't passed stage 4. |
| `lesson-plan-builder` | 0.2.0 | N1 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — first skill for the Teacher audience tier (`docs/specs/STRATEGY_SPEC.md` §5.1). Validates a K-12 lesson plan (KHBD) against Phụ lục IV CV 5512's mandatory structure (4 fixed-order activities, each with 4 sub-parts incl. the 4-step "tổ chức thực hiện") plus the fixed CT GDPT 2018 competency vocabulary (5 phẩm chất, 3 năng lực chung) and level-adaptive assessment rule (TT27 tiểu học = no scores, TT22 THCS/THPT = scores allowed); renders clean Markdown. Grounded in prior system's real deployed `lesson_plan` skill for domain knowledge only (not its orchestration machinery), used loosely per thatlq1812's explicit "not gospel" caveat. thatlq1812 directed this tier to skip prior system's bureaucratic-paperwork skills entirely (Nghị định 30/2020 admin templates), keeping scope to skills serving actual teaching. v0.2.0 (2026-08-13) adapted 3 patterns from a second real reference source (`outside_research/references/anthropic-k12-teacher-skills`'s `k12-lesson-planning`, Apache-2.0): clarify-priority-order guidance, an explicit copyright/originality guardrail, and a scaled-down presenting-results UX note — see the dated entry above for the real bug this surfaced and fixed (`is_placeholder()`). Verified real: a valid THCS plan passed clean and rendered correctly; a Tiểu học plan with scoring language correctly errored citing TT27; a deliberately broken plan caught real errors (wrong activity order, invalid phẩm chất/năng lực vocabulary, missing steps) + cross-reference warnings; malformed/missing-field/time-deviation cases all handled correctly; the new placeholder-leak check correctly blocks unedited template text while not false-positiving on real math content using `<`/`>` as inequality symbols. Hasn't passed stage 4. |
| `assessment-builder` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — consolidates 6 near-duplicate prior system exam skills (`exam_builder`, `review_exam`, `sample_exam`, `question_bank`, `review_outline`, `tn_thpt_review`) into one matrix balancer + validator, built via a parallel sub-agent, reviewed and re-tested by the orchestrating session. `build_exam_matrix.py` apportions question counts and points across topics × 4 cognitive levels (Nhận biết/Thông hiểu/Vận dụng/Vận dụng cao, default 40/30/20/10) via largest-remainder so totals always sum exactly; `validate_exam.py` checks a drafted MCQ+essay exam against that plan (per-level/per-cell counts, MCQ 4-choice/1-answer invariant, grading-guide sums, total points) and refuses loudly on any mismatch. No LLM/AI call — question content always supplied externally; DOCX rendering delegates to `office-doc-creator`. Verified real (by both the building agent and the orchestrating session independently): 8 balance combinations all summed exactly; a real bug (zero-question levels getting nonzero points, an impossible allocation) found and fixed during testing; 8 deliberately-broken exams each caught with the exact reason. Hasn't passed stage 4. |
| `grading-and-feedback` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 on hold** — consolidates 3 prior system skills (`grading`, `primary_remarks`, `report_card_remarks`) into the Teacher tier's scoring/feedback skill, built via a parallel sub-agent, reviewed by the orchestrating session. Deterministic MCQ answer-key matching + per-criterion rubric aggregation (essay scores arrive pre-assigned by a human/upstream process — never LLM-graded), a criterion score exceeding its max is hard-refused, never clamped. Level-adaptive remark linter reuses `lesson-plan-builder`'s proven TH-vs-THCS/THPT scoring-language logic. Local-only HSnn roster-anonymization round-trip (real name ↔ code mapping never leaves the local file, per Vietnam's "Quy chế AI Điều 6" student-PII rule). 3 scripts, stdlib-only, zero network. N2 reflects subject-matter risk (student PII/scores), not a tooling finding. Verified real: a mixed MCQ/essay class graded correctly; an over-max essay score correctly refused with no output written; a real regex bug (numeric-grade pattern missing its leading "đ") found and fixed during testing; anonymize→grade→reattach round-tripped correctly at 3- and 105-student scale. Hasn't passed stage 4. |
| `competency-rubric-builder` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — sixth prior system-consolidation skill for the Teacher tier, built via a parallel sub-agent, reviewed and re-tested by the orchestrating session. Validates a competency/product/activity rubric (tiêu chí × mức độ table, 3 or 4 levels): every criterion needs a non-empty observable-behavior description for every level, criterion weights must sum exactly to the declared total scale, identical description text reused across two levels of the same criterion is flagged (warning, not error); renders clean Markdown. Grounded in prior system's real `competency_rubric` skill for domain knowledge only. Verified real: valid 3-level and 4-level rubrics both passed clean and rendered correctly; a weight-sum mismatch refused with exact numbers; a missing description cell refused by exact criterion/level; malformed/non-object JSON, too-few levels, duplicate criteria, non-numeric weights, and render-overwrite protection all handled correctly. No bugs found during testing. Hasn't passed stage 4. |
| `classroom-materials-builder` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — consolidates 3 prior system folders (`classroom_worksheet`/`homework_sheet`/`learning_game`) into one validator for student-facing classroom materials, distinct from `lesson-plan-builder`'s internal KHBD, built via a parallel sub-agent, reviewed by the orchestrating session. Deliberately modest scope — these 3 prior system skills are lighter on hard regulatory structure than lesson-plan-builder/assessment-builder, so this only checks real, mechanically-checkable invariants (every item has a task; every homework item's answer key covers every item with no gaps; worksheet items missing a response space warn; game rules must exist and reference the declared topic), not manufactured rigor. Verified real: valid worksheet/homework/game all pass and render correctly with intact Vietnamese diacritics; a homework item with a missing answer refused by exact item name; a game with no rules refused; 13 total real test cases. No bugs found during testing. Hasn't passed stage 4. |
| `grade-book-builder` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 on hold** — aggregates already-scored assessments into a class-wide term gradebook, built via a parallel sub-agent, reviewed by the orchestrating session. Two-stage weighted average per student (average within each declared category, then weight across categories against the actual declared total) + classification against caller-declared bands + class statistics. Deliberately hardcodes NO official TT22/2021 weight percentages or classification thresholds — prior system's own source flags its 1/2/3 weight example as a non-mandatory reference default, so `category_weights`/`classification_bands` are required inputs with no built-in defaults, per `docs/specs/STRATEGY_SPEC.md` §7 principle 5 (never fabricate regulatory citations). Verified real: a 3-student/3-category case hand-verified correct (7.75 → 7.8); weight-sum mismatch, unknown/missing/unused category, out-of-range score, misordered bands, and duplicate student id all correctly refused with field-level detail. Hasn't passed stage 4. |
| `parent-communication` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 on hold** — consolidates 2 prior system folders (`parent_letter`, `parent_brief`) into one completeness+tone validator, built via a parallel sub-agent, reviewed by the orchestrating session. Checks required fields, refuses leftover template placeholders, hard-blocks the real prior system-identified failure mode of a single-student letter sent with the student-name placeholder unreplaced, requires a concrete requested action when one is stated, warns (non-blocking) on harsh/accusatory language about the student. N2 reflects subject-matter risk (a wrong/unfilled name reaching a real parent). Verified real: a complete letter passed clean; an unfilled `<student_name>` placeholder correctly refused; harsh language correctly warned without blocking; one real bug (a success message that falsely claimed "no warnings" while warnings were printed above it) found and fixed during testing. Hasn't passed stage 4. |
| `contract-consistency-linter` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 on hold** — first skill for the Legal specializer network (`docs/specs/STRATEGY_SPEC.md` §5.2). Mechanical proofreading lint for a Vietnamese contract: Điều (article) numbering must be sequential with no gaps/duplicates, every inline "Điều N" reference must resolve to a real heading (dangling cross-reference detection), and only declared party labels (e.g. "Bên A"/"Bên B") may appear (catches leftover template text like a stray "Bên C"). Elicited from a real junior-lawyer survey (`outside_research/research_01_survey.md` items 7-8) naming exactly these checks. Chosen to build first among the legal-cluster candidates because it needs no external legal database or thatlq1812-supplied checklist data — unlike `legal-citation-checker` (no free Vietnamese legal-document API exists) or `legal-form-filler` (no real government checklist/template data available yet), both flagged as open gaps. Self-authored, stdlib-only, zero network. Verified real: a clean 3-article contract passed with zero flags; a deliberately broken contract (out-of-sequence numbering, a duplicate article, 2 dangling references, an undeclared "Bên C") caught all 5 issues by exact line number; missing file, malformed config, and a config missing the required field all correctly refused (exit 2). Hasn't passed stage 4. |
| `legal-research-brief` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 on hold** — second Legal-tier skill. Validates that a structured legal brief (câu hỏi chính → sự kiện đã xác minh → thiếu sót/giả định → căn cứ pháp lý → phân tích áp dụng → quan điểm trái chiều → đánh giá rủi ro, schema elicited from an AI research report on real Vietnamese legal-opinion practice) grounds every claim in the 3 factual/legal sections in a real, caller-declared source — a citation to a nonexistent source id is a hard error. Elicited directly from survey items 1/10's explicit note "Dựa theo nguồn 100%, không tự generate thông tin mới," same discipline already applied by `citation-management` to academic citations. Does no research/web search itself — pure grounding validator, stdlib-only, zero network. Verified real: a valid 2-source brief passed clean and rendered correctly with source labels inlined; a deliberately broken brief (a fabricated source id, a missing citation on a grounded claim, a missing required field) caught all 3 errors; duplicate source id, empty sources list, and malformed brief JSON all correctly refused (exit 2). Hasn't passed stage 4. |
| `legal-citation-checker` | 0.3.0 | N2 | `null` | `passed` (re-audited at 0.3.0) | v0.3.0 (2026-07-27) added an optional `--web-record` flag consuming an already-validated `legal-web-search` record — when a citation's `van_ban_so_hieu` matches a result's `document_ref`, prints a disclosed live-source hint (what the source displayed, dated, sourced), plus explicit per-citation `access_mode` reporting (no-source/user-supplied/live-source), adapted from a 3-state pattern found independently in 3 outside LegalTech repos surveyed this session. Verified real against `legal-web-search`'s actual `vanban.chinhphu.vn` record for Nghị định 30/2020/NĐ-CP, plus a synthetic js_shell-hint case, a malformed web-record refusal, and a combined `--catalog`+`--web-record` run (all 3 access modes on one citation); running with neither flag regression-checked unchanged. **Script-verified; stage 4 on hold** — third Legal-tier skill, deliberately scoped down from the survey's ask. Validates Vietnamese legal-citation *format* (Điều/Khoản/Điểm structure, văn bản số hiệu against known numbering conventions) and flags the same document cited under two different titles. Explicitly does NOT verify hiệu lực (in-effect) status — no free Vietnamese legal-document API exists for this project to query, and the source survey item itself flags auto-tracking amendments/repeals as hard; every run prints a stderr scope-limit reminder so this can't be mistaken for a full validity check. v0.2.0 added an optional `--catalog` flag (thatlq1812-directed, 2026-07-27): given `document-ai-structurer`'s `build_catalog.py` output, also checks that a cited Điều number actually **exists** in the local structured corpus for documents the catalog covers — explicitly documented as an existence check, not a hiệu lực claim (a real but repealed Điều still "exists" in an out-of-date corpus). Self-authored, stdlib-only, zero network. Verified real: the bundled template initially FAILED validation — a real bug (the regex rejected mixed-case agency abbreviations like "GDTrH") found and fixed during testing, then passed clean; a deliberately broken set (negative Điều, invalid Khoản/Điểm, an unrecognized số hiệu format, a title mismatch for the same document) caught all 5 issues; malformed/empty-list cases correctly refused (exit 2). `--catalog` mode verified real against a synthetic 2-document corpus: a citation to a real Điều passed, a citation to a nonexistent Điều number was correctly flagged, a citation to a document the catalog doesn't cover was correctly left unflagged (no false positive), malformed/missing catalog file correctly refused, and running without `--catalog` still behaves exactly as v0.1.0 (regression-checked). Hasn't passed stage 4. |
| `contract-risk-log` | 0.2.0 | N3 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — fourth Legal-tier skill. Validates a structured contract risk-review log: every issue needs a clause, concern, category, severity, and a specific (not vague) recommended action; an empty issues list requires an explicit `no_issues_found_after_review: true` flag, since a silently-empty risk log is ambiguous between "reviewed, clean" and "not reviewed." Never auto-detects risk itself — mirrors `peer-review`'s claim-evidence-matrix pattern (validates the structure of a human/agent-authored assessment, not a substitute for legal judgment). N3 reflects subject-matter risk. Self-authored, stdlib-only, zero network. Verified real: the bundled template initially warned on an internal inconsistency in its own example (fixed, re-verified clean); ambiguous empty-issues, contradictory no-issues-flag, invalid severity, and duplicate id all correctly refused; a vague recommended action ("review this") correctly warned without blocking; an explicit "reviewed, found nothing" case passed; malformed JSON refused (exit 2). **v0.2.0 (2026-07-27)** added `scripts/batch_tabular_review.py`: validates N documents x M caller-declared review columns in one pass (a due-diligence batch, not just one contract's issue list) — every document x column cell must have an explicit state (`value` with `verbatim_quote`+`location`, `not_present`, or `needs_review` with a reason), a missing cell is refused rather than silently treated as blank. thatlq1812 confirmed no real thatlq1812-supplied diligence schema exists and explicitly authorized a generic schema instead of blocking — adapted from a 3-state tabular-review pattern found independently in `claude-for-legal`/`lq-skills` during this session's legal-repo comparison (schema shape only, no code/content copied). Verified real: the bundled 2-document x 3-column template passed clean and rendered a correct Markdown table; 7 deliberately broken cases (missing cell, duplicate cell, missing verbatim_quote, missing needs_review note, duplicate document, cell referencing an undeclared column, malformed JSON) each caught with the exact expected reason. Hasn't passed stage 4. |
| `legal-form-filler` | 0.2.0 | N2 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 on hold** — fifth Legal-tier skill. Two generic engines, checklist/template always caller-supplied, never hard-coded (no real government procedure data available): `check_dossier.py` checks provided documents against a required checklist (case/whitespace-normalized exact match, deliberately not fuzzy); `fill_form.py` validates every required field is filled and flags data keys that don't match a declared field (catches typos). Explicitly does NOT decide which checklist/form applies to a procedure (survey item 4's "form suggestion") — that domain judgment has no real data to ground it in, so it's out of scope, not guessed. v0.2.0 (2026-07-27, thatlq1812-directed) added `checklist_from_catalog.py`: given a `document-ai-structurer` catalog.json + văn bản số hiệu + Điều number, mechanically extracts that Điều's Khoản list as `required_documents` — grounds the checklist in a real document instead of it always being hand-typed. Deliberately still doesn't judge whether a Khoản list is actually a document checklist (an Điều can enumerate obligations, not requirements) — every run prints a reminder to verify this first. Self-authored, stdlib-only, zero network. Verified real: complete/incomplete dossier cases both correct including case-insensitive matching; a missing required form field correctly refused; a typo'd data key ("ngay_sin" vs "ngay_sinh") correctly flagged as unknown AND correctly left the real field reported unfilled. `checklist_from_catalog.py` verified end-to-end: extraction from a real synthetic Điều fed straight into `check_dossier.py`, which correctly flagged 1 of 2 items missing; all 4 refusal paths correct (Điều with no Khoản, unknown số hiệu, unknown Điều number, malformed catalog). **2026-07-27 (same day, no version bump — doc-only)**: a real attempt was made to close the form-suggestion gap via `legal-web-search` against `dichvucong.gov.vn` — real WebFetch calls against 2 page templates returned only a JS-rendered shell, a direct CSDL download link returned HTTP 503. Gap confirmed still genuinely open in practice, not solved by the `legal-web-search` architecture alone; recorded in SKILL.md rather than left silently stale. Hasn't passed stage 4. |
| `legal-web-search` | 0.2.2 | N3 | `null` | `passed` (re-audited at 0.2.0) | v0.2.2 (2026-07-27, doc-only): `legal-citation-checker` v0.3.0 added a `--web-record` flag that consumes a record already validated by this skill's own `validate_search_record.py` — no code change here, `validate_search_record.py`'s contract is unchanged. **Script-verified; stage 4 on hold** — sixth Legal-tier skill, built after reviewing and reframing another agent's `request.md` proposal (2026-07-27). Original proposal (scrape thuvienphapluat.vn/luatvietnam.vn to assert hiệu lực status) rejected as functionally the "legal-lookup chatbot" `STRATEGY_SPEC.md` §2 rules out, and as the same fabrication risk `legal-citation-checker`/`document-ai-structurer` already declined without a real official API. Reframed per thatlq1812 clarification as a rules-based search **protocol** (not a lookup service, no network call in the skill itself) mimicking how a lawyer searches — same shape as `deep-research`: the calling agent searches with its own web tools, this skill supplies a domain allowlist (`references/allowed_domains.json`, 4 government-tier + 2 commercial-tier domains) and `validate_search_record.py`, which hard-requires every result on an allowlisted domain, dated, any hiệu lực-like status recorded as "what the source page displayed" (a structured object, never a bare string claim), and a disagreement between 2 sources on the same document surfaced in `contradiction_notes`, not silently dropped. Verified real (fixtures): bundled template passes clean; a deliberately broken record caught all 4 errors; the same contradiction correctly passed once explicitly noted; malformed JSON refused. **v0.1.1 (same day) — real end-to-end verification round using the calling agent's actual WebSearch/WebFetch tools**, per an agreed 3-step plan with the proposing agent (request.md): searched + fetched `vanban.chinhphu.vn` for real (Nghị định 30/2020/NĐ-CP — got real title/document number/issue date/signatory), built a real record (`assets/verified_real_example.json`), validated it — VALID. This round also found 2 real access failures, documented rather than hidden: `thuvienphapluat.vn` returns HTTP 403 on direct fetch; `dichvucong.gov.vn` procedure/form pages are JS-rendered and unreachable via WebFetch (2 page templates + 1 direct CSDL download link, all 3 failed). Net effect: the proposing agent's claim that this skill fully resolves survey Item 4 (form suggestion) does NOT hold in practice — `dichvucong.gov.vn` is exactly the site that use case depends on, and it isn't reachable with the tools available this session. `legal-form-filler/SKILL.md` updated to record this real attempt rather than silently leave its scope note stale. **v0.2.0 (same day, thatlq1812 decision after the v0.1.1 findings)**: short-term mitigation — a result may set `js_shell_detected: true` + a non-empty `snippet_note` to record WebSearch-summary-level content instead of a literal page quote, when a direct fetch only returns a JS shell. Verified real: a record built from an actual WebSearch summary of `dichvucong.gov.vn` business-registration procedures, correctly flagged, passes (`assets/verified_real_example_js_shell.json`); the same record missing `snippet_note` correctly refused. This is a **disclosed degradation, not a fix** — long-term browser-automation is tracked as a backlog item in the skill's own SKILL.md, not built. Survey coverage explicitly held at **85.5%** per thatlq1812 decision, Item 4 (form suggestion) keeps an Open Gap flag — not marked resolved. **v0.2.1 (same day, doc-only)**: `browser-web-renderer` (see its own row) was built and tested for real against `dichvucong.gov.vn` — result was a WAF block, not a JS-rendering success, confirming the gap is harder than the browser-automation backlog item assumed. Updated here to reflect the real finding rather than leave browser automation listed as an untested hoped-for fix. Hasn't passed stage 4. |

| `pii-masking` | 0.1.0 | N2 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — foundation-tier skill (`domain: general`, not legal-specific), built after thatlq1812 explicitly requested a "mask sensitive info" skill for the Legal cluster. Generalizes 2 existing local-only-mapping precedents already in this repo (`grading-and-feedback/anonymize_roster.py`, `peer-review`'s confidentiality gate) to arbitrary free text — the gap was already flagged in `docs/ROADMAP.md` as deliberately deferred until actually needed. 2-step-review shape matching `document-ai-structurer`'s legal-article mode: `scan_sensitive_patterns.py` proposes candidate structured identifiers (CCCD/CMND-shaped digit runs, VN phone numbers, emails, keyword-flagged digit runs), explicitly does NOT detect personal names/addresses (no reliable regex — must be declared explicitly by the caller); `mask_terms.py` mechanically masks an approved term list, exit 1 (not silent success) if any declared term had zero matches; `unmask.py` reverses it via a local-only `map.json`. Verified real against a synthetic fixture (`assets/sample_document.md`, fake CCCD/phone/email/names — no real client data available or needed): 6/6 candidates correctly found, full mask→unmask round-trip byte-identical, all refusal paths correct (overwrite protection, duplicate/empty terms, zero-match term flagged with exit 1, unmask finding no codes refused), a substring-term edge case (`"Văn A"` inside `"Nguyễn Văn A"`) handled correctly (longest-term-first replacement, no corruption). 2 real bugs found and fixed during testing: Windows text-mode write silently converting `\n`→`\r\n` broke mask/unmask byte-for-byte round-trip fidelity (fixed with explicit `newline="\n"`); a naive substring-overlap replacement order would have corrupted a longer term if a shorter overlapping term were replaced first (fixed by sorting replacement order by term length, descending, while keeping caller-order code numbering). Not yet wired into any Legal-cluster skill's workflow — masking is a manual step for now. Hasn't passed stage 4. |
| `deep-research` | 0.1.0 | N2 | `null` | `passed` (self-audit) | **Script-verified; stage 4 on hold** — foundation-tier skill (`domain: general`), built after thatlq1812 clarified (during discussion) that a "deep research" capability must NOT mean Scriptorium holding its own AI provider key — the calling agent does the actual research with its own tools/backend, this skill supplies decomposition/grounding discipline plus a citation validator. Directly reuses `legal-research-brief`'s proven grounding-validator shape (fabricated/mistyped citation = hard error), generalized past the legal domain: `question`/`sub_questions`/`sources`/`findings` schema, plus a new check `legal-research-brief` doesn't have — every inline `[S..]` citation marker used in the free-text `synthesis` field must also resolve to a real declared source, not just citations inside structured `findings`. Deliberately no scaffold generator (only a validator) — the v0.2.0 hardening round found an ungated generator was the exact failure mode in `hypothesis-generation`/`peer-review`, so this skill doesn't repeat it. Verified real: bundled template passes clean and renders; a deliberately broken brief (fabricated finding-level citation, empty-cites finding, invalid confidence value, missing confidence_rationale, zero synthesis citation markers) caught all 5 errors; a citation marker fabricated only inline in `synthesis` (not in any finding) caught separately; malformed JSON and render-without-`--force` both correctly refused. Not yet exercised as part of a real end-to-end research task by an agent. Hasn't passed stage 4. |

| `personal-profile-manager` | 0.1.1 | N2 | `null` | `passed` (self-audit) | **Script-verified; stage 4 not applicable** (`stage4_required: false` — foundation/general-capability, no expert elicitation, no uncontrolled external input) — first skill of `UPGRADE_PLAN_20260729.md` Item 1, built 2026-07-29. Local-only personal profile store + generic auto-fill engine (mirrors `legal-form-filler`'s "caller always supplies the specifics" shape) plus a separate writing-style feedback-to-proposal pipeline that never auto-applies to system instructions. 4 scripts, stdlib-only (json/argparse/pathlib/collections), zero network. Verified real: `init_profile.py` scaffold + overwrite-protection + `--force` all correct; `validate_profile.py` — bundled template valid, missing-section and empty-required-field cases each correctly refused naming the exact section/field, malformed JSON refused (exit 2); `autofill.py` — bundled field_map resolved 8/8 fields against the bundled profile (Vietnamese diacritics round-tripped correctly), a typo'd path correctly reported unresolved without blocking the other 7, a path pointing at a nested object (not a leaf) correctly treated as unresolved rather than serialized; `propose_style_update.py` — bundled feedback log produced a correctly category-grouped Markdown proposal with an explicit non-auto-apply banner, a malformed entry and an empty entries list both correctly refused (exit 2). `.gitignore` gained `/personal` (privacy-by-default). **v0.1.1 (2026-07-29)**: wired for real into `legal-form-filler` — `autofill.py`'s output already matches `fill_form.py`'s `form_data.json` shape exactly, zero conversion needed; verified real end-to-end (extra unmatched fields correctly reported, not an error; required fields correctly filled). See `docs/DECISIONS_PENDING.md` resolved item 9. Doesn't apply stage 4 by design, not deferred. |

| `project-workspace-initializer` | 0.2.1 | N1 | `null` | `passed` (re-audited at 0.2.0) | **Script-verified; stage 4 not applicable** — Item 2 of `UPGRADE_PLAN_20260729.md`, built 2026-07-29. **v0.2.1 (2026-08-05)**: `WORKSPACE.md` gained a fixed "Shared resources (once per workspace, never per project)" section (skills install location + Python venv), matching `skill-exporter`'s new manifest section — real friction reported from a sibling project re-linking a fresh `.agents/skills` junction into every new small project instead of once per workspace. Verified real: regenerated `WORKSPACE.md` contains the section verbatim, rest of the file unaffected. Two-layer deterministic scaffolder driven by a caller-supplied profession `template.json`: `init_workspace.py` (workspace-level, once per workspace) creates the workspace root's own subdirs + a local template copy under `projects/_templates/<template_id>/` + a `WORKSPACE.md` control panel; `scaffold_workspace.py` (project-level, once per matter) creates `project_YYYYMMDD_NN/` + subdirs + a generated `PROJECT.md` control panel. Only one real template exists (`legal-practitioner`), grounded in a real pilot recorded in `docs/ROADMAP.md` (the pilot directory itself is no longer accessible on this machine, so the recorded description is the elicitation source). **v0.2.0 (2026-08-05, thatlq1812-directed)**: built `init_workspace.py` for real, correcting a stale registry `security_audit.note` that had referenced a nonexistent `init_root_workspace.py` — confirmed via `git log --all` that file never existed in this repo before this fix; also reconciled a pre-existing version mismatch (registry said 0.2.0, `SKILL.md` said 0.1.0) that predates this session. 2 scripts, stdlib-only (added `shutil`). Verified real: `init_workspace.py` against the real `legal-practitioner` template created `projects/`+`projects/_templates/`, copied the template, rendered `WORKSPACE.md` with all 3 real `workspace_md_prompts`; `scaffold_workspace.py` chained immediately after against the local template copy, producing a correct `project_20260805_01/`; re-running `init_workspace.py` without `--force` correctly refused (exit 1, `WORKSPACE.md` already exists), with `--force` correctly re-initialized; a template missing `workspace_md_prompts` correctly refused (exit 2). Prior verification retained: first/second same-day scaffold correctly produced `_01`/`_02` with no collision; two scaffolds against an explicit historical `--date` correctly sequenced independently of today's date; a template missing `project_md_prompts` and a prompt entry missing `text` were each correctly refused (exit 2); nonexistent template path refused (exit 2). Doesn't apply stage 4 by design (see Stage 4 applicability table above). |

| `svg-poster-builder` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 not applicable** — Item 4 of `UPGRADE_PLAN_20260729.md` (Light Design cluster), built 2026-07-29. Deterministic SVG layout renderer (composition only, never real imagery) at real ISO 216 sizes (A1/A4). Zone taxonomy (hero_image/content_scene/vignette/decorative_element/background_canvas/typography_frame) grounded in `D:/Document/May052026/scripts/component_types.md`, a real production A1-poster pipeline's own documented schema. 1 script, stdlib-only. Verified real: bundled 6-zone template rendered to well-formed SVG (confirmed via `xml.etree.ElementTree.parse`) with correct A4 `viewBox`; an overflowing zone, a duplicate zone id, a `typography_frame` missing its required `text_label`, and an unknown canvas preset were each correctly refused (exit 1) naming the exact violation. |
| `brand-identity-linter` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 not applicable** — Item 4 companion skill, built 2026-07-29. 3 mechanical checks traced to a real signboard/menu revision session (`D:/elix/temp_project_20260728/brand-data.json` + its real client-feedback log): primary/secondary/accent color-role completeness with valid hex, CTA/contact text emphasis (font size >= body size), and orphan-icon detection (every icon element must reference a declared motif). Fixture is a generic anonymized restaurant example, not the real client's business data. 1 script, stdlib-only. Verified real: bundled template valid; an under-sized CTA, an orphan icon, an icon referencing an undeclared motif, a missing `accent` color role, and an invalid hex value were each correctly refused naming the exact element/role at fault. |
| `slogan-copy-linter` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 not applicable** — Item 4 companion skill, built 2026-07-29. Length-cap (caller-declared, no built-in default), banned-phrase substring scan, and non-blocking all-caps warning for short marketing copy. 1 script, stdlib-only. Verified real: bundled template valid with no warning; an over-length slogan, a banned-phrase match, and an empty slogan were each correctly refused; an all-caps slogan correctly produced a non-blocking warning (still exit 0). |
| `light-logo-arranger` | 0.1.0 | N1 | `null` | `passed` (self-audit) | **Script-verified; stage 4 not applicable** — Item 4 companion skill, built 2026-07-29. Deterministic named-anchor (7 anchor points) coordinate calculator with exclusion-zone collision refusal, unit-agnostic, no file writes. Anchor-then-exclude concept lightly grounded in `D:/Document/May052026/scripts/component_types.md`'s master/group anchor-chaining idea. 1 script, stdlib-only. Verified real: all 7 anchor points hand-verified correct against the bundled 210x297 fixture; an exclusion-zone overlap, an oversized logo, a margin pushing the logo off-canvas, and an unknown anchor name were each correctly refused. |

### UPGRADE_PLAN_20260729.md Item 3 — Role Capability Layer (17 skills, built 2026-07-29)

Built via 6 parallel background sub-agents, one per role cluster (Student K12 extension, Parent/Guardian, University Student, TA/Graduate, Lecturer/Researcher, Lifelong Learner), each independently re-verified by the orchestrating session before registration — same process as the 2026-07-26 Teacher-tier parallel-dispatch round. Orchestrator verification: `validate_skill.py` passed on all 17; `py_compile` syntax-clean on all 17; a security grep (`subprocess`/`os.system`/`eval(`/`urllib`/`requests`/`socket`/`shell=True`) across every script found zero real hits (one false-positive substring match on `socratic-concept-helper`'s safe `_eval` AST-walker function name, confirmed by inspection — no actual `eval()` call exists); a real independent re-run of a sample script from 2 of the 6 agent batches (`academic-grant-proposal`'s fabricated-line-item detection, `ta-grading-rubric-linter`'s over-max-score refusal) reproduced the agents' claimed behavior exactly. All 17 are general-capability tier (`stage4_required: false`) except one flagged exception below. Each skill's own `SKILL.md` has its own real "Verified"/"Known limitations" sections from the building agent.

- **Student (K12) extension**: `socratic-concept-helper`, `exam-ready-scaffold`, `reading-note-structurer`.
- **Parent/Guardian**: `parent-school-communicator`, `home-study-environment-guide`, `student-progress-tracker`.
- **University Student**: `essay-structure-scaffold`, `academic-source-finder`, `group-work-coordinator`.
- **TA/Graduate**: `ta-grading-rubric-linter`, `lab-practicum-guide`, `dissertation-proposal-scaffold`.
- **Lecturer/Researcher**: `academic-grant-proposal`, `manuscript-journal-formatter` (checked for overlap against the existing 5 general-tier research skills first — none found, both genuinely new).
- **Lifelong Learner**: `upskilling-roadmap-builder` (directly imports `study-plan-builder`'s scheduling core rather than duplicating it — real dependency, recorded in the registry), `certification-exam-prep`, `knowledge-gap-analyzer`. **2026-07-29 update**: thatlq1812 review of the full registry flagged `knowledge-gap-analyzer` and `upskilling-roadmap-builder` as a real overlap (both compute a current-vs-target skill gap). Resolved by composing rather than merging/deleting — `upskilling-roadmap-builder` v0.2.0 added `scripts/from_gap_analysis.py`, converting `knowledge-gap-analyzer`'s confidence-scored diagnostic report directly into a schedulable gap input. Verified real end-to-end: a real 3-skill self-assessment → gap analysis (1 below threshold, 1 never-assessed) → conversion → an actual 10-day roadmap scheduling only the 2 real gaps, correctly excluding the 2 already-held skills. See `docs/DECISIONS_PENDING.md` resolved item 8.

**PAUSED — `socratic-concept-helper` (`risk_tier: N3`, `stage4_required: true`, `operational_status: paused`)**: `docs/specs/STRATEGY_SPEC.md` §5.1's own Student-tier research brief (`outside_research/research_02/research_02_result_01.md` §7) explicitly names this exact candidate as the one needing a real student/teacher survey before `skill-creator` runs ("Yes, strongly" — highest-value AND highest-risk). This build proceeded on direct 2026-07-29 task-dispatch instruction treating it as general-capability tier (Khan Academy Khanmigo's publicly documented Socratic-restraint pattern + cited 2025-2026 research on AI-assisted-homework harm) instead of waiting on that survey. The building agent flagged this explicitly rather than silently resolving it. A same-day follow-up `deep-research` round (`outside_research/research_06_socratic_helper/research_brief.json`, 6 sources, 4 independently fetched/read) confirmed public-source research does not substitute for the survey — Khan Academy's own well-resourced Khanmigo only reaches ~15% regular student usage (adoption/engagement, not just answer-leakage, is the dominant real risk, unaddressed by this skill's current design); current AIED research names teacher empowerment a core design principle, not optional; Vietnam's own 2025-2026 Ministry of Education AI-in-schools rollout is explicitly staged as a measured pilot with teacher capacity a named prerequisite. **thatlq1812 then raised a second, independent structural objection in discussion**: this skill assumes a K12 student directly operates an agent harness in real time — which doesn't match how K12 students actually access AI (chat apps, not agent CLIs), the same operator-mismatch logic already applied to the Teacher tier. **Decision (2026-07-29, thatlq1812): PAUSED**, not deleted — `registry/skills.json` now carries `operational_status: {"state": "paused", ...}` (new field, `registry/SCHEMA.md`), and `skill-exporter` v0.2.1 hard-refuses exporting it (verified real: both `list_candidates.py` and `export_bundle.py` correctly exclude/refuse it, reason printed in full). Full record: `docs/DECISIONS_PENDING.md` resolved item 5. The mechanical restraint-linter (`check_restraint.py`) has one known real gap, found during the agent's own testing: a false positive when a computed arithmetic answer coincidentally matches a number already present in the student's own question text — left as-is since the skill isn't in production use pending the survey/redesign.

**Not built — School Admin/Academic Coordinator sub-tier**: per `UPGRADE_PLAN_20260729.md` §3c, no real school-administrative-workflow source exists (same category of gap Teacher tier already excluded for its own admin-paperwork cluster) — left as an explicitly flagged open gap in `docs/ROADMAP.md`, not fabricated.

### New this session (2026-08-15): Business + Marketing clusters, first skills

| skill_id | version | risk_tier | quality_score | security_audit | Operational status |
| --- | --- | --- | --- | --- | --- |
| `meeting-action-tracker` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** First skill in the new `business` domain (`skills/business/`). Validates a meeting record's structure against 2 real authoritative conventions grounded via `deep-research` (`references/research_09_business_admin_standards/research_brief.json`): Robert's Rules of Order for minutes content, RAID-log/action-log convention for action items. Deliberately does NOT format/generate a minutes document (that's `office-doc-creator`/`latex-project-bootstrap`'s job) — validates a caller-supplied structured record only. Verified real: bundled template passes; a 10-violation deliberately broken record caught all 10 in one run; missing `motions`/`action_items` keys flagged (not silently treated as empty); malformed JSON refused (exit 2). |
| `vn-ad-compliance-checker` | 0.1.0 | N3 | `null` | `passed` | **Script-verified; stage 4 not applicable.** First skill in the new `marketing` domain (`skills/marketing/`). Checks a caller-declared online-ad record against Vietnam's Nghị định 342/2025/NĐ-CP (Điều 17 close-button/wait-time, Điều 18 24h takedown SLA, Điều 19 retention/reporting, Điều 3 special-category router) — grounded by directly reading the decree's own signed PDF page-by-page (`references/research_10_digital_marketing_regulations/research_brief.json`), not just secondary summaries; this caught and corrected a real 1.5s-vs-5s wait-time discrepancy between an early news report and the final effective text. Explicitly does NOT verify special-category (Điều 4-12) substantive content and is NOT legal advice — routes/flags only. A prior scouting round (same session) confirmed no license/dedup conflict: 2 real MIT marketing-skill-pack repos were scouted (`coreyhaines31/marketingskills` verified 44,335 stars, `kostja94/marketing-skills` 172 skills) and neither carries Vietnam-specific regulatory content. Verified real: bundled template passes; an 8-violation deliberately broken record plus a special-category routing case caught all 9 in one run; a leap-year retention edge case (`2028-02-29` + 3 years) correctly computed a Feb-28 fallback; a mixed-timezone takedown pair flagged cleanly instead of crashing; missing `ad` key and malformed JSON both refused (exit 2). |

Both grounded via an expanded `deep-research` pass (official standards/regulations, not blog opinion, per thatlq1812 direction this session) plus, for `vn-ad-compliance-checker`, a real `scout-harvester` + `license-compliance-check` round on the Digital Marketing cluster generally (documented in `docs/ROADMAP.md`'s Business Administrator/Digital Marketing cluster section). Registry grew 72 → 74. `CLAUDE.md`'s domain-folder list updated to include `business`/`marketing`.

| `sop-structure-validator` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** Grounded in `references/research_11_business_admin_sop_budget/research_brief.json` -- a 6-part SOP structure (header/purpose/scope/roles/numbered-steps/revision-history-with-approval) corroborated across 3 English sources, confirmed structurally identical in Vietnamese SOP material. Companion to `meeting-action-tracker`. Verified real: bundled template passes; a 7-violation broken record caught all 7; a header-only record missing roles/steps/revision_history was flagged (3 issues); malformed JSON refused (exit 2). |
| `budget-expense-tracker` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** Grounded in the same research_11 brief -- international expense-report/budget-variance shape PLUS a real Vietnam-specific finding (searched Vietnamese sources directly, per thatlq1812 direction): a `Giấy đề nghị thanh toán` must state the amount in words as well as numerals, and a `công tác phí` claim needs a prerequisite travel-authorization reference plus a tax-code-bearing invoice -- neither international source mentioned this. Verified real: bundled template (2 claims incl. 1 travel, 2 budget lines) passes; a 10-violation broken record caught all 10; a zero-budgeted-amount edge case correctly omitted the variance_pct requirement; empty `expense_claims: []` correctly passed; malformed JSON and a record with neither list refused (exit 2). |
| `utm-campaign-governance-linter` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** Grounded directly in Google's own official GA4 documentation (`references/research_10_digital_marketing_regulations/research_brief.json` findings S4/S5, support.google.com), upgrading research_08's original secondary-sourced UTM finding to primary-source grounding. The companion capability flagged (not yet built) when `vn-ad-compliance-checker` shipped as the stronger initial flagship -- built this round to complete the cluster. Verified real: bundled template (1 tagged external + 1 untagged internal link) passes; a 9-violation broken record (mixed-case/spaced values, playbook mismatch, missing required param, internal link carrying UTM params, non-boolean `is_internal`) caught all 9; empty/missing `links` correctly flagged/refused; omitting `utm_playbook` correctly skipped only the approved-value check; malformed JSON refused (exit 2). |
| `campaign-funnel-brief-validator` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** Grounded in `references/research_12_marketing_funnel_campaign/research_brief.json` -- a real 9-element campaign-brief structure (English sources) plus a funnel-stage-needs-its-own-metric finding reconciled across a 3-stage (English TOFU/MOFU/BOFU) vs. 4-stage (Vietnamese, +loyalty) discrepancy by supporting both rather than hardcoding one. Genuinely novel -- neither scouted marketing-skill-pack repo's topic listing named a brief-completeness checker. Verified real: bundled template (full brief + 3-stage funnel) passes; a 15-violation broken brief (empty required fields, invalid dates, a budget-split sum mismatch, a null KPI target, a non-list `mandatories`, a duplicate funnel stage, an unrecognized stage name) caught all 15 in one run; a near-empty brief was flagged (9 issues); malformed JSON refused (exit 2). |

All 4 grounded via 2 more `deep-research` briefs (`research_11`/`research_12`, both `VALID`), each including a dedicated Vietnamese-language search round per thatlq1812's explicit direction ("search cả tiếng việt nữa nhé"). Registry grew 74 → 78 (6 skills total across the 2 new clusters this session). `business` domain: `meeting-action-tracker`, `sop-structure-validator`, `budget-expense-tracker`. `marketing` domain: `vn-ad-compliance-checker`, `utm-campaign-governance-linter`, `campaign-funnel-brief-validator`.

### New this session (2026-08-15, cont'd): Events/Diplomacy/Academic/Psychology, first skills

| skill_id | version | risk_tier | quality_score | security_audit | Operational status |
| --- | --- | --- | --- | --- | --- |
| `event-compliance-checker` | 0.1.0 | N3 | `null` | `passed` | **Script-verified; stage 4 not applicable.** First skill in the new `events` domain. Grounded in Nghị định 144/2020/NĐ-CP's actual signed PDF, read page-by-page (`references/research_13_events_vn_compliance/research_brief.json`) -- caught and corrected a real 7-vs-10-working-day discrepancy in a secondary source. Checks the decree's real 2-pathway model (notification vs. approval) mechanically. Verified real: bundled template passes; an 8-violation broken record caught all 8; missing `event` key and malformed JSON refused (exit 2). |
| `diplomatic-correspondence-checker` | 0.1.0 | N3 | `null` | `passed` | **Script-verified; stage 4 not applicable.** First skill in the new `diplomacy` domain. Reverse-engineered from the Vienna Convention (1961) + a real US State Department protocol page + note-verbale format references (`references/research_14_diplomacy_protocol/research_brief.json`), per thatlq1812's explicit direction that this substitutes for real elicitation when unavailable. Deliberately FORMAT-only, never diplomatic content. Verified real: bundled template passes; a 7-violation broken record caught all 7; malformed input refused. |
| `survey-adaptation-quality-checker` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** First skill in the `academic` domain grounded in this session's new research (extends the existing citation-management/literature-review cluster). Grounded in peer-reviewed TRAPD/back-translation methodology (`references/research_15_survey_methodology/research_brief.json`). Verified real: bundled template passes; a 6-violation broken record caught all 6; malformed input refused. |
| `clinical-note-structure-validator` | 0.1.0 | **N4** | `null` | `passed` | **Script-verified; stage 4 REQUIRED** (elevated-risk extended criterion, same bucket as `socratic-concept-helper`). First skill in the new `psychology` domain, and the highest-risk skill in this registry. Reverse-engineered SOAP structure from a real deployed product (SimplePractice) + the APA's own official Ethics Code (`references/research_16_clinical_note_structure/research_brief.json`) -- explicitly NOT real practitioner elicitation, flagged honestly. Never reads clinical content, only checks field presence/non-emptiness; SKILL.md states real practitioner review is a hard precondition before any production use. Verified real: bundled template (placeholder text only, no real clinical content used in testing by design) passes; a 5-violation broken record caught all 5; malformed input refused. |

All 4 grounded via 4 more `deep-research` briefs (`research_13`-`16`, all `VALID`). Registry grew 78 → 82 (10 skills total across 4 new clusters/domains this full session). `events`: `event-compliance-checker`. `diplomacy`: `diplomatic-correspondence-checker`. `academic` (new entrant this session): `survey-adaptation-quality-checker`. `psychology`: `clinical-note-structure-validator`.

### New this session (2026-08-15, cont'd): design cluster upgrade + new `webpage` domain

| skill_id | version | risk_tier | quality_score | security_audit | Operational status |
| --- | --- | --- | --- | --- | --- |
| `design-system-recommender` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 not applicable.** Upgrades the `design` cluster (previously validator-only) with a real curated lookup: 192 product types × 16-token color palettes, 74 font pairings, 34 landing patterns, harvested from `nextlevelbuilder/ui-ux-pro-max-skill` (verified 116,839 stars, MIT) with full provenance in its own `references/PROVENANCE.md`. Data integrity verified: 192/192 products↔colors cross-reference match, 3,072/3,072 color-token values pass format validation. Verified real: exact and fuzzy lookups both correct; not-found and missing-args cases correctly refuse; `--list-product-types` prints all 192. |
| `landing-page-composer` | 0.1.0 | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** First skill in the new `webpage` domain. Drafts a real, scrollable 6-section HTML/CSS landing page, reusing `html-poster-composer`'s already-audited Playwright rendering pattern. Chains directly from `design-system-recommender`. Real bug found and fixed via direct visual inspection of the rendered PNG: a declared Google Font wasn't actually loading without a `<link>` tag (silent fallback to generic sans-serif) — fixed, re-verified the heading font visibly changed. Verified real: bundled 6-section template renders correctly end-to-end (.html + .png via the shared venv's real Chromium); a 21-issue deliberately broken record caught all 21, and `compose.py` correctly refused to touch Playwright at all when validation failed (no file written). |

Both grounded in a real, directly-verified 116,839-star repo (`gh api`, not a blog) — the largest, most rigorously-engineered prior-art find of the whole session, scouted after thatlq1812 asked to read it and directed harvesting once confirmed worthwhile. `license-compliance-check` run for real (root LICENSE read directly: clean MIT, no nested override) before harvest; full source commit/provenance/what-was-and-wasn't-adapted documented in `design-system-recommender/references/PROVENANCE.md`. Registry grew 82 → 84. `CLAUDE.md`'s domain-folder list updated to include `webpage`.

### New this session (2026-08-15, cont'd): broader harvest after thatlq1812 pushback

thatlq1812 reviewed the 2-skill harvest above and pushed back directly: "cả cái repo đó bạn trích ra được mỗi 2 skill, và chỉ 1 cái cho webpage nhỉ... cần rộng hơn, không chỉ landing page ấy" (that whole repo and you only extracted 2 skills, only 1 for webpage — needs to be broader, not just landing pages). Went back to the already-cloned repo and inspected the 6 other `.claude/skills/` sub-skills not yet read (`design-system`, `design`, `ui-styling`, `banner-design`, `brand`, `slides` — mostly thin routers to external tooling, except `design-system` which has real slide-generation code+data deliberately not harvested to avoid duplicating `slide-deck-composer`'s different, already-real presentation mechanism) plus 3 more real, substantial, previously-unharvested datasets already sitting in the clone: `styles.csv` (88 rows), `icons.csv` (105 rows, curated from the real Phosphor library), and `ux-guidelines.csv` (119 rows, Web) + `app-interface.csv` (32 rows, iOS/Android/React Native).

| skill_id | version | risk_tier | quality_score | security_audit | Operational status |
| --- | --- | --- | --- | --- | --- |
| `ui-style-guide-lookup` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 not applicable.** Further upgrades `design`: 88 curated UI styles (Glassmorphism, Neumorphism, Brutalism, Bento Grid...) with real CSS implementation detail (colors, effects, implementation checklist, CSS variables, accessibility risk). Data integrity: 88/88 have a non-empty style_id. Verified real: exact + fuzzy lookup, not-found/missing-args refuse correctly, `--list-styles` prints all 88. |
| `ui-icon-recommender` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 not applicable.** 105 curated Phosphor icons with real import code AND correct accessibility-role guidance (decorative/meaningful/interactive). Data integrity: 105/105 have name+import code. Verified real: exact/keyword/category lookup all correct (8 results for Navigation category), 18 real categories listed, not-found/missing-args refuse correctly. |
| `ui-guideline-lookup` | 0.1.0 | N1 | `null` | `passed` | **Script-verified; stage 4 not applicable.** 2nd skill in `webpage` domain — deliberately NOT another page-assembly tool, a UI-review reference merging 151 real Do/Don't guidelines (119 Web + 32 iOS/Android/React Native) filterable by category/platform/severity/keyword. Verified real: combined filters correct (10 Accessibility+Web matches, 7 Critical+mobile matches), 24 real categories / 5 real platforms listed, no-match and no-filter cases refuse correctly. |

All 3 harvested from the same verified source (commit `a38d04c3d5c298c851dbe5e6ee1965ee3de42cb5`), each with its own `references/PROVENANCE.md` pointer back to the canonical record in `design-system-recommender/references/PROVENANCE.md` (updated this round to reflect what got harvested where, and what's still real-but-unharvested: the 1,935-row Google Fonts catalog, `ui-reasoning.csv`'s conditional decision-rule engine, `charts.csv`/`motion.csv`/`react-performance.csv`). `design` domain now 7 skills, `webpage` domain now 2. Registry grew 84 → 87.

**Real cold-agent employee simulation (2026-08-15)**: thatlq1812 directed dispatching a background subagent role-played as a company ops coordinator ("nhân viên"), given 2 realistic tasks (write up a real meeting, check a real TikTok ad before launch) with zero hint either skill existed, told only that this repo contains discoverable tools. Both skills found and run for real, no coaching: `meeting-action-tracker` correctly validated a drafted meeting record (`OK`, 1 motion/2 action items, exit 0); `vn-ad-compliance-checker` correctly flagged the drafted TikTok ad as non-compliant (12s wait time over the 5s cap, no report mechanism, exit 1) and the agent's writeup named the exact 2 fixes needed. One real, minor friction point surfaced and fixed same round: `meeting-action-tracker`'s description didn't make "validator, not drafter" clear until the agent read down to "What this skill does NOT do" — reworded the description's opening sentence (v0.1.0→0.1.1, doc-only). `vn-ad-compliance-checker` had no friction reported. Full agent report available in this session's transcript, not separately filed.

### New this session (2026-08-15, cont'd): real persona probe → SQUARE preset + `announcement-poster-builder`

thatlq1812 posed a direct, concrete test rather than a review of already-shipped work: for a Facebook-fanpage-managing teacher, an intern, or an HR person who needs a square or A4 poster summarizing a decree, a notice, or a contest result — which skill would actually get pulled up, and would it be good enough? ("Case: một người, ví dụ như giáo viên phụ trách fanpage facebook, thực tập sinh, HR nào đó... Tôi nghĩ là chưa có, và nếu có thì chưa ổn.") Investigating `html-poster-composer` directly confirmed the concern: 2 real gaps for this exact persona — no square canvas preset (only A1/A4/VIDEO_HD, despite square being the actual standard Facebook/Instagram feed-post size), and a zone-taxonomy `layout.json` authoring requirement no non-technical caller can be assumed to handle.

| skill_id | version | risk_tier | quality_score | security_audit | Operational status |
| --- | --- | --- | --- | --- | --- |
| `html-poster-composer` | 0.4.0 → **0.5.0** | N1 | `null` | `passed` (re-audited 2026-08-15) | **Script-verified.** Added the `SQUARE` canvas preset (1080×1080, the real Facebook/Instagram feed-post size) — a data-only entry in `CANVAS_PRESETS_MM`, no new code path. Verified real: `285.75mm` converts to exactly `1080px` at the existing fixed 96 CSS-px/inch conversion, confirmed via an actual Playwright render (final-render mode produced an exact 1080×1080 PNG). |
| `announcement-poster-builder` | 0.1.0 (new) | N2 | `null` | `passed` | **Script-verified; stage 4 not applicable.** New sibling skill in `media` — generates a valid `html-poster-composer` `layout.json`+`content.json` for 3 real Vietnamese announcement shapes (`decree_summary`, `announcement`, `contest_result`) from content fields only, no zone-taxonomy/x-y-w-h math required. Chains directly into `html-poster-composer`'s `compose.py`, same "generate valid input for a real render engine" shape as `design-system-recommender` → `landing-page-composer`. Caught and fixed a real bug during its own verification: the `decree_summary`/`announcement` templates' first hand-calculated zone-area budget forgot to include the `kicker` zone's own area, exceeding `html-poster-composer`'s real 40%-max-text-coverage limit (44.0%/43.2% vs. 40.0%) — caught by `compose.py`'s own downstream refusal when actually run (not by eyeballing the percentages), fixed to 35.2%/36.8%, and a permanent runtime self-check added to `build()` to prevent silent recurrence. Verified real: all 3 bundled templates rendered end-to-end through the shared venv's real Playwright/Chromium and visually inspected (correct Vietnamese diacritics, correct square/A4 aspect ratios, no overlap/clipping); empty required fields, an invalid `content_type`/`canvas`, a 1-item `top_results` list (correctly padded with "Chưa công bố"), a 4-item `top_results` list (correctly refused, max 3), and malformed JSON (exit 2) all correctly handled. |

Registry grew 87 → 88. `media` domain now has 2 poster-related skills (`html-poster-composer`, `announcement-poster-builder`) plus the existing seal/batch/video-assembly skills.

Source: `registry/skills.json`. If the numbers here differ from `registry/skills.json`, the registry wins — this file may be stale.

## v0.2.0 hardening round (2026-07-26)

The 5 harvested skills were re-tested adversarially — running each script against inputs it was never shown during authoring (unresolvable identifiers, non-UTF-8 files, duplicate headers, `inf` cells, 1200-level JSON nesting, unfilled templates, lowercase headings, domain vocabulary that collides with the tone lexicon). Every defect below was reproduced first, then fixed, then re-verified. Full per-skill detail lives in each `SKILL.md`'s `metadata.changelog_0_2_0`.

Three were the kind that make a skill worse than having no skill at all — the tool's stated guarantee was false in exactly the case the guarantee exists for:

1. **`citation-management` fabricated citations.** An unresolvable PMID returned `@article{Unknownnd, title={Untitled}, author={Unknown}, pmid={99999999}}` and exit 0. The SKILL.md claimed it "fails loudly instead of inventing plausible-looking metadata," and cites `STRATEGY_SPEC.md` §5 (the *Mata v. Avianca* fabricated-case-law precedent) as the reason. Every resolver now validates the record it receives; an unknown identifier writes nothing and exits 1.
2. **`hypothesis-generation` had an ungated generator.** `generate_preregistration_scaffold.py` ran on any record, so a record with `status: "confirmed"`, a single non-rival candidate, a self-contrasting prediction and no accountable human produced a clean-looking preregistration. The invariant "a hypothesis is never confirmed by construction" was enforced only by a validator nothing required. The generator is now gated on it, no bypass flag — matching how `peer-review` already gated its own generator.
3. **`peer-review`'s mandatory intake gate accepted an unfilled template.** Copying `review_intake_template.json`, flipping the 3 booleans, and changing nothing else returned `READY_FOR_LOCAL_REVIEW` — authorization, accountable human, and retention plan all still template placeholders. Placeholders now count as unfilled, and `ai_tool_policy` (which the SKILL.md required but no tool checked) is enforced.

The rest were correctness and robustness: wrong quartile math driving `exploratory-data-analysis`'s headline outlier flag (index-based, no interpolation); 4 uncaught-traceback paths in that same skill (`inf` cell, non-UTF-8 file, deep JSON, oversized CSV field) in a skill whose entire posture is "every value is untrusted"; silent data loss on duplicate CSV headers; JSON missingness undercounted for any key first appearing after record 1; `literature-review` never merging an arXiv preprint with its published record (the most common duplicate shape, corrupting the PRISMA dedup count) and exiting 0 when every source failed; unescaped BibTeX output breaking any LaTeX build on a title containing `&`, `%` or JATS markup; and a tone linter flagging "garbage collection" as abuse while passing an actual personal attack.

**Process note**: all 17 were reachable by running the scripts against hostile input for ~20 minutes. None required reading the source carefully. This is the gap `quality-eval` (stage 4) is supposed to catch and has never run on any skill — the more valuable version of that stage is not "does the skill help vs. baseline," it is "does the skill do what its own SKILL.md says under inputs the author didn't imagine."

**Acted on, same day (thatlq1812 decision)**: that pass is now built into `quality-eval` v0.2.0 as **Pass A — contract conformance under adversarial input**, with the input classes distilled into `skills/quality-eval/references/adversarial-input-catalog.md` and a fixture generator at `skills/quality-eval/scripts/make_adversarial_fixtures.py`. **Running stage 4 on any skill remains deliberately deferred** — thatlq1812's call was to build the instrument now and not start evaluating yet. Do not run stage 4 in a later session without asking.

The instrument was exercised against the round's own output rather than left as prose: 21 fixtures generated, all run through the hardened `exploratory-data-analysis` with zero tracebacks — and it immediately found an 18th defect, this one in this session's own fix. The `inf` crash had been closed for the token form (`inf`, `-Infinity`) but not the value form: a literal like `1e400` passes any numeric pattern, overflows to infinity on `float()`, and crashed the identical `statistics.pstdev` call. Fixed and re-verified, along with typing an all-non-finite column as `all_non_finite` instead of mislabeling it text. The lesson is the same one the catalog closes with, now demonstrated on the fixer rather than the original author: a fix verified against the case you thought of is not a fix.

## Pipeline stage — which have an operating skill, which don't

| Stage | Operating skill | Status |
| --- | --- | --- |
| 1. Research | — | No skill yet; done manually many times (thatlq1812 + Claude) |
| 2. Elicit tacit process | — | No skill yet; done manually for each existing skill |
| 3. skill-creator | `skills/skill-creator/SKILL.md` | Yes |
| 4. Quality evaluation | `skills/quality-eval/SKILL.md` | Yes — design complete (v0.2.0: Pass A contract conformance + Pass B baseline delta, with fixtures/catalog/report template). Deliberately NOT run on any skill yet (thatlq1812 decision 2026-07-26) |
| 5. Security audit | `skills/security-audit/SKILL.md` | Yes — already applied for real to 5 skills (self-audit) |
| 6. Scout/harvester | `skills/scout-harvester/SKILL.md` | Yes |
| 7. License-compliance check | `skills/license-compliance-check/SKILL.md` | Yes |
| 8. Dedup/novelty-check | `skills/dedup-novelty-check/SKILL.md` | Yes |
| 9. Registry | `registry/SCHEMA.md` + `registry/skills.json` | Yes, 13 entries |

The pipeline skeleton (stages 3-9) is now fully staffed, 7/7 skills operating. Only gap: stages 1/2 (Research, Elicit tacit process) — not yet skill-ified, still done well manually; unclear whether skill-ifying them is even necessary.

## License debt ledger

Empty — no skill currently has `license_debt != null`. This table must be fully reviewed before the system leaves the bootstrap phase (before Phase 2, the legal vertical). See `docs/specs/STRATEGY_SPEC.md` §7 point 5, `registry/SCHEMA.md`.

| skill_id | Source | Reason for debt | Remediation plan | Date |
| --- | --- | --- | --- | --- |

## Infrastructure

- Git repo: initialized 2026-07-26, no remote yet.
- AI backend: none, by design — Scriptorium does not integrate Elixverse or any AI API (`docs/specs/STRATEGY_SPEC.md` §2, §6).
- Shared Python venv: `<repo_root>/.venv`, managed by `python-env-bootstrap`, gitignored. Currently holds dependencies for `document-ai-structurer`, `office-doc-creator`, `image-generator-gemini`.
