Đây là note từ vị trí của chính owner.

1. Các thư mục có thể tham khảo, đều nằm trong D:\elix , và có thể đọc các thư mục docs của các thư mục bên trong nó, cùng cấp với workspace này nếu cần

2. Bạn có thể chủ động clone mọi thứ mà bạn muốn, để tham khảo, trong quá trình làm việc ở dự án này

3. Với kinh nghiệm của tôi, việc tạo một .venv nội bộ là một cách khá tốt. Tôi cũng có ý nghĩa về một skill chứa đầy đủ một phiên bản python bên trong nó, để có thể chạy các tác vụ, mà khi người dùng phổ thông sử dụng các skill cần chạy lệnh phức tạp bằng code python, nó có thể đáp ứng được

4. Quan điểm của tôi là, một skill không nhất thiết chỉ là một thư mục chứa chỉ toàn text với text, mà có thể chứa nhiều hơn thế, thậm chỉ có thể chứa cả pandoc, các phiên bản latex nhỏ gọn, và bất kì cái gì có ích cho quy trình làm việc

5. Về edustation, nó đang bị biệt hóa ra quá nhiều skill, nhưng lại trống rỗng bên trong, chưa tối ưu, và chỉ tối ưu bởi các agent làm ra nó, chạy thử nó

6. Trên máy dev này, có khá đầy đủ công cụ mà bạn có thể sử dụng,
