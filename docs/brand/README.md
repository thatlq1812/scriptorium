# Brand assets — staged, not wired up yet

Scriptorium is a skill registry, not a running web/desktop app, so there is no `public/` or
packaging spec to drop these into yet. Staged here so they're ready the moment one exists (e.g. a
registry browser website, or a packaged CLI/desktop tool).

Source of truth: `marketing/assets/logos/subbrands/scriptorium-icon.svg` (and the pack this folder
is a copy of: `marketing/assets/logos/subbrands/pack/scriptorium/`). If the source SVG changes,
re-copy from there rather than editing files in this folder directly.

Monogram: "St". Border gradient: `#d4af37 → #818cf8 → #9f1239` (illuminated-manuscript theme: gold
leaf / ink / wax seal).

See `marketing/assets/logos/subbrands/pack/README.md` for what each file is and full-pack context.
