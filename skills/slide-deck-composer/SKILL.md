---
name: slide-deck-composer
description: Generates a real .pptx deck by cloning a caller-supplied real PowerPoint template's slides (design/fonts/colors/decorative shapes preserved) and injecting content via XML surgery — not drawing shapes from scratch. BYOT: caller supplies a `.pptx` template path, never bundled. Supports 6 layout types, a Vietnamese-diacritic font-safety checker, content-length font-size balancing, a thumbnail-grid renderer so the agent can VISUALLY pick which template slide to clone per content block (`thumbnail_grid.py` + `source_slide_idx`), real picture injection/replacement (`image_injector.py`), and a structural output validator (`validate_deck.py`). Use when an agent needs a professionally-designed deck from real content, given a real template. Do NOT use for flat title/bullet decks with no template (`office-doc-creator`'s pptx path) or to read/analyze an existing deck (`document-ai-structurer`).
license: MIT
compatibility: Requires Python 3.11+ + `python-pptx`/`lxml`/`Pillow`/`pypdfium2` (via `python-env-bootstrap`, shared `.venv`) + LibreOffice (`soffice`) for `thumbnail_grid.py` only, every other script is subprocess-free. Verified: Claude Code, Windows/PowerShell, LibreOffice at `C:\Program Files\LibreOffice\program\soffice.exe` (2026-08-01) — see "Verified".
metadata:
  domain: general
  task_type: document-conversion
  risk_tier: N2
  source: self-authored
  elicited_from: "Owner's own archived prior production platform, Elixverse (D:/elix/archive/platform_archive/modules/presentation/ — no license issue, owner's own code), per-file: scripts/constants.py <- pptx/constants.py + ooxml/constants.py (both independently defined the same real EMU/16:9 values, merged clean). scripts/text_utils.py <- pptx/text_utils.py (929 lines; ported the pure content-parsing layer, dropped the v2 JSON-metadata/auto-pagination Elixverse-specific fields not part of this skill's markdown+layout-name contract). scripts/rich_text.py <- pptx/rich_text.py (near-verbatim). scripts/shape_roles.py <- pptx/shape_roles.py (near-verbatim scoring heuristics; dropped grouped-shape recursion, out of v1 scope). scripts/slide_classifier.py <- pptx/slide_classifier.py (near-verbatim, then extended — see Verified bug #4). scripts/slide_duplicator.py <- pptx/compiler.py's PPTXCompiler._duplicate_slide/_strip_template_slides (the actual clone-and-inject XML-surgery primitive this skill's whole architecture is built around). scripts/font_manager.py <- pptx/font_manager.py (near-verbatim, read-only theme XML). scripts/font_engine.py <- pptx/font_engine.py (kept only set_theme_fonts; dropped apply_font_pack's Google-Fonts-download path, network I/O, out of scope). scripts/font_vietnamese.py <- layout_registry/font_config.py's FONT_METRICS table (35 fonts, real per-font supports_vietnamese data) cross-checked against template_factory/font_pairing.py's VIETNAMESE_SAFE_FONTS and this project's own skills/office-doc-creator ALLOWED_FONTS allowlist (every shared font agrees); refuse-rather-than-guess pattern matched from skills/light-logo-arranger/scripts/resolve_font_fallback.py (read in full before writing this module). scripts/font_size_balance.py — new feature, no direct port; heuristic designed from the owner's own stated framing (see module docstring for the exact quote and reasoning). scripts/placeholder_images.py <- pptx/placeholder_images.py's generate_placeholder_png (re-implemented, not verbatim — the source pulls in a separately-licensed stock-photo inventory this skill explicitly excludes; kept only the 'no network I/O, pure Pillow' path). scripts/image_formatter.py <- pptx/image_formatter.py (near-verbatim). scripts/compile_deck.py — self-authored entry point/orchestrator; mirrors the *shape* of PPTXCompiler.compile()'s call chain (see 'Architecture decision' below) without porting its ~1994 lines (dropped DB template loading, image prefetch via httpx, MD5 compile cache, PDF conversion via libreoffice subprocess — all confirmed non-portable/out-of-scope by this session's own research pass). v0.1.1: scripts/shape_capacity.py <- template_import/capacity.py's estimate_character_capacity (dimension-only EMU heuristic, ported near-verbatim) — added after a hands-on visual review (render to JPG via libreoffice) found shape_roles.py's scoring had no real notion of shape size, see Verified bugs #6-10. v0.2.0 (SOTA upgrade round): scripts/thumbnail_grid.py, scripts/image_injector.py, scripts/validate_deck.py — all self-authored, no direct port. thumbnail_grid.py's core idea (render every template slide to a labeled grid so the calling agent picks the source slide visually, instead of a script guessing from shape geometry) was CONCEPT-learned from reading Anthropic's own `pptx` skill (`anthropics/skills` repo, `skills/pptx/SKILL.md`'s `scripts/thumbnail.py`) for pattern research only — that skill's license is Proprietary (hard-BLOCKED per this project's CLAUDE.md principle 5, which names Anthropic's docx/pdf/pptx skills explicitly); no code, text, or its color-palette table was copied, this module's implementation (LibreOffice-to-PDF + pypdfium2 + Pillow grid composition, 0-based slide_idx labeling tied directly to compile_deck.py's own source_slide_idx contract) is independently written. validate_deck.py's `--original` baseline-diff idea was similarly concept-learned from the same source's `validate.py --original` pattern, not copied — every actual check it runs is grounded in this skill's own real, previously-documented bugs (#1-10 below), not generic guesses. image_injector.py generalizes a real working prototype built earlier this session (`tests (ignore)/slide-deck-composer/swap_images.py`, a zip-level media-byte-swap proof of concept) into a python-pptx relationship-part-level implementation — see 'Architecture decision' below for why the zip-level approach doesn't work once `slide_duplicator.duplicate_slide`'s shared-image-part behavior is accounted for."
  version: 0.2.0
---

# slide-deck-composer

Clones a real, professionally-designed `.pptx` template's own slides and injects caller-supplied content into them via lxml XML surgery — the template's actual design (fonts, colors, layout geometry, decorative elements) survives untouched. This is deliberately **not** a from-scratch layout builder: an agent choosing "which shape at which coordinates" produces generic geometric "AI slop" with no real design DNA. This skill keeps the agent's job where it's actually strong — content-to-slot mapping, judgment on what to emphasize, which template best fits the deck's real content — and leaves visual design to a real designer's real template.

## Architecture decision: clone-and-inject, not build-from-scratch

Two approaches were evaluated against the source platform's ~90-file presentation engine (`D:/elix/archive/platform_archive/modules/presentation/`):

1. **Build-from-scratch** (`ooxml/layout_builder.py` + `shape_factory.py`) — rejected. Forces the agent to invent layout/design from raw coordinates with ~5 generic shape primitives.
2. **Clone-and-inject** (`pptx/compiler.py` + `pptx/slide_builder.py`) — **chosen**. Clone a real template slide, replace its text in place.

**A further deliberate simplification versus the source system**: the source's `SlideBuilder._add_slide()` clones an empty **Slide Layout** and fills placeholders by role name from a `TemplateManifest` (a declarative idx-mapping layer, `pptx/template_manifest.py`, 1071 lines). This skill instead clones a real, already-content-bearing **slide** (`slide_duplicator.duplicate_slide`, ported from `PPTXCompiler._duplicate_slide`) and runs `shape_roles`' heuristic scoring directly against its real shapes to decide which cloned shape is the title vs. body. **No `template_manifest.py`-equivalent placeholder-idx mapping layer exists in this skill, and that is intentional, not a gap**: a manifest's whole job is figuring out which placeholder idx on an *empty layout* plays which role — this skill never touches an empty layout, only a real slide whose shapes' actual size/position/font/paragraph-count are directly measurable, which is exactly what `shape_roles.score_title_shape`/`score_body_shape` already consume. Verified end-to-end against three real Slidesgo templates with wildly different layout structures (see "Verified") with zero placeholder-idx bugs — the scoring approach generalizes better across templates that don't share a common Slide Layout ordering, which real-world Slidesgo/Canva/Google-Slides templates never do.

## Architecture decision (v0.2.0): agent-driven slide selection over pure heuristics

v0.1.1's hands-on visual-review round (bugs #6-10) proved a structural point: automatic slide-classification heuristics are an arms race against every new template's idiosyncrasies (icon-grids, world-map infographics, color-palette meta-slides each fooled the geometry scoring in turn, one fix at a time). The actually-right fix isn't a smarter heuristic, it's removing the guess entirely for the cases that matter: `scripts/thumbnail_grid.py` renders every real template slide to a labeled grid image so the CALLING AGENT looks at it and decides, the same way a human designer would — then `compile_deck.py`'s optional `source_slide_idx` field lets that decision flow straight through, skipping `pick_source_slide_index`'s heuristic for that slide entirely. This matches this project's own stated philosophy this round (owner, verbatim): "với độ thông minh của agent hiện tại, việc cung cấp hạ tầng và bối cảnh là đủ, thêm nữa là thừa và lấy đi mất khả năng sáng tạo của agent" — a capable agent with good infrastructure and context needs exactly that, not another layer of scripted judgment calls on top. The automatic heuristic is NOT deprecated by this — it's demoted to what it always should have been: a legitimate lower-effort fallback for when a caller doesn't want to spend the extra round-trip, not the only path.

## BYOT (Bring Your Own Template) — hard constraint

The caller supplies a `.pptx` template file path they already have rights to use. This skill's own `scripts/`/`assets/` **never** bundle, reference, or hardcode any specific template file or filename (verified — see "Security & isolation self-check" below). Real Slidesgo `.pptx` files under `data/slidesgo_templates/` in this repo are **local test fixtures only**, used exclusively for this skill's own development/verification and never copied into the skill folder or referenced by path from any shipped script.

## Usage

**Recommended workflow for quality-sensitive decks** — render the template's slides first, look at them, choose deliberately:

```bash
# 1. Render every slide of the template to a labeled thumbnail grid.
.venv\Scripts\python.exe skills\slide-deck-composer\scripts\thumbnail_grid.py <template.pptx> <output_prefix> [--cols 4] [--per-grid 24]
# -> writes <output_prefix>_grid_1.jpg, _grid_2.jpg, ... each cell labeled "slide_idx=N"
# (0-based, matches prs.slides[N] for this template file exactly)

# 2. Look at the grid image(s). Pick which real slide fits each content block.

# 3. Compile, passing the exact indices you chose via source_slide_idx.
.venv\Scripts\python.exe skills\slide-deck-composer\scripts\compile_deck.py <template.pptx> <content.json> <output.pptx> [--heading-font "Name"] [--body-font "Name"] [--vietnamese]

# 4. Validate the real output structurally before trusting it.
.venv\Scripts\python.exe skills\slide-deck-composer\scripts\validate_deck.py <output.pptx> [--original <template.pptx>]
```

Skipping step 1-2 (omitting `source_slide_idx` on every slide) is a legitimate, still-fully-supported lower-effort fallback: `compile_deck.py` falls back to the automatic geometry-based heuristic exactly as before, unchanged, for any slide without an explicit index.

`content.json`:
```json
{
  "slides": [
    {"layout": "title", "markdown": "# Deck Title\nA subtitle line", "source_slide_idx": 0},
    {"layout": "standard", "markdown": "## Heading\n- bullet one\n- bullet two"},
    {"layout": "two_column", "markdown": "## Compare\n### Left\n- a\n### Right\n- b"},
    {"layout": "image", "markdown": "## Caption heading\nCaption body text",
     "image_path": "assets/photo.jpg"},
    {"layout": "image", "markdown": "## Placeholder example\nNo real photo yet",
     "generate_placeholder_image": true},
    {"layout": "quote", "markdown": "# \"A standalone statement.\""},
    {"layout": "section", "markdown": "## Section N\nSection subtitle"}
  ]
}
```

`layout` must be one of exactly these 6 keys (`compile_deck.py` refuses loudly, non-zero exit, on any other value — see "Known limitations" for the layout types not yet built). `--vietnamese` requires every font actually used (template theme fonts, or `--heading-font`/`--body-font` overrides) to have confirmed Vietnamese diacritic support; an unmapped font name is refused loudly rather than risking garbled diacritics.

`source_slide_idx` (optional int, any layout): explicitly clones `prs.slides[source_slide_idx]` for this output slide, bypassing the automatic heuristic. Still refused loudly (never silently guessed past) if the chosen slide genuinely lacks a scorable title shape or enough body shapes for the layout.

`image_path` / `generate_placeholder_image` (optional, `"image"` layout only): actually replace the cloned slide's real picture — a caller-supplied file, or (no `image_path` given) a deterministic no-network Pillow placeholder. A markdown `![alt](path)` reference in the slide's own `markdown` is honored as a fallback `image_path` source. Neither field set = the template's own original picture is left untouched (unchanged pre-v0.2.0 behavior).

Output is a JSON report on stdout: which real template slide was cloned for each output slide (and whether that choice was explicit or automatic), whether it was a structural match, how many stale template shapes were blanked, every font-size-balancing step actually applied, and (when an image was injected) the real injection report (frame size, whether letterboxing was applied, whether a pre-existing crop was cleared) — inspect this to confirm the compile did what you expect before trusting the `.pptx` blind, or run `validate_deck.py` for a mechanical check.

## The two owner-requested features (v0.1.x)

**1. Vietnamese font-safety checker** (`scripts/font_vietnamese.py`) — `check_font_vietnamese_safety(font_name)` reports `safe` / `substitute` (with a real same-category substitute) / `unknown` (refuses loudly — never guesses). Ported from a real, measured 35-font table (`FONT_METRICS` in the source's `layout_registry/font_config.py`), not invented. `compile_deck.py --vietnamese` wires this to BOTH the template's own theme fonts (read via `font_manager.FontManager`) and any caller-requested `--heading-font`/`--body-font` override, and writes the resolved font explicitly onto every run (`rich_text.apply_runs_to_paragraph`'s `base_font_name`) — not just the theme XML — so diacritic safety holds even for runs that would otherwise inherit an unsafe theme font.

**2. Content-length font-size balancing** (`scripts/font_size_balance.py`) — compares the just-cloned shape's ORIGINAL template text length against the caller's REPLACEMENT text length; every +50% over original length steps the font down one discrete tier (title: 4pt/step, floor 18pt; body: 2pt/step, floor 10pt; capped at 4 steps). Deterministic, not continuous scaling — see the module docstring for the full reasoning, including why the step unit is 50% (short version: `text_utils.enable_autofit`'s OOXML `normAutofit` already covers the 0–50% band at PowerPoint-open-time; this module pre-empts the harder cases with an explicit, predictable size instead of relying on one uniform autofit pass).

## Real picture injection (v0.2.0) — `scripts/image_injector.py`

Generalizes a real working prototype built earlier this session (`tests (ignore)/slide-deck-composer/swap_images.py`, a zip-level media-byte-swap proof of concept that never touches shape XML). That prototype's technique doesn't survive contact with `slide_duplicator.duplicate_slide`, though: `duplicate_slide` re-relates each cloned slide's picture to the SAME underlying image PART object (python-pptx's `relate_to(rel.target_part, ...)` does not duplicate media bytes), so a raw zip-level swap of `ppt/media/imageN.png` would silently overwrite that image for EVERY slide sharing it, not just the one being targeted. This module instead adds a genuinely NEW image part per replacement via `SlidePart.get_or_add_image_part`, so each slide's swap is independent.

**Granularity actually achieved (be honest about scope, not overclaiming)**: per-slide, single-picture. `image_injector.pick_best_picture` selects ONE top-level (non-grouped — same deliberate scope limit as `shape_roles.collect_text_shapes`) `Picture` shape per slide via `shape_roles.score_picture_shape` (largest/most-centered wins). A slide with multiple independently-addressable pictures is not supported this round — only the best-scoring one is replaced.

**Aspect-ratio handling actually achieved**: the replacement bitmap is letterboxed (padded, never stretched) to match the TARGET SHAPE's own frame aspect ratio (`shape.width/height` in EMU), and any pre-existing `<a:srcRect>` crop on the shape is explicitly removed — a crop rectangle authored against the OLD photo's pixels has no defined meaning against a brand-new image and would otherwise silently produce an arbitrary, wrong crop of the replacement. This is the "resize/letterbox to the frame's own aspect ratio" tier from this round's brief, not full crop-aware resizing against the original photo's own `srcRect` geometry.

**A real finding from this round's visual-review loop, investigated and resolved (not a defect in the end)**: the first real-template test (`Business Innovation Plan by Slidesgo.pptx`, source slide 13, a phone-mockup graphic) appeared UNCHANGED after injection when inspected at the `thumbnail_grid.py` default 480px-wide thumbnail resolution — alarming, since this skill's whole discipline is "don't trust a report, look at the render." Investigation (extracting the swapped media part directly, then re-rendering that one slide at 4x scale via `pypdfium2`) confirmed the swap WAS correct and visible — the target shape's frame is a narrow ~1.8in-wide sliver (a phone screen inside a larger illustration), so a thumbnail-grid cell for it is only ~65px wide, too small to distinguish a solid blue replacement photo's fine white border/caption text from the original's similarly-blue-toned sprite-sheet crop at a glance. Real lesson banked for future visual-review rounds: `thumbnail_grid.py`'s default resolution is right for SLIDE-level layout judgment (which template slide fits which content), not for verifying a small shape's exact pixel content — that needs a targeted higher-resolution single-slide render, which `thumbnail_grid.py`'s `pdf_to_thumbnails(width_px=...)` already supports as a parameter for exactly this case.

## Verified

Ran the required self-test loop (create sample content → compile → inspect real defects → fix engine → re-run → confirm) against **real Slidesgo templates**: `data/slidesgo_templates/Business Plan_ Minimalist Aesthetics by Slidesgo.pptx` (55 slides, 30 layouts, minimal/corporate), `Candy Pastels Galaxy by Slidesgo.pptx` (62 slides, 37 layouts, colorful/creative), `Elegant Black & White Style by Slidesgo.pptx` (41 slides, 22 layouts) — plus 2 synthetic python-pptx-built templates (`template_corporate_minimal.pptx`, `template_creative_colorful.pptx`, built from a shared helper script, not part of this repo) used for earlier fast iteration before the real fixtures were confirmed accessible in this environment. Three content sets: a 5-slide short pitch outline, a 3-slide deliberately content-heavy set (to exercise font-size balancing), a 3-slide Vietnamese-diacritics set (to exercise the font-safety checker). All outputs re-opened via `python-pptx` (zero exceptions), zip-integrity-checked (`zipfile.testzip()`, all clean), and inspected via direct XML read-back for `rFonts`/font-size/`normAutofit` completeness.

**5 real bugs found and fixed during this loop:**

1. **Naive round-robin source-slide selection caused silent content loss and stale-text leakage.** First run (`template_corporate_minimal.pptx` + short-pitch content) blindly round-robinned through the template's content-slide pool without checking whether the picked slide's shape count matched the requested layout. Result: a "standard" bullets request landed on a two-column source slide (leaving its second placeholder's ORIGINAL template demo text — `'Right demo point'` — visible in real output), and a genuine "two_column" request landed on a single-body source slide, **silently dropping the entire right-column content** (`'One live model / Everyone sees the same numbers'` vanished with no error). Fixed: `pick_source_slide_index` now requires a source slide have both a scorable title shape AND enough body-candidate shapes for the target layout (`_MIN_BODY_REQUIRED` + `_fits`) before it's eligible, round-robining only among structurally-matching candidates; a post-injection sweep (`_clear_unused_text_shapes`) additionally blanks any leftover non-decorative text shape not used for this slide's content, so stale demo text can never survive even when a perfect match isn't available. Re-ran: both columns now present, zero stale text.
2. **Shape-identity bug wiped ALL just-injected content.** The stale-text cleanup sweep (fix for bug #1) tracked "used" shapes by `id(python-pptx-wrapper-object)`, but `shape_roles.find_title_and_body()` and the separate `collect_text_shapes()` cleanup pass each call `.shapes` independently — python-pptx returns a **new wrapper object with a different `id()`** on each access, even for the identical underlying XML element. The cleanup sweep therefore saw every just-injected shape as "unused" and blanked it — re-opening the output showed every text frame empty. Fixed: identity now compares `id(shape._element)` (the stable lxml Element), not `id(shape)`. Re-ran: all content present.
3. **LAYOUT_SECTION silently dropped subtitle content for a common markdown shape.** `## Ask\nWe are raising a $1.5M seed round` (a single heading + one plain body line, no second heading) parses via `text_utils._parse_standard_slide`, which routes an un-headed body line into `bullets`, not `subtitle` (only an explicit H1+H2 pair populates `subtitle`). `compile_deck.py`'s SECTION branch only checked `content.subtitle`, so the whole second line vanished from output with no error. Fixed: falls back to `" ".join(content.bullets)` when `subtitle` is empty. Re-ran: subtitle text present.
4. **English-only vendor-instruction-slide detection missed a real non-English template page.** Real template `Business Plan_ Minimalist Aesthetics by Slidesgo.pptx`'s slide 1 is a vendor "About this template" page written in **Portuguese** (`"Conteúdo deste modelo" / "Isto é o que você vai encontrar nesse modelo de Slidesgo: ..."`). `slide_classifier._INSTRUCTION_MARKERS` is an English-phrase list, so it never matched; the slide slipped into the content pool, got picked as a clone source, and its Portuguese instructional paragraph leaked into real output. Worse, because the paragraph happened to name "Slidesgo", `shape_roles.is_watermark_text` also flagged it as an immutable watermark — which is *correct* behavior for a genuine short attribution footer (must never be scrubbed, per Slidesgo's own license terms) but had no length ceiling, so it also shielded a full paragraph from bug #1's cleanup sweep. Fixed with a language-independent structural signal: any text shape longer than 40 characters that names a vendor brand (`slidesgo`/`freepik`/`flaticon`/`storyset`/`wepik`) now flags the whole slide as instructional regardless of the surrounding language — a legitimate footer credit is always short; a full paragraph mentioning the same brand is instructional content in any language. Re-ran: Portuguese slide excluded from the content pool, output clean.
5. **Closing-slide reuse could pick a title-less picture-only slide, silently dropping content.** Real template `Elegant Black & White Style by Slidesgo.pptx`'s classified "closing" slide is a full-bleed `BLANK`-layout slide containing exactly one `Picture` shape and **zero text frames**. The old structural filter only checked body-shape count (`min_body=0` for a `quote` layout lets anything through), so it happily picked this slide as the clone source for a quote — and the quote's entire text had nowhere to go (`title_shape_found: false` in the JSON report, confirmed by re-opening the output: the slide had no text at all). Fixed: `_fits()` now requires a scorable title shape on the candidate in addition to the body-count check, for every pool (title/closing/content); when the classified closing pool has no title-capable slide, selection now falls through to the general content-pool search instead of guaranteeing content loss. Re-ran: `title_shape_found: true`, quote text present.

**v0.1.1 — 5 more real bugs found and fixed via hands-on visual review (2026-08-01).** The bugs above were all caught by structural/XML inspection, never an actual rendered image. The owner asked for a real 10-slide test deck (`tests (ignore)/slide-deck-composer/content.json`, a real deck introducing this project) against a real Slidesgo template (`Business Innovation Plan by Slidesgo.pptx`), then rendered it to JPG via `libreoffice --headless --convert-to pdf` + `pypdfium2` for actual visual inspection — something the original build round explicitly skipped ("libreoffice not installed," see "Other things directly verified" above; it was installed by the time of this round). The very first render immediately surfaced defects invisible to XML-level checks:

6. **Flat placeholder-type scoring let a tiny icon-grid caption box outscore a genuinely spacious body placeholder.** `shape_roles.score_body_shape` gave any `BODY`/`OBJECT`/`CONTENT`-typed shape a flat `+1000` bonus regardless of actual size. A template slide styled as a 6-item icon grid has 6 tiny `CONTENT`-typed caption placeholders (each maybe 30-40 real characters of capacity) sitting beside large decorative icon shapes; one of those captions won the ranking purely from the type bonus and got a whole 4-bullet paragraph crammed into it — rendered, the 6 unrelated icons visually dominated the slide while the real content was unreadably small in a corner. Fixed two ways: (a) new `scripts/shape_capacity.py` (ported from `D:/elix/archive/platform_archive/modules/presentation/template_import/capacity.py`'s dimension-only EMU heuristic — no font-metrics dependency) gives every scored shape a real `capacity_chars` estimate from its actual width/height/font-size; (b) `score_body_shape`'s placeholder-type bonus was capped from `+1000`/`+400` down to `+150`/`+80`, with `capacity_chars` (capped at 600) now the dominant term, so a genuinely large body placeholder wins over a same-typed tiny one.
7. **Short content still fit a tiny caption box via relative capacity slack, but the box remained the wrong SHAPE for prose.** After fix #6, a short section subtitle ("Why Scriptorium exists", ~23 chars) still cleared an icon-caption's tiny capacity via `_CAPACITY_SLACK` — technically enough room, but the slide's 6 unrelated icons still visually dominated. Fixed with `_MIN_PROSE_CAPACITY_CHARS = 120`: any layout injecting real prose now requires a source slide's best body candidate to clear at least 120 characters of capacity regardless of how short the caller's actual text is — a real sentence deserves a real text box, not a numeral badge.
8. **`quote`/`title` layouts never touch a body shape at all, so an icon-grid slide's title being normal-sized let it slip through every capacity check.** A quote's text is injected into the TITLE shape, not body — and an icon-grid slide's title placeholder is usually a completely normal, well-sized heading, separate from its 6 tiny icon captions. Neither fix #6 nor #7 touches title selection, so the icon-grid slide still got picked for a quote, again leaving 6 unrelated icons in frame beside an otherwise-clean quote. Fixed with a new `max_unused_body_shapes` cap in `_fits()`: `quote`/`title` layouts now reject any candidate slide with more than 2 body-scored shapes outright, since a real quote/title-only template slide is expected to be visually minimal.
9. **A different infographic pattern (a decorative world-map graphic with 19 small per-region caption boxes) passed every check above** (its title was fine, and — with 19 candidates to choose from — at least one had adequate capacity), and got picked for a plain 6-bullet `standard` slide: the map was irrelevant to the content, and the bullets landed cramped in one small map-label box. Fixed by generalizing fix #8's shape-count cap to ALL layout types, not just quote/title: `_MAX_BODY_SHAPES_FOR_PROSE = 8` — a normal single-message template slide has a small, coherent number of body candidates (typically 1-4); a slide scoring far more is a structural signal of an infographic/map/diagram design regardless of any individual candidate's capacity.
10. **Fixing #9 shifted which slide round-robin landed on for one position, exposing a template meta-slide that was never supposed to be in the content pool at all.** The newly-exposed slide's title was literally "Fonts & colors used" (a Slidesgo template's own credits page documenting its design system) — real, well-sized, normal-capacity text (306 chars, 24% area), so none of fixes #6-9 (all shape-geometry checks) could have caught it; the defect here is semantic, not geometric. `slide_classifier._INSTRUCTION_MARKERS` already had `"fonts used"` from bug #4's round, but `"fonts & colors used"` doesn't contain that as a substring (the `"& colors"` breaks the match) — a near-miss, not a missing category. Fixed by adding `"fonts & colors used"`, `"fonts and colors used"`, `"colors used"`, and the credits paragraph's own opening phrase as additional markers. This bug is the clearest illustration in this skill's whole build/fix history that geometry-based filtering (fixes #6-9) and content-based classification (`slide_classifier`) are solving genuinely different problems and neither substitutes for the other.

All 10 fixes independently re-verified by re-running the exact same real 10-slide test deck against the same real template and re-rendering to JPG after each fix — the final render shows all 10 slides clean: readable body text correctly sized, no unrelated decorative/infographic slides picked, no template meta-content leaking through. Test artifacts (content.json, every render round's `.pptx`/`.pdf`/`.jpg`) live in `tests (ignore)/slide-deck-composer/` (gitignored, not part of this skill's shipped assets).

**Other things directly verified, not just claimed:**
- Font-size step-down actually lands in the XML at the exact expected point value: e.g. title ratio 4.37x -> 4 steps -> floor 18pt, confirmed via direct `r.font.size` read-back (`228600` EMU / 12700 = 18.0pt exactly) on real output.
- `<a:normAutofit/>` present on every injected text frame's `bodyPr` (confirmed via direct XML read-back) as the PowerPoint-open-time safety net beneath the explicit step-down.
- Decorative shapes (a colored accent bar + circle placed directly on individual slides, not the layout) survive `duplicate_slide`'s spTree clone unchanged on every one of 5 cloned slides (2 autoshapes/slide, confirmed via `shape_type` enumeration) — direct proof the clone-and-inject mechanism preserves template visual identity, not just placeholder text.
- Vietnamese diacritics (`Chiến Lược Kinh Doanh 2026`, `Đà Nẵng`, `Cần Thơ`, etc.) round-trip correctly through markdown parsing -> injection -> file save -> re-open, confirmed by direct UTF-8 read-back of every run's text and `font.name` against a real template whose own theme font (`Garamond`) is Vietnamese-**unsafe** — confirmed the checker correctly substituted `Palatino Linotype` and that substitute is written explicitly on every affected run's XML (`w:rFonts`-equivalent `a:latin`/run-level `font.name`), not just left to theme inheritance.
- Refuse-loudly paths: unknown layout name (`"chart"`) exits 2 with a message naming the 6 supported layouts; `--vietnamese` + an unmapped font (`"Comic Neue"`) exits 2 without silently guessing.
- `libreoffice`/`soffice` was not installed during the original build round, so the optional visual render check was skipped there (permitted by that round's task brief). It was confirmed installed for the v0.1.1 hands-on visual review round (`C:\Program Files\LibreOffice\program\soffice.exe`) and used for real: `--headless --convert-to pdf` + `pypdfium2` PNG/JPG rendering, the actual mechanism that surfaced bugs #6-10.

**v0.2.0 (this round, 2026-08-01) — thumbnail-grid selection + real image injection + validator, verified against a real Slidesgo template.**

- `thumbnail_grid.py` run for real against `data/slidesgo_templates/Business Innovation Plan by Slidesgo.pptx` (42 real slides): produced 2 real JPG grid files (24 slides/grid), each cell correctly labeled `slide_idx=N` 0-based, confirmed by visual inspection that the labels exactly match the template's real slide content and order (slide_idx=0 is the cover, slide_idx=41 is the last content slide, etc.) — the numbering contract this whole feature depends on is correct, not just assumed.
- `compile_deck.py`'s new `source_slide_idx` field run for real: a 6-slide content.json explicitly chose slide indices 0, 5, 21, and 13/11 (for two image-layout slides) after visually reading the rendered grid; the output JSON report's `slide_plan` confirmed every explicit choice was honored EXACTLY (`source_slide_idx_explicit: true`, `source_slide_idx` matching the requested value in every case) and a 4th slide with NO `source_slide_idx` field correctly fell back to the automatic heuristic (`source_slide_idx_explicit: false`, picked slide 3 on its own).
- Backward compatibility confirmed: re-ran the pre-existing `tests (ignore)/slide-deck-composer/content.json` (the real 10-slide test deck from the v0.1.1 round, no `source_slide_idx`/`image_path` fields at all) against the same template — compiled cleanly, 10 slides generated, zip-integrity clean, matching pre-v0.2.0 behavior exactly.
- Real picture injection verified two ways: (a) a real caller-supplied PIL-generated test JPG/PNG injected into source slide 13's picture shape, confirmed via direct media-part extraction that the swapped bytes are exactly the caller's image, and via a targeted 4x-scale single-slide render (see the "real finding" note above — the default thumbnail resolution was too small to tell at a glance, a genuinely useful lesson from this round, not a defect); (b) `generate_placeholder_image: true` on a second slide (source slide 11) confirmed the deterministic Pillow placeholder engine (`placeholder_images.generate_placeholder_png`) rendered correctly in place of the original picture, clearly visible even at default thumbnail resolution.
- Refuse-loudly paths for the new features, all confirmed exit 2 with a specific, correct message: `source_slide_idx` out of range (999 against a 42-slide template); `source_slide_idx` structurally impossible for the requested layout (slide 0, which has only 1 body shape, requested for `two_column` which needs 2 — message names the exact shortfall); `image_path` pointing at a file that doesn't exist (message includes the resolved absolute path); an image-layout slide with `generate_placeholder_image: true` whose explicitly-chosen source slide has zero top-level picture shapes to inject into.
- `validate_deck.py` run for real against the v0.2.0 compiled output plus `--original` pointed at the real template: all 7 checks passed (`all_checks_passed: true`). Then verified the validator actually CATCHES real defects, not just passes happy paths: (a) a deliberately corrupted (non-zip) file correctly failed `zip_integrity` and short-circuited the remaining checks, exit 3; (b) a deliberately-crafted dangling relationship (a real `<Relationship Type=".../image">` element removed from a slide's own `.rels` part while the slide XML still referenced its `r:id`) correctly failed `no_dangling_relationships` with the exact slide/rId named, exit 3.
- Security self-check re-run for the 3 new scripts (see "Security & isolation self-check" below): only 1 new `subprocess` call, in `thumbnail_grid.py`, fixed argument list, no `shell=True`, documented inline.

## Security & isolation self-check

`grep -rnE "subprocess|os\.system|eval\(|exec\(|urllib|requests|socket|shell=True" scripts/*.py` (re-run for v0.2.0, all 3 new scripts included) — **1 legitimate match**: `thumbnail_grid.py`'s `subprocess.run([soffice, "--headless", "--norestore", "--convert-to", "pdf", "--outdir", str(workdir), str(template_path)], capture_output=True, text=True, timeout=180, shell=False)` — a fixed argument list built entirely from a hardcoded flag set + two `Path` objects, no `shell=True`, no string interpolation into a shell command, documented inline in the function's own docstring (same convention as `latex-project-bootstrap`'s existing `typst`/`pandoc` subprocess calls). Every other match is the same pre-existing false-positive as v0.1.1 (the English word "explicitly" containing the substring "exec"). No network, shell-injection, or `eval`/`exec` calls anywhere in this skill's own code. `grep -rli "slidesgo_templates\|data[\\/]slidesgo"` across the whole skill folder — **clean**, zero matches in any script (SKILL.md itself legitimately names real Slidesgo templates in prose describing what was tested, same as v0.1.1); the literal `"slidesgo"` string hits that do exist in scripts (`shape_roles.WATERMARK_TEXT_PATTERNS`, `slide_classifier._BRAND_WATERMARK_WORDS`/`_INSTRUCTION_MARKERS`) are heuristic **detection** strings for recognizing vendor watermark/instruction text inside a CALLER-supplied template at runtime — not a bundled asset reference or hardcoded path. No `.pptx`/`.pdf`/`.jpg`/`.png` file exists anywhere under `skills/slide-deck-composer/`.

## Known limitations

- **Only 6 of ~20 layout types are implemented**: `title`, `standard` (bullets), `two_column`, `image`, `quote`, `section`. **NOT built this round either** (refused with a clear error naming the 6 supported types, never silently downgraded to a similar one): `three_column`, `table`, `code`, `timeline`, `chart`, `stat`, `diagram`, `pros_cons`, `steps`, `qa`, `icon_grid`, `center_generic`, `end_card`, `fullbleed_image`. This round's P1 priority ("expand layout coverage" — `icon_grid`/`stat`) was explicitly deferred, not attempted: the time budget went to P0-A (thumbnail-grid selection) and P0-B (real image injection) instead, both fully verified against real templates; building a NEW layout type this round would have meant less real verification depth on those two, and this project's own discipline (`CLAUDE.md`: "one skill that runs well ... beats ten unused skills") favors that trade. `shape_roles`' icon-grid/infographic DETECTION logic (`_MAX_BODY_SHAPES_FOR_PROSE`, `max_unused_body_shapes`) from v0.1.1 is real and already proven against real templates — a future round building `icon_grid`/`stat` layouts would start from a solid, already-verified detection base, not from scratch.
- **`image` layout picture injection (v0.2.0) is real but single-picture-per-slide granularity** — see "Real picture injection" above for exactly what was and wasn't built (per-slide best-picture selection via `shape_roles.score_picture_shape`, frame-aspect-ratio letterboxing + crop clearing, not full crop-aware resizing against the original photo's own `srcRect`). A slide with multiple independently-addressable pictures only gets its single best-scoring one replaced. `scripts/image_formatter.py` (rounded-corner/border/shadow finishing on an already-inserted picture) remains implemented and independently testable but still not wired into `compile_deck.py` — a natural follow-up now that real injection exists to finish on top of.
- **Grouped shapes (`<p:grpSp>`) are skipped entirely** by `shape_roles.collect_text_shapes` AND `image_injector.find_top_level_pictures` (same deliberate scope limit, kept consistent this round) — text or pictures inside a template's grouped decorative composition are never scored as title/body/image candidates (preserved untouched by the clone, never accidentally targeted, but also never a valid injection target).
- **`thumbnail_grid.py` requires a real LibreOffice install** (`soffice`) — the only script in this skill with an external-binary dependency; every other script (including `compile_deck.py` itself) remains subprocess-free. If `soffice` isn't installed, the recommended agent-driven-selection workflow degrades gracefully to the automatic-heuristic fallback (still fully functional), just without the visual-picking step.
- **No auto-pagination.** If real content is far too long to fit even after font-size balancing, this skill does not automatically split it across additional slides (the source system's `split_content_for_pagination` was deliberately not ported — out of scope). `text_utils.truncate_text`'s hard character limits are the only backstop against runaway text.
- **No `--vietnamese` auto-detection.** The flag must be passed explicitly by the caller; content containing Vietnamese diacritics compiled without `--vietnamese` will use whatever font the template/caller specifies, safe or not.
- Not yet tested against templates with embedded charts, SmartArt, or video — `slide_duplicator.duplicate_slide` clones their XML/relationships generically (the same rId-remap logic that handles pictures) but this was not specifically exercised in this round's real-template tests either.
- **P2 items (font QA-reliability tagging, `references/design-guidance.md`) were not attempted this round** — explicitly out of scope given the P0/P1 time budget; not started, not partially started, nothing to report.
- Hasn't gone through stage 4 (formal multi-harness quality eval) — per this project's `CLAUDE.md`, this is a deliberate hold on the formal scoring gate, not a statement about implementation quality; security-audit self-check passed (see above), and 10 real bugs across two rounds (5 structural, 5 visual-review) plus this round's real image-injection/thumbnail-grid verification have been found/fixed/confirmed against real adversarial template content.

## What this skill does NOT do

- Does not invent visual design, layout geometry, or color choices from scratch — see "Architecture decision" above. If the caller has no template, use `office-doc-creator`'s flat pptx path instead.
- Does not download, embed, or manage fonts (Google Fonts fetch, font embedding) — `font_engine.set_theme_fonts` only rewrites the theme XML's font *name* reference; if that font isn't installed on the machine that later opens the file, PowerPoint substitutes its own fallback, same as any `.pptx`.
- Does not call any AI/image-generation backend — per this project's `CLAUDE.md` principle 8, Scriptorium never integrates an AI API. Picture content is the template's own original image (untouched, when no `image_path`/`generate_placeholder_image` is given), a caller-supplied real file, a deterministic Pillow placeholder, or something a caller-side skill (e.g. `image-generator-gemini`) generates and hands over as a file path.
- Does not verify or fact-check the caller-supplied content — this is a rendering/injection engine, not a research or grounding tool (pair with `deep-research`/`literature-review`/`citation-management` upstream if factual grounding is required).
- Does not read/analyze an existing presentation — that's `document-ai-structurer`'s job.
- `compile_deck.py` itself still does not convert to PDF or render a preview image — the compile contract ends at a valid `.pptx` file. `thumbnail_grid.py` (v0.2.0) is a deliberate, separate, opt-in tool that DOES call a real `libreoffice` subprocess (fixed argument list, no `shell=True`, documented in "Security & isolation self-check") specifically to render a template's slides for agent-driven selection — this is a scoped exception to the prior round's "no shell-out by default" stance, not a reversal of it: the compile pipeline's own security posture is unchanged, and the one new subprocess call is isolated to a single opt-in script.

## Composability

- **`image-generator-gemini`** (BYOK image generation): now a real, wired filler for the `image` layout's picture slot (v0.2.0) — the calling agent generates an image via that skill, then passes the resulting file path as `content.json`'s `image_path` (or a markdown `![alt](path)` reference); `compile_deck.py`'s `LAYOUT_IMG` branch calls `image_injector.inject_slide_image` to swap it in for real, letterboxed to the target shape's frame aspect ratio. Verified this round with a real Pillow-generated test file standing in for `image-generator-gemini`'s actual output (same file-path contract, no live chaining test run since that would require actually invoking the other skill).
- **`deep-research` / `literature-review` / `citation-management`**: their findings/summaries are natural sources for a `content.json`'s markdown bullets — this skill takes already-written content, not raw research.
- **`personal-profile-manager`**'s `org_profile` section (org name/logo): natural source for a title slide's subtitle line or a caption on a closing slide — this skill has no org-identity concept of its own, it only injects whatever text the caller supplies.
- **`office-doc-creator`**: sibling document-creation skill, **no scope overlap** — `office-doc-creator`'s pptx path is flat title/bullet-only with no template support; this skill is the real template-clone engine for when a caller has (or is given) an actual designer template.
- **`light-logo-arranger`**'s `scripts/resolve_font_fallback.py`: `font_vietnamese.py`'s refuse-rather-than-guess pattern is the same discipline, applied to a different question (Vietnamese diacritic support vs. geometric font substitution) — read together, they're two instances of one project-wide convention: never silently guess a font behaves a way it hasn't been verified to behave.
